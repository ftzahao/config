# @generated by usage-cli from llama-download/zh-CN.usage.kdl

if (-not (Get-Command 'usage' -ErrorAction SilentlyContinue)) {
    Write-Warning "Error: usage CLI not found. This is required for completions to work in llama-download."
    Write-Warning "See https://usage.jdx.dev for more information."
    return
}

Register-ArgumentCompleter -Native -CommandName 'llama-download' -ScriptBlock {
    param($wordToComplete, $commandAst, $cursorPosition)

    $tmpDir = if ($env:TEMP) { $env:TEMP } else { [System.IO.Path]::GetTempPath() }
    $specFile = Join-Path $tmpDir "usage__usage_spec_llama_download.kdl"

    'name "LLaMA Download"
bin "llama download"
version "0.4.0-dev"
about "llama.cpp 模型下载工具"
flag "-h --help --usage" help=打印用法并退出
flag "-m --model" help=要加载的模型路径 {
    arg <FNAME>
}
flag --model-url help="模型下载 URL（默认：未使用）" {
    arg <MODEL_URL>
}
flag --docker-repo help="Docker Hub 模型仓库（repo 可选，默认 ai/）" {
    arg <REPO>
}
flag --hf-repo help="Hugging Face 模型仓库；quant 可选，默认 Q4_K_M" {
    arg "<user/model:quant>"
}
flag --hf-file help="Hugging Face 模型文件（覆盖 --hf-repo 中的 quant）" {
    arg <FILE>
}
flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量）" {
    arg <TOKEN>
}
flag --mtp help="同时下载 MTP 头（如果可用）"
flag --dflash help="同时下载 DFlash sidecar（如果可用）"
flag --eagle3 help="同时下载 Eagle3 sidecar（如果可用）"
flag --no-mmproj help="禁用自动下载 mmproj"
flag --offline help=离线模式：强制使用缓存，禁止网络访问
' | Out-File -FilePath $specFile -Encoding utf8

    $words = $commandAst.CommandElements | ForEach-Object { $_.ToString() }
    if ($words.Count -lt 1) {
        $words = @('llama-download')
    }

    $completions = & 'usage' complete-word --shell powershell -f "$specFile" -- @words $wordToComplete 2>$null

    if ($completions) {
        $completions | ForEach-Object {
            $parts = $_ -split "`t", 2
            $completion = $parts[0]
            $description = if ($parts.Count -gt 1) { $parts[1] } else { '' }

            [System.Management.Automation.CompletionResult]::new(
                $completion,
                $completion,
                'ParameterValue',
                $description
            )
        }
    }
}
