# @generated by usage-cli from mlx_lm.cache_prompt/zh-CN.usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.cache_prompt." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_mlx_lm_cache_prompt 'name "MLX 提示词缓存"
bin mlx_lm.cache_prompt
version "0.31.3"
about "缓存提示词的状态，供 mlx_lm.generate 复用"
flag "-h --help" help=显示此帮助信息并退出
flag --model help="本地模型目录或 Hugging Face 仓库的路径。" {
    arg <MODEL>
}
flag --adapter-path help=已训练适配器权重和配置的可选路径。 {
    arg <ADAPTER_PATH>
}
flag --trust-remote-code help=允许信任分词器的远程代码
flag --eos-token help="分词器的序列结束 token" {
    arg <EOS_TOKEN>
}
flag --max-kv-size help=设置最大键值缓存大小 {
    arg <MAX_KV_SIZE>
}
flag --prompt-cache-file help=保存提示词缓存的文件 {
    arg <PROMPT_CACHE_FILE>
}
flag --prompt help="要交给模型处理的消息（\'-\' 表示从 stdin 读取）" {
    arg <PROMPT>
}
flag --kv-bits help="KV 缓存量化位数。默认不量化。" {
    arg <KV_BITS>
}
flag --kv-group-size help="KV 缓存分组的组大小。" {
    arg <KV_GROUP_SIZE>
}
flag --quantized-kv-start help="设置 --kv-bits 后，从此步开始量化 KV 缓存。" {
    arg <QUANTIZED_KV_START>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_mlx_lm_cache_prompt.spec"
echo $_usage_spec_mlx_lm_cache_prompt > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc mlx_lm.cache_prompt -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc mlx_lm.cache_prompt -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
