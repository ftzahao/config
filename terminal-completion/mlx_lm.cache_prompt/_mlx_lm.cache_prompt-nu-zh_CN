# @generated by usage-cli from mlx_lm.cache_prompt/zh-CN.usage.kdl
module _mlx_lm_cache_prompt_completions {
    const _usage_spec_mlx_lm_cache_prompt = r##'name "MLX 提示词缓存"
bin mlx_lm.cache_prompt
version "0.31.3"
about "缓存提示词的状态，供 mlx_lm.generate 复用"
flag "-h --help" help=显示此帮助信息并退出
flag --model help="本地模型目录或 Hugging Face 仓库的路径。" {
    arg <MODEL>
}
flag --adapter-path help=已训练适配器权重和配置的可选路径。 {
    arg <ADAPTER_PATH>
}
flag --trust-remote-code help=允许信任分词器的远程代码
flag --eos-token help="分词器的序列结束 token" {
    arg <EOS_TOKEN>
}
flag --max-kv-size help=设置最大键值缓存大小 {
    arg <MAX_KV_SIZE>
}
flag --prompt-cache-file help=保存提示词缓存的文件 {
    arg <PROMPT_CACHE_FILE>
}
flag --prompt help="要交给模型处理的消息（'-' 表示从 stdin 读取）" {
    arg <PROMPT>
}
flag --kv-bits help="KV 缓存量化位数。默认不量化。" {
    arg <KV_BITS>
}
flag --kv-group-size help="KV 缓存分组的组大小。" {
    arg <KV_GROUP_SIZE>
}
flag --quantized-kv-start help="设置 --kv-bits 后，从此步开始量化 KV 缓存。" {
    arg <QUANTIZED_KV_START>
}
'##
    def mlx_lm_cache_prompt_completer [spans: list<string>] {
        let spec_dir = ($env.XDG_CACHE_HOME? | default ($nu.home-dir? | default $nu.home-path? | path join ".cache") | path join "usage")
        if not ($spec_dir | path exists) {
            mkdir $spec_dir
            if $nu.os-info.family != "windows" { ^chmod 0700 $spec_dir }
        }
        let spec_file = ($spec_dir | path join $"usage__usage_spec_mlx_lm_cache_prompt.spec")
        $_usage_spec_mlx_lm_cache_prompt | save -f $spec_file

        (^usage complete-word -f $spec_file --shell nu -- ...$spans)
        | lines
        | each {|it| $it | split column "\t" | rename value description | into record }
    }

    @complete mlx_lm_cache_prompt_completer
    export extern mlx_lm.cache_prompt []
}


# if "usage" is not installed show an error
if (which usage | is-empty) {
    return (error make {
        msg: "Error: usage CLI not found. This is required for completions to work in mlx_lm.cache_prompt.",
        help: "See https://usage.jdx.dev for more information."
    })
}
use _mlx_lm_cache_prompt_completions *
