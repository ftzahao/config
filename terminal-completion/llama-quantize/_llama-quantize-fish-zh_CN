# @generated by usage-cli from llama-quantize/zh-CN.usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in llama-quantize." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_llama_quantize 'name "LLaMA Quantize (中文)"
bin llama-quantize
version "0.4.0-dev"
about "GGUF 模型量化工具"
flag --allow-requantize help=""
flag --leave-output-tensor help=""
flag --pure help=""
flag --imatrix help="" {
    arg <FILE_NAME>
}
flag --include-weights help="" {
    arg <TENSOR_NAME>
}
flag --exclude-weights help="" {
    arg <TENSOR_NAME>
}
flag --output-tensor-type help="" {
    arg <GGML_TYPE>
}
flag --token-embedding-type help="" {
    arg <GGML_TYPE>
}
flag --tensor-type help="" {
    arg <TENSOR_NAME_GGML_TYPE>
}
flag --tensor-type-file help="" {
    arg <TENSOR_TYPES_TXT>
}
flag --prune-layers help="" {
    arg <L0_L1_L2>
}
flag --keep-split help=""
flag --override-kv help="" {
    arg <KEY_TYPE_VALUE>
}
flag --dry-run help=""
flag --max-buffer-size help="" {
    arg <MIB>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_llama_quantize.spec"
echo $_usage_spec_llama_quantize > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc llama-quantize -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc llama-quantize -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
