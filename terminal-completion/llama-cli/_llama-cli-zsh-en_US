#compdef llama-cli
# @generated by usage-cli from llama-cli/usage.kdl
local curcontext="$curcontext"

_llama_cli() {
  emulate -L zsh
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in llama-cli." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_dir="${XDG_CACHE_HOME:-$HOME/.cache}/usage"
  [[ -d "$spec_dir" ]] || mkdir -p -m 700 "$spec_dir"
  local spec_file="$spec_dir/usage__usage_spec_llama_cli.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name "LLaMA CLI (English)"
bin llama-cli
version "0.4.0-dev"
about "LLaMA 推理命令行工具"
flag "-h --help --usage" help="print usage and exit"
flag --version help="show version and build info"
flag --cache-list help="show list of models in cache"
flag --completion-bash help="print source-able bash completion script for llama.cpp"
flag "-t --threads" help="number of CPU threads to use during generation (default: -1)" {
    arg <N>
}
flag "-C --cpu-mask" help="CPU affinity mask: arbitrarily long hex. Complements cpu-range (default: \"\")" {
    arg <M>
}
flag --cpu-range help="range of CPUs for affinity. Complements --cpu-mask" {
    arg <LO_HI>
}
flag --cpu-strict help="use strict CPU placement (default: 0)" {
    arg <N>
}
flag --prio help="set process/thread priority : low(-1), normal(0), medium(1), high(2), realtime(3) (default: 0)" {
    arg <N>
}
flag --poll help="use polling level to wait for work (0 - no polling, default: 50)" {
    arg <0_100>
}
flag --cpu-mask-batch help="CPU affinity mask: arbitrarily long hex. Complements cpu-range-batch (default: same as --cpu-mask)" {
    arg <M>
}
flag --cpu-strict-batch help="use strict CPU placement (default: same as --cpu-strict)" {
    arg <N>
}
flag --prio-batch help="set process/thread priority : 0-normal, 1-medium, 2-high, 3-realtime (default: 0)" {
    arg <N>
}
flag --poll-batch help="use polling to wait for work (default: same as --poll)" {
    arg <N>
}
flag "-c --ctx-size" help="size of the prompt context (default: 0, 0 = loaded from model)" {
    arg <N>
}
flag "-n --predict --n-predict" help="number of tokens to predict (default: -1, -1 = infinity)" {
    arg <N>
}
flag "-b --batch-size" help="logical maximum batch size (default: 2048)" {
    arg <N>
}
flag --ubatch-size help="physical maximum batch size (default: 512)" {
    arg <N>
}
flag --keep help="number of tokens to keep from the initial prompt (default: 0, -1 = all)" {
    arg <N>
}
flag "--perf --no-perf" help="whether to enable internal libllama performance timings (default: false)"
flag "-e --escape --no-escape" help="whether to process escapes sequences (\\n, \\r, \\t, \\', \\\", \\\\) (default: true)"
flag --parallel help="number of parallel sequences to decode (default: 1)" {
    arg <N>
}
flag --override-tensor help="override tensor buffer type" {
    arg <TENSOR>
}
flag --cpu-moe help="keep all Mixture of Experts (MoE) weights in the CPU"
flag --n-cpu-moe help="keep the Mixture of Experts (MoE) weights of the first N layers in the" {
    arg <N>
}
flag --n-cpu-ffn help="keep the dense FFN weights of the first N layers in the CPU (dense models; for MoE expert weights use --n-cpu-moe)" {
    arg <N>
}
flag --check-tensors help="check model tensor data for invalid values (default: false)"
flag --override-kv help="advanced option to override model metadata by key. to specify multiple overrides, either use comma-separated values. types: int, float, bool, str. example: --override-kv tokenizer.ggml.add_bos_token=bool:false,tokenizer.ggml.add_eos_token=bool:false" {
    arg <KEY_TYPE_VALUE>
}
flag "--op-offload --no-op-offload" help="whether to offload host tensor operations to device (default: true)"
flag "-m --model" help="model path to load" {
    arg <FNAME>
}
flag --model-url help="model download url (default: unused)" {
    arg <MODEL_URL>
}
flag --hf-file help="Hugging Face model file. If specified, it will override the quant in --hf-repo (default: unused)" {
    arg <FILE>
}
flag --hf-token help="Hugging Face access token (default: value from HF_TOKEN environment variable)" {
    arg <TOKEN>
}
flag "--spec-draft-override-tensor --override-tensor-draft" help="override tensor buffer type for draft model" {
    arg <TENSOR>
}
flag --gpt-oss-20b-default help="use gpt-oss-20b (note: can download weights from the internet)"
flag --gpt-oss-120b-default help="use gpt-oss-120b (note: can download weights from the internet)"
flag --vision-gemma-4b-default help="use Gemma 3 4B QAT (note: can download weights from the internet)"
flag --vision-gemma-12b-default help="use Gemma 3 12B QAT (note: can download weights from the internet)"
flag --spec-default help="enable default speculative decoding config"
flag --rope-scaling help="RoPE frequency scaling method, defaults to linear unless specified by the model" {
    arg <NONE_LINEAR_YARN>
}
flag --rope-scale help="RoPE context scaling factor, expands context by a factor of N" {
    arg <N>
}
flag --rope-freq-base help="RoPE base frequency, used by NTK-aware scaling (default: loaded from model)" {
    arg <N>
}
flag --rope-freq-scale help="RoPE frequency scaling factor, expands context by a factor of 1/N" {
    arg <N>
}
flag --yarn-orig-ctx help="YaRN: original context size of model (default: 0 = model training context size)" {
    arg <N>
}
flag --yarn-ext-factor help="YaRN: extrapolation mix factor (default: -1.00, 0.0 = full interpolation)" {
    arg <N>
}
flag --yarn-attn-factor help="YaRN: scale sqrt(t) or attention magnitude (default: -1.00)" {
    arg <N>
}
flag --yarn-beta-slow help="YaRN: high correction dim or alpha (default: -1.00)" {
    arg <N>
}
flag --yarn-beta-fast help="YaRN: low correction dim or beta (default: -1.00)" {
    arg <N>
}
flag "--repack --no-repack" help="whether to enable weight repacking (default: enabled)"
flag --no-host help="bypass host buffer allowing extra buffers to be used"
flag --cache-type-k help="KV cache data type for K allowed values: f32, f16, bf16, q8_0, q4_0, q4_1, iq4_nl, q5_0, q5_1 (default: f16)" {
    arg <TYPE>
}
flag --cache-type-v help="KV cache data type for V allowed values: f32, f16, bf16, q8_0, q4_0, q4_1, iq4_nl, q5_0, q5_1 (default: f16)" {
    arg <TYPE>
}
flag --defrag-thold help="KV cache defragmentation threshold (DEPRECATED)" {
    arg <N>
}
flag --cache-ram help="set the maximum cache size in MiB (default: 8192, -1 - no limit, 0 - disable)" {
    arg <N>
}
flag --swa-full help="use full-size SWA cache (default: false) info)](https://github.com/ggml-org/llama.cpp/pull/13194#issuecomment-2868343055)"
flag --rpc help="comma-separated list of RPC servers (host:port)" {
    arg <SERVERS>
}
flag --mlock help="DEPRECATED in favor of `--load-mode`: force system to keep model in RAM rather than swapping or compressing"
flag "--mmap --no-mmap" help="DEPRECATED in favor of `--load-mode`: whether to memory-map model. (if mmap disabled, slower load but may reduce pageouts if not using mlock)"
flag --load-mode help="model loading mode (default: auto) - auto: mmap, unless a device does not support it - none: no special loading mode - mmap: memory-map model (if mmap disabled, slower load but may reduce pageouts if not using mlock) - mlock: force system to keep model in RAM rather than swapping or compressing - mmap+mlock: mmap + force system to keep model in RAM rather than swapping or compressing - dio: use DirectIO if available" {
    arg <MODE>
}
flag --lazy-mode help="on-demand reading of certain tensors, for example per-layer embeddings (default: auto) - on: read the rows of such tensors from disk on demand instead of keeping them resident (requires mmap) - auto: on, but only for tensors larger than 4 GiB - off: always keep them resident" {
    arg <MODE>
}
flag --device help="comma-separated list of devices to use for offloading (none = don't offload) use --list-devices to see a list of available devices" {
    arg <DEV1_DEV2>
}
flag "--gpu-layers --n-gpu-layers" help="max. number of layers to store in VRAM, either an exact number, 'auto', or 'all' (default: auto)" {
    arg <N>
}
flag --tensor-split help="fraction of the model to offload to each GPU, comma-separated list of proportions, e.g. 3,1" {
    arg <N0_N1_N2>
}
flag --main-gpu help="the GPU to use for the model (with split-mode = none), or for intermediate results and KV (with split-mode = row) (default: 0)" {
    arg <INDEX>
}
flag --fit help="whether to adjust unset arguments to fit in device memory ('on' or 'off', default: 'on')" {
    arg <ON_OFF>
}
flag --fit-ctx help="minimum ctx size that can be set by --fit option, default: 4096" {
    arg <N>
}
flag --lora help="path to LoRA adapter (use comma-separated values to load multiple adapters)" {
    arg <FNAME>
}
flag --lora-scaled help="path to LoRA adapter with user defined scaling (format: FNAME:SCALE,...) note: use comma-separated values" {
    arg <FNAME_SCALE>
}
flag --control-vector help="add a control vector note: use comma-separated values to add multiple control vectors" {
    arg <FNAME>
}
flag --control-vector-layer-range help="layer range to apply the control vector(s) to, start and end inclusive" {
    arg <START>
}
flag "--spec-draft-poll --poll-draft" help="Use polling to wait for draft model work (default: same as --poll)" {
    arg <N>
}
flag --spec-draft-n-max help="number of tokens to draft for speculative decoding (default: 3)" {
    arg <N>
}
flag --spec-draft-n-min help="minimum number of draft tokens to use for speculative decoding (default: 0)" {
    arg <N>
}
flag --spec-synth-len help="target mean synthetic acceptance length, including the target token (benchmarking only)" {
    arg <L>
}
flag --spec-synth-rates help="comma-separated unconditional per-position synthetic acceptance probabilities (benchmarking only)" {
    arg <P0_P1>
}
flag "--spec-draft-p-split --draft-p-split" help="speculative decoding split probability (default: 0.10)" {
    arg <P>
}
flag "--spec-draft-p-min --draft-p-min" help="minimum speculative decoding probability (greedy) (default: 0.00)" {
    arg <P>
}
flag "--spec-draft-backend-sampling --no-spec-draft-backend-sampling" help="offload draft sampling to the backend (default: enabled)"
flag --spec-type help="comma-separated list of types of speculative decoding to use (default: none)" {
    arg <NONE_DRAFT_SIMPLE_DRAFT_EAGLE3_DRAFT_MTP_DRAFT_DFLASH_DRAFT_DSPARK_NGRAM_SIMPLE_NGRAM_MAP_K_NGRAM_MAP_K4V_NGRAM_MOD_NGRAM_CACHE>
}
flag --spec-ngram-mod-n-min help="minimum number of ngram tokens to use for ngram-based speculative decoding (default: 48)" {
    arg <N>
}
flag --spec-ngram-mod-n-max help="maximum number of ngram tokens to use for ngram-based speculative decoding (default: 64)" {
    arg <N>
}
flag --spec-ngram-mod-n-match help="ngram-mod lookup length (default: 24)" {
    arg <N>
}
flag --spec-ngram-simple-size-n help="ngram size N for ngram-simple speculative decoding, length of lookup n-gram (default: 12)" {
    arg <N>
}
flag --spec-ngram-simple-size-m help="ngram size M for ngram-simple speculative decoding, length of draft m-gram (default: 48)" {
    arg <N>
}
flag --spec-ngram-simple-min-hits help="minimum hits for ngram-simple speculative decoding (default: 1)" {
    arg <N>
}
flag --spec-ngram-map-k-size-n help="ngram size N for ngram-map-k speculative decoding, length of lookup n-gram (default: 12)" {
    arg <N>
}
flag --spec-ngram-map-k-size-m help="ngram size M for ngram-map-k speculative decoding, length of draft m-gram (default: 48)" {
    arg <N>
}
flag --spec-ngram-map-k-min-hits help="minimum hits for ngram-map-k speculative decoding (default: 1)" {
    arg <N>
}
flag --spec-ngram-map-k4v-size-n help="ngram size N for ngram-map-k4v speculative decoding, length of lookup n-gram (default: 12)" {
    arg <N>
}
flag --spec-ngram-map-k4v-size-m help="ngram size M for ngram-map-k4v speculative decoding, length of draft m-gram (default: 48)" {
    arg <N>
}
flag --spec-ngram-map-k4v-min-hits help="minimum hits for ngram-map-k4v speculative decoding (default: 1)" {
    arg <N>
}
flag "--draft --draft-n --draft-max" help="the argument has been removed. use --spec-draft-n-max or --spec-ngram-mod-n-max" {
    arg <N>
}
flag "--draft-min --draft-n-min" help="the argument has been removed. use --spec-draft-n-min or --spec-ngram-mod-n-min" {
    arg <N>
}
flag --samplers help="samplers that will be used for generation in the order, separated by" {
    arg <SAMPLERS>
}
flag "-s --seed" help="RNG seed (default: -1, use random seed for -1)" {
    arg <SEED>
}
flag --ignore-eos help="ignore end of stream token and continue generating (implies --logit-bias EOS-inf)"
flag "--temp --temperature" help="temperature (default: 0.80)" {
    arg <N>
}
flag --top-k help="top-k sampling (default: 40, 0 = disabled)" {
    arg <N>
}
flag --top-p help="top-p sampling (default: 0.95, 1.0 = disabled)" {
    arg <N>
}
flag --min-p help="min-p sampling (default: 0.05, 0.0 = disabled)" {
    arg <N>
}
flag "--top-nsigma --top-n-sigma" help="top-n-sigma sampling (default: -1.00, -1.0 = disabled)" {
    arg <N>
}
flag --xtc-probability help="xtc probability (default: 0.00, 0.0 = disabled)" {
    arg <N>
}
flag --xtc-threshold help="xtc threshold (default: 0.10, 1.0 = disabled)" {
    arg <N>
}
flag "--typical --typical-p" help="locally typical sampling, parameter p (default: 1.00, 1.0 = disabled)" {
    arg <N>
}
flag --repeat-last-n help="last n tokens to consider for penalize (default: 64, 0 = disabled)" {
    arg <N>
}
flag --repeat-penalty help="penalize repeat sequence of tokens (default: 1.00, 1.0 = disabled)" {
    arg <N>
}
flag --presence-penalty help="repeat alpha presence penalty (default: 0.00, 0.0 = disabled)" {
    arg <N>
}
flag --frequency-penalty help="repeat alpha frequency penalty (default: 0.00, 0.0 = disabled)" {
    arg <N>
}
flag --dry-multiplier help="set DRY sampling multiplier (default: 0.00, 0.0 = disabled)" {
    arg <N>
}
flag --dry-base help="set DRY sampling base value (default: 1.75)" {
    arg <N>
}
flag --dry-allowed-length help="set allowed length for DRY sampling (default: 2)" {
    arg <N>
}
flag --dry-penalty-last-n help="set DRY penalty for the last n tokens (default: 64, 0 = disable)" {
    arg <N>
}
flag --dry-sequence-breaker help="add sequence breaker for DRY sampling, clearing out default breakers ('\\n', ':', '\"', '*') in the process; use \"none\" to not use any sequence breakers" {
    arg <STRING>
}
flag --adaptive-target help="adaptive-p: select tokens near this probability (valid range 0.0 to 1.0; negative = disabled) (default: -1.00)" {
    arg <N>
}
flag --adaptive-decay help="adaptive-p: decay rate for target adaptation over time. lower values are more reactive, higher values are more stable. (valid range 0.0 to 0.99) (default: 0.90)" {
    arg <N>
}
flag --dynatemp-range help="dynamic temperature range (default: 0.00, 0.0 = disabled)" {
    arg <N>
}
flag --dynatemp-exp help="dynamic temperature exponent (default: 1.00)" {
    arg <N>
}
flag --mirostat help="use Mirostat sampling. Top K, Nucleus and Locally Typical samplers are ignored if used. (default: 0, 0 = disabled, 1 = Mirostat, 2 = Mirostat 2.0)" {
    arg <N>
}
flag --mirostat-lr help="Mirostat learning rate, parameter eta (default: 0.10)" {
    arg <N>
}
flag --mirostat-ent help="Mirostat target entropy, parameter tau (default: 5.00)" {
    arg <N>
}
flag "-l --logit-bias" help="modifies the likelihood of token appearing in the completion, i.e. `--logit-bias 15043+1` to increase likelihood of token ' Hello', or `--logit-bias 15043-1` to decrease likelihood of token ' Hello'" {
    arg <TOKEN_ID_BIAS>
}
flag --grammar help="BNF-like grammar to constrain generations (see samples in grammars/ dir)" {
    arg <GRAMMAR>
}
flag --grammar-file help="file to read grammar from" {
    arg <FNAME>
}
flag "-j --json-schema" help="JSON schema to constrain generations (https://json-schema.org/), e.g. `{}` for any JSON object For schemas w/ external $refs, use --grammar + example/json_schema_to_grammar.py instead" {
    arg <SCHEMA>
}
flag --json-schema-file help="File containing a JSON schema to constrain generations (https://json-schema.org/), e.g. `{}` for any JSON object For schemas w/ external $refs, use --grammar + example/json_schema_to_grammar.py instead" {
    arg <FILE>
}
flag --backend-sampling help="enable backend sampling (experimental) (default: disabled)"
flag --mmproj help="path to a multimodal projector file. see tools/mtmd/README.md note: if -hf is used, this argument can be omitted" {
    arg <FILE>
}
flag --mmproj-url help="URL to a multimodal projector file. see tools/mtmd/README.md" {
    arg <URL>
}
flag "--mmproj-offload --no-mmproj-offload" help="whether to enable GPU offloading for multimodal projector (default: enabled)"
flag --mmproj-device help="device to use for multimodal projector (none = don't offload, default: follows --device) use --list-devices to see a list of available devices" {
    arg <DEVICE>
}
flag "--image --audio --video" help="path to an image, audio, or video file. use with multimodal models, use comma-separated values for multiple files" {
    arg <FILE>
}
flag --image-min-tokens help="minimum number of tokens each image can take, only used by vision models with dynamic resolution (default: read from model)" {
    arg <N>
}
flag --image-max-tokens help="maximum number of tokens each image can take, only used by vision models with dynamic resolution (default: read from model)" {
    arg <N>
}
flag --video-fps help="target video frame rate (default: 4.0)" {
    arg <N>
}
flag --video-timestamp-interval help="interval in milliseconds between text timestamps (default: 5000)" {
    arg <N>
}
flag --video-ffmpeg-dir help="path to the directory containing ffmpeg and ffprobe (default: search in PATH)" {
    arg <DIR>
}
flag --reasoning help="Use reasoning/thinking in the chat ('on', 'off', or 'auto', default: 'auto' (detect from template))" {
    arg <ON_OFF_AUTO>
}
flag --reasoning-effort help="reasoning effort level given to the chat template: 'default' to keep the template default, or a level such as 'minimal', 'low', 'medium', 'high', 'xhigh' or 'max' (default: default)" {
    arg <LEVEL>
}
flag --reasoning-budget help="token budget for thinking: -1 for unrestricted, 0 for immediate end, N>0 for token budget (default: -1)" {
    arg <N>
}
flag --reasoning-budget-message help="message injected before the end-of-thinking tag when reasoning budget is exhausted (default: none)" {
    arg <MESSAGE>
}
flag "--reasoning-preserve --no-reasoning-preserve" help="preserve reasoning trace in the full history, not just the last assistant message (default: enabled) compatible with certain templates having 'supports_preserve_reasoning' capability example: https://docs.z.ai/guides/capabilities/thinking-mode#preserved-thinking"
flag "-p --prompt" help="prompt to start generation with; for system message, use -sys" {
    arg <PROMPT>
}
flag "-f --file" help="a file containing the prompt (default: none)" {
    arg <FNAME>
}
flag --binary-file help="binary file containing the prompt (default: none)" {
    arg <FNAME>
}
flag --server-base help="connect to this server instead of starting a new one, example: 'http://localhost:8080' (default: none)" {
    arg <URL>
}
flag --verbose-prompt help="print a verbose prompt before generation (default: false)"
flag "--display-prompt --no-display-prompt" help="whether to print prompt at generation (default: true)"
flag --color help="Colorize output to distinguish prompt and user input from generations ('on', 'off', or 'auto', default: 'auto') 'auto' enables colors when output is to a terminal" {
    arg <ON_OFF_AUTO>
}
flag --system-prompt help="system prompt to use with model (if applicable, depending on chat template)" {
    arg <PROMPT>
}
flag "--show-timings --no-show-timings" help="whether to show timing information after each response (default: true)"
flag "-r --reverse-prompt" help="halt generation at PROMPT, return control in interactive mode" {
    arg <PROMPT>
}
flag --special help="special tokens output enabled (default: false)"
flag --single-turn help="run conversation for a single turn only, then exit when done will not be interactive if first turn is predefined with --prompt (default: false)"
flag --multiline-input help="allows you to write or paste multiple lines without ending each in '\\'"
flag "--warmup --no-warmup" help="whether to perform warmup with an empty run (default: enabled)"
flag --chat-template-kwargs help="sets additional params for the json template parser, must be a valid json object string, e.g. '{\"key1\":\"value1\",\"key2\":\"value2\"}'" {
    arg <STRING>
}
flag "--jinja --no-jinja" help="whether to use jinja template engine for chat (default: enabled)"
flag --log-disable help="Log disable"
flag --log-file help="Log to file" {
    arg <FNAME>
}
flag "--log-jsonl --no-log-jsonl" help="Log as JSONL (one JSON object per line) to stdout, this also disables colored logging (default: disabled)"
flag --log-colors help="Set colored logging ('on', 'off', or 'auto', default: 'auto') 'auto' enables colors when output is to a terminal" {
    arg <ON_OFF_AUTO>
}
flag "-v --verbose --log-verbose" help="Set verbosity level to infinity (i.e. log all messages, useful for debugging)"
flag --offline help="Offline mode: forces use of cache, prevents network access"
flag "--verbosity --log-verbosity" help="Set the verbosity threshold. Messages with a higher verbosity will be ignored. Values: - 0: generic output - 1: error - 2: warning - 3: info - 4: trace (more info) - 5: debug (default: 1)" {
    arg <N>
}
flag "--log-prefix --no-log-prefix" help="Enable prefix in log messages"
flag "--log-timestamps --no-log-timestamps" help="Enable timestamps in log messages"
flag --log-prompts-dir help="Log prompts to directory (auto-created if not present; only used for debugging, default: disabled)" {
    arg <PATH>
}
flag --threads-batch help="number of threads to use during batch and prompt processing (default: same as --threads)" {
    arg <N>
}
flag --cpu-range-batch help="ranges of CPUs for affinity. Complements --cpu-mask-batch" {
    arg <LO_HI>
}
flag --flash-attn help="set Flash Attention use ('on', 'off', or 'auto', default: 'auto')" {
    arg <ON_OFF_AUTO>
}
flag "--kv-offload --no-kv-offload" help="whether to enable KV cache offloading (default: enabled)"
flag "--direct-io --no-direct-io" help="DEPRECATED in favor of `--load-mode`: use DirectIO if available"
flag --numa help="attempt optimizations that help on some NUMA systems - distribute: spread execution evenly over all nodes - isolate: only spawn threads on CPUs on the node that execution started on - numactl: use the CPU map provided by numactl if run without this previously, it is recommended to drop the system page cache before using this see https://github.com/ggml-org/llama.cpp/issues/1437" {
    arg <TYPE>
}
flag --list-devices help="print list of available devices and exit"
flag --split-mode help="how to split the model across multiple GPUs, one of: - none: use one GPU only - layer (default): split layers and KV across GPUs (pipelined) - row: split weight across GPUs by rows (parallelized) - tensor: split weights and KV across GPUs (parallelized, EXPERIMENTAL)" {
    arg <NONE_LAYER_ROW_TENSOR>
}
flag --fit-target help="target margin per device for --fit, comma-separated list of values, single value is broadcast across all devices, default: 1024" {
    arg <MIB0_MIB1_MIB2>
}
flag --control-vector-scaled help="add a control vector with user defined scaling SCALE note: use comma-separated values (format: FNAME:SCALE,...)" {
    arg <FNAME_SCALE>
}
flag --docker-repo help="Docker Hub model repository. repo is optional, default to ai/. quant is optional, default to :latest. example: gemma3 (default: unused)" {
    arg <REPO_MODEL_QUANT>
}
flag --hf-repo help="Hugging Face model repository; quant is optional, case-insensitive, default to Q4_K_M, or falls back to the first file in the repo if Q4_K_M doesn't exist. mmproj is also downloaded automatically if available. to disable, add --no-mmproj example: ggml-org/GLM-4.7-Flash-GGUF:Q4_K_M (default: unused)" {
    arg <USER_MODEL_QUANT>
}
flag "--spec-draft-type-k --cache-type-k-draft" help="KV cache data type for K for the draft model allowed values: f32, f16, bf16, q8_0, q4_0, q4_1, iq4_nl, q5_0, q5_1 (default: f16)" {
    arg <TYPE>
}
flag "--spec-draft-type-v --cache-type-v-draft" help="KV cache data type for V for the draft model allowed values: f32, f16, bf16, q8_0, q4_0, q4_1, iq4_nl, q5_0, q5_1 (default: f16)" {
    arg <TYPE>
}
flag "--sampler-seq --sampling-seq" help="simplified sequence for samplers that will be used (default: edskypmxt)" {
    arg <SEQUENCE>
}
flag "--spec-draft-hf --hf-repo-draft" help="Same as --hf-repo, but for the draft model (default: unused)" {
    arg <USER_MODEL_QUANT>
}
flag "--spec-draft-threads --threads-draft" help="number of threads to use during generation (default: same as --threads)" {
    arg <N>
}
flag "--spec-draft-threads-batch --threads-batch-draft" help="number of threads to use during batch and prompt processing (default: same as --threads-draft)" {
    arg <N>
}
flag "--spec-draft-cpu-mask --cpu-mask-draft" help="Draft model CPU affinity mask. Complements cpu-range-draft (default: same as --cpu-mask)" {
    arg <M>
}
flag "--spec-draft-cpu-range --cpu-range-draft" help="Ranges of CPUs for affinity. Complements --cpu-mask-draft" {
    arg <LO_HI>
}
flag "--spec-draft-cpu-strict --cpu-strict-draft" help="Use strict CPU placement for draft model (default: same as --cpu-strict)" {
    arg <N>
}
flag "--spec-draft-prio --prio-draft" help="set draft process/thread priority : 0-normal, 1-medium, 2-high, 3-realtime (default: 0)" {
    arg <N>
}
flag "--spec-draft-cpu-mask-batch --cpu-mask-batch-draft" help="Draft model CPU affinity mask. Complements cpu-range-draft (default: same as --cpu-mask)" {
    arg <M>
}
flag "--spec-draft-cpu-strict-batch --cpu-strict-batch-draft" help="Use strict CPU placement for draft model (default: --cpu-strict-draft)" {
    arg <N>
}
flag "--spec-draft-prio-batch --prio-batch-draft" help="set draft process/thread priority : 0-normal, 1-medium, 2-high, 3-realtime (default: 0)" {
    arg <N>
}
flag "--spec-draft-poll-batch --poll-batch-draft" help="Use polling to wait for draft model work (default: --poll-draft)" {
    arg <N>
}
flag "--spec-draft-cpu-moe --cpu-moe-draft" help="keep all Mixture of Experts (MoE) weights in the CPU for the draft model"
flag "--spec-draft-n-cpu-moe --spec-draft-ncmoe --n-cpu-moe-draft" help="keep the Mixture of Experts (MoE) weights of the first N layers in the CPU for the draft model" {
    arg <N>
}
flag "--spec-draft-device --device-draft" help="comma-separated list of devices to use for offloading the draft model (none = don't offload, default: follows --device) use --list-devices to see a list of available devices" {
    arg <DEV1_DEV2>
}
flag "--spec-draft-ngl --gpu-layers-draft --n-gpu-layers-draft" help="max. number of draft model layers to store in VRAM, either an exact number, 'auto', or 'all' (default: auto)" {
    arg <N>
}
flag "--spec-draft-model --model-draft" help="draft model for speculative decoding (default: unused)" {
    arg <FNAME>
}
flag "--ctx-checkpoints --swa-checkpoints" help="max number of context checkpoints to create per slot (default: 32)" {
    arg <N>
}
flag "--context-shift --no-context-shift" help="whether to use context shift on infinite text generation (default: disabled)"
flag --system-prompt-file help="a file containing the system prompt (default: none)" {
    arg <FNAME>
}
flag "--mmproj-auto --no-mmproj --no-mmproj-auto" help="whether to use multimodal projector file (if available), useful when using -hf (default: enabled)"
flag "-o --output --output-file" help="output file (default: '')" {
    arg <FNAME>
}
flag --reasoning-format help="controls whether thought tags are allowed and/or extracted from the response, and in which format they're returned; one of: - none: leaves thoughts unparsed in `message.content` - deepseek: puts thoughts in `message.reasoning_content` - deepseek-legacy: keeps `<think>` tags in `message.content` while also populating `message.reasoning_content` (default: auto)" {
    arg <FORMAT>
}
flag --chat-template help="set custom jinja chat template (default: template taken from model's metadata) if suffix/prefix are specified, template will be disabled only commonly used templates are accepted (unless --jinja is set before this flag): list of built-in templates: bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
    arg <JINJA_TEMPLATE>
}
flag --chat-template-file help="set custom jinja chat template file (default: template taken from model's metadata) if suffix/prefix are specified, template will be disabled only commonly used templates are accepted (unless --jinja is set before this flag): list of built-in templates: bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
    arg <JINJA_TEMPLATE_FILE>
}
flag "--skip-chat-parsing --no-skip-chat-parsing" help="force a pure content parser, even if a Jinja template is specified; model will output everything in the content section, including any reasoning and/or tool calls (default: disabled)"
flag --simple-io help="use basic IO for better compatibility in subprocesses and limited consoles"
__USAGE_EOF__
  local -a values=() descs=() inserts=()
  local needs_menu=0 line
  while IFS= read -r line; do
    local -a parts=("${(@ps:\t:)line}")
    values+=("${parts[1]}")
    descs+=("${parts[2]}")
    inserts+=("${parts[3]}")
    [[ "${parts[3]}" == "'"* ]] && needs_menu=1
  done < <(command usage complete-word --shell zsh -f "$spec_file" --cword=$((CURRENT - 1)) -- "${(Q)words[@]}")
  (( needs_menu )) && compstate[insert]=menu
  if (( ${#inserts[@]} )); then
    local -a _usage_display=()
    local _usage_i _usage_max=0 _usage_v _usage_pad
    for _usage_v in "${values[@]}"; do
      (( ${#_usage_v} > _usage_max )) && _usage_max=${#_usage_v}
    done
    for ((_usage_i=1; _usage_i<=${#values[@]}; _usage_i++)); do
      if [[ -n "${descs[_usage_i]}" ]]; then
        _usage_pad=$(( _usage_max - ${#values[_usage_i]} ))
        _usage_display+=("${values[_usage_i]}${(l:_usage_pad:: :)}  -- ${descs[_usage_i]}")
      else
        _usage_display+=("${values[_usage_i]}")
      fi
    done
    compadd -l -d _usage_display -U -Q -S '' -a inserts
  fi
  return 0
}

if [ "$funcstack[1]" = "_llama_cli" ]; then
    _llama_cli "$@"
else
    compdef _llama_cli llama-cli
fi

# vim: noet ci pi sts=0 sw=4 ts=4
