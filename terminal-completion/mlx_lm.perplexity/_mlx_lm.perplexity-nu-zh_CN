# @generated by usage-cli from mlx_lm.perplexity/zh-CN.usage.kdl
module _mlx_lm_perplexity_completions {
    const _usage_spec_mlx_lm_perplexity = r##'name "MLX 困惑度"
bin mlx_lm.perplexity
version "0.31.3"
about "评估 MLX 模型的困惑度"
flag "-h --help" help=显示此帮助信息并退出
flag --model help="模型路径或 Hugging Face 模型 ID" {
    arg <MODEL>
}
flag --trust-remote-code help="从 Hugging Face 加载分词器/模型时允许信任远程代码。"
flag --batch-size help=评估的批大小 {
    arg <BATCH_SIZE>
}
flag --sequence-length help=评估的序列长度 {
    arg <SEQUENCE_LENGTH>
}
flag --num-samples help="使用的样本数量（-1 表示全部可用样本）" {
    arg <NUM_SAMPLES>
}
flag --data-path help="兼容 mlx-lm 数据集格式的 Hugging Face 数据集。" {
    arg <DATA_PATH>
}
flag --seed help=数据采样的随机种子 {
    arg <SEED>
}
'##
    def mlx_lm_perplexity_completer [spans: list<string>] {
        let spec_dir = ($env.XDG_CACHE_HOME? | default ($nu.home-dir? | default $nu.home-path? | path join ".cache") | path join "usage")
        if not ($spec_dir | path exists) {
            mkdir $spec_dir
            if $nu.os-info.family != "windows" { ^chmod 0700 $spec_dir }
        }
        let spec_file = ($spec_dir | path join $"usage__usage_spec_mlx_lm_perplexity.spec")
        $_usage_spec_mlx_lm_perplexity | save -f $spec_file

        (^usage complete-word -f $spec_file --shell nu -- ...$spans)
        | lines
        | each {|it| $it | split column "\t" | rename value description | into record }
    }

    @complete mlx_lm_perplexity_completer
    export extern mlx_lm.perplexity []
}


# if "usage" is not installed show an error
if (which usage | is-empty) {
    return (error make {
        msg: "Error: usage CLI not found. This is required for completions to work in mlx_lm.perplexity.",
        help: "See https://usage.jdx.dev for more information."
    })
}
use _mlx_lm_perplexity_completions *
