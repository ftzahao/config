# @generated by usage-cli from mlx_lm.perplexity/zh-CN.usage.kdl
# Requires bash-completion (https://github.com/scop/bash-completion), which must be
# sourced before this script.
_mlx_lm_perplexity() {
    if ! type -P usage &> /dev/null; then
        echo >&2
        echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.perplexity." >&2
        echo "See https://usage.jdx.dev for more information." >&2
        return 1
    fi
    if ! declare -F _init_completion > /dev/null 2>&1; then
        echo >&2
        echo "Error: bash-completion is required for mlx_lm.perplexity completions but was not loaded." >&2
        echo "Install it from your package manager and source it before this script." >&2
        return 1
    fi

	local cur prev words cword comp_args
    _init_completion -n : -- "$@" || return
    local spec_dir="${XDG_CACHE_HOME:-$HOME/.cache}/usage"
    [[ -d "$spec_dir" ]] || mkdir -p -m 700 "$spec_dir"
    local spec_file="$spec_dir/usage__usage_spec_mlx_lm_perplexity.spec"
    cat >| "$spec_file" <<'__USAGE_EOF__'
name "MLX 困惑度"
bin mlx_lm.perplexity
version "0.31.3"
about "评估 MLX 模型的困惑度"
flag "-h --help" help=显示此帮助信息并退出
flag --model help="模型路径或 Hugging Face 模型 ID" {
    arg <MODEL>
}
flag --trust-remote-code help="从 Hugging Face 加载分词器/模型时允许信任远程代码。"
flag --batch-size help=评估的批大小 {
    arg <BATCH_SIZE>
}
flag --sequence-length help=评估的序列长度 {
    arg <SEQUENCE_LENGTH>
}
flag --num-samples help="使用的样本数量（-1 表示全部可用样本）" {
    arg <NUM_SAMPLES>
}
flag --data-path help="兼容 mlx-lm 数据集格式的 Hugging Face 数据集。" {
    arg <DATA_PATH>
}
flag --seed help=数据采样的随机种子 {
    arg <SEED>
}
__USAGE_EOF__
    # shellcheck disable=SC2207
	COMPREPLY=($(compgen -W "$(command usage complete-word --shell bash -f "$spec_file" --cword="$cword" -- "${words[@]}")" -- "$cur"))
	__ltrim_colon_completions "$cur"
    # shellcheck disable=SC2181
    if [[ $? -ne 0 ]]; then
        unset COMPREPLY
    fi
    return 0
}

if [[ "${BASH_VERSINFO[0]}" -eq 4 && "${BASH_VERSINFO[1]}" -ge 4 || "${BASH_VERSINFO[0]}" -gt 4 ]]; then
    shopt -u hostcomplete && complete -o nospace -o bashdefault -o nosort -F _mlx_lm_perplexity mlx_lm.perplexity
else
    shopt -u hostcomplete && complete -o nospace -o bashdefault -F _mlx_lm_perplexity mlx_lm.perplexity
fi
# vim: noet ci pi sts=0 sw=4 ts=4 ft=sh
