# @generated by usage-cli from mlx_lm.benchmark/zh-CN.usage.kdl
module _mlx_lm_benchmark_completions {
    const _usage_spec_mlx_lm_benchmark = r##'name "MLX 基准测试"
bin mlx_lm.benchmark
version "0.31.3"
about "LLM 基准测试脚本"
flag "-h --help" help=显示此帮助信息并退出
flag --model help="本地模型目录或 Hugging Face 仓库的路径。若未指定模型，则使用 mlx-community/Llama-3.2-3B-Instruct-4bit。" {
    arg <MODEL>
}
flag "-p --prompt-tokens" help=提示词长度 {
    arg <PROMPT_TOKENS>
}
flag "-g --generation-tokens" help=生成内容长度 {
    arg <GENERATION_TOKENS>
}
flag "-b --batch-size" help=批大小 {
    arg <BATCH_SIZE>
}
flag "-n --num-trials" help=计时测试次数 {
    arg <NUM_TRIALS>
}
flag --pipeline help=使用流水线而非张量并行
flag --quantize-activations help=使用对应层的量化配置对激活值进行量化。
flag --prefill-step-size help=预填充处理的步长（默认：2048） {
    arg <PREFILL_STEP_SIZE>
}
flag --delay help=每次测试之间的延迟（秒）（默认：0） default="0" {
    arg <DELAY>
}
'##
    def mlx_lm_benchmark_completer [spans: list<string>] {
        let spec_dir = ($env.XDG_CACHE_HOME? | default ($nu.home-dir? | default $nu.home-path? | path join ".cache") | path join "usage")
        if not ($spec_dir | path exists) {
            mkdir $spec_dir
            if $nu.os-info.family != "windows" { ^chmod 0700 $spec_dir }
        }
        let spec_file = ($spec_dir | path join $"usage__usage_spec_mlx_lm_benchmark.spec")
        $_usage_spec_mlx_lm_benchmark | save -f $spec_file

        (^usage complete-word -f $spec_file --shell nu -- ...$spans)
        | lines
        | each {|it| $it | split column "\t" | rename value description | into record }
    }

    @complete mlx_lm_benchmark_completer
    export extern mlx_lm.benchmark []
}


# if "usage" is not installed show an error
if (which usage | is-empty) {
    return (error make {
        msg: "Error: usage CLI not found. This is required for completions to work in mlx_lm.benchmark.",
        help: "See https://usage.jdx.dev for more information."
    })
}
use _mlx_lm_benchmark_completions *
