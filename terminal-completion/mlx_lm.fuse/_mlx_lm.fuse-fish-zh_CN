# @generated by usage-cli from mlx_lm.fuse/zh-CN.usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.fuse." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_mlx_lm_fuse 'name "MLX 权重合并"
bin mlx_lm.fuse
version "0.31.3"
about 将微调的适配器合并进基础模型。
flag "-h --help" help=显示此帮助信息并退出
flag --model help="本地模型目录或 Hugging Face 仓库的路径。" {
    arg <MODEL>
}
flag --save-path help=保存合并后模型的路径。 {
    arg <SAVE_PATH>
}
flag --adapter-path help=已训练适配器权重和配置的路径。 {
    arg <ADAPTER_PATH>
}
flag --upload-repo help="要上传模型到的 Hugging Face 仓库。" {
    arg <UPLOAD_REPO>
}
flag --dequantize help=生成反量化模型。
flag --export-gguf help="以 GGUF 格式导出模型权重。"
flag --gguf-path help="保存导出的 GGUF 格式模型权重的路径。默认为 ggml-model-f16.gguf。" {
    arg <GGUF_PATH>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_mlx_lm_fuse.spec"
echo $_usage_spec_mlx_lm_fuse > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc mlx_lm.fuse -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc mlx_lm.fuse -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
