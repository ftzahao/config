# @generated by usage-cli from llama/zh-CN.usage.kdl
# Requires bash-completion (https://github.com/scop/bash-completion), which must be
# sourced before this script.
_llama() {
    if ! type -P usage &> /dev/null; then
        echo >&2
        echo "Error: usage CLI not found. This is required for completions to work in llama." >&2
        echo "See https://usage.jdx.dev for more information." >&2
        return 1
    fi
    if ! declare -F _init_completion > /dev/null 2>&1; then
        echo >&2
        echo "Error: bash-completion is required for llama completions but was not loaded." >&2
        echo "Install it from your package manager and source it before this script." >&2
        return 1
    fi

	local cur prev words cword comp_args
    _init_completion -n : -- "$@" || return
    local spec_dir="${XDG_CACHE_HOME:-$HOME/.cache}/usage"
    [[ -d "$spec_dir" ]] || mkdir -p -m 700 "$spec_dir"
    local spec_file="$spec_dir/usage__usage_spec_llama.spec"
    cat >| "$spec_file" <<'__USAGE_EOF__'
name "llama (中文)"
bin llama
version "0.4.0-dev"
about "llama.cpp 多工具启动器"
flag "-h --help --usage" help=打印用法并退出
flag --version help=显示版本与构建信息
cmd serve help="HTTP API 服务器" {
    flag "-h --help --usage" help=打印用法并退出
    flag --version help=显示版本和构建信息
    flag --cache-list help=列出缓存中的模型
    flag --completion-bash help="输出可 source 的 bash 补全脚本"
    flag "-t --threads" help="生成时使用的 CPU 线程数（默认：-1）" {
        arg <N>
    }
    flag "-C --cpu-mask" help="CPU 亲和性掩码：任意长度十六进制。与 cpu-range 互补（默认：\"\")" {
        arg <M>
    }
    flag --cpu-range help="CPU 亲和性范围。与 --cpu-mask 互补" {
        arg <LO_HI>
    }
    flag --cpu-strict help="使用严格的 CPU 放置（默认：0）" {
        arg <N>
    }
    flag --prio help="设置进程/线程优先级：low(-1), normal(0), medium(1), high(2), realtime(3)（默认：0）" {
        arg <N>
    }
    flag --poll help="使用轮询级别等待工作（0 - 不轮询，默认：50）" {
        arg <0_100>
    }
    flag --cpu-mask-batch help="批处理 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag --cpu-strict-batch help="使用严格的 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <N>
    }
    flag --prio-batch help="设置进程/线程优先级：0-normal, 1-medium, 2-high, 3-realtime（默认：0）" {
        arg <N>
    }
    flag --poll-batch help="使用轮询等待工作（默认：与 --poll 相同）" {
        arg <N>
    }
    flag "-c --ctx-size" help="提示词上下文大小（默认：0，0 = 从模型加载）" {
        arg <N>
    }
    flag "-n --predict --n-predict" help="预测的 token 数量（默认：-1，-1 = 无限）" {
        arg <N>
    }
    flag "-b --batch-size" help=逻辑最大批处理大小（默认：2048） {
        arg <N>
    }
    flag --ubatch-size help=物理最大批处理大小（默认：512） {
        arg <N>
    }
    flag --keep help="从初始提示词中保留的 token 数（默认：0，-1 = 全部）" {
        arg <N>
    }
    flag "--perf --no-perf" help="是否启用内部 libllama 性能计时（默认：false）"
    flag "-e --escape --no-escape" help="是否处理转义序列 (\\n, \\r, \\t, \\', \\\", \\\\) （默认：true）"
    flag --override-tensor help=覆盖张量缓冲区类型 {
        arg <TENSOR>
    }
    flag --cpu-moe help="将所有 MoE 权重保留在 CPU 中"
    flag --n-cpu-moe help="将前 N 层的 MoE 权重保留在 CPU 中" {
        arg <N>
    }
    flag --n-cpu-ffn help="将前 N 层的密集 FFN 权重保留在 CPU 中" {
        arg <N>
    }
    flag --check-tensors help=检查模型张量数据中的无效值（默认：false）
    flag --override-kv help=按键覆盖模型元数据的高级选项 {
        arg <KEY_TYPE_VALUE>
    }
    flag "--op-offload --no-op-offload" help=是否将主机张量操作卸载到设备（默认：true）
    flag "-m --model" help=要加载的模型路径 {
        arg <FNAME>
    }
    flag --model-url help="模型下载 URL（默认：未使用）" {
        arg <MODEL_URL>
    }
    flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
        arg <TOKEN>
    }
    flag "--spec-draft-override-tensor --override-tensor-draft" help=覆盖草稿模型的张量缓冲区类型 {
        arg <TENSOR>
    }
    flag --embd-gemma-default help="使用默认 EmbeddingGemma 模型（可能从网络下载权重）"
    flag --fim-qwen-1.5b-default help="使用默认 Qwen 2.5 Coder 1.5B（可能从网络下载权重）"
    flag --fim-qwen-3b-default help="使用默认 Qwen 2.5 Coder 3B（可能从网络下载权重）"
    flag --fim-qwen-7b-default help="使用默认 Qwen 2.5 Coder 7B（可能从网络下载权重）"
    flag --fim-qwen-7b-spec help="使用 Qwen 2.5 Coder 7B + 0.5B draft 投机解码（可能从网络下载权重）"
    flag --fim-qwen-14b-spec help="使用 Qwen 2.5 Coder 14B + 0.5B draft 投机解码（可能从网络下载权重）"
    flag --fim-qwen-30b-default help="使用默认 Qwen 3 Coder 30B A3B Instruct（可能从网络下载权重）"
    flag --gpt-oss-20b-default help="使用 gpt-oss-20b（可能从网络下载权重）"
    flag --gpt-oss-120b-default help="使用 gpt-oss-120b（可能从网络下载权重）"
    flag --vision-gemma-4b-default help="使用 Gemma 3 4B QAT（可能从网络下载权重）"
    flag --vision-gemma-12b-default help="使用 Gemma 3 12B QAT（可能从网络下载权重）"
    flag --spec-default help=启用默认投机解码配置
    flag --rope-scaling help="RoPE 频率缩放方法（默认：linear）" {
        arg <NONE_LINEAR_YARN>
    }
    flag --rope-scale help="RoPE 上下文缩放因子" {
        arg <N>
    }
    flag --rope-freq-base help="RoPE 基础频率（默认：从模型加载）" {
        arg <N>
    }
    flag --rope-freq-scale help="RoPE 频率缩放因子" {
        arg <N>
    }
    flag --yarn-orig-ctx help="YaRN：模型原始上下文大小（默认：0 = 模型训练上下文大小）" {
        arg <N>
    }
    flag --yarn-ext-factor help=YaRN：外推混合因子（默认：-1.00） {
        arg <N>
    }
    flag --yarn-attn-factor help="YaRN：缩放 sqrt(t) 或注意力幅度（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-slow help="YaRN：高修正维度或 alpha（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-fast help="YaRN：低修正维度或 beta（默认：-1.00）" {
        arg <N>
    }
    flag "--repack --no-repack" help=是否启用权重重打包（默认：启用）
    flag --no-host help=绕过主机缓冲区以允许使用额外缓冲区
    flag --cache-type-k help="K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --cache-type-v help="V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --defrag-thold help="KV 缓存碎片整理阈值（已弃用）" {
        arg <N>
    }
    flag --kv-unified-per-slot help=每个并行槽位的上下文限制 {
        arg <N>
    }
    flag --checkpoint-min-step help=上下文检查点的最小间距（默认：8192） {
        arg <N>
    }
    flag --cache-ram help="设置最大缓存大小（MiB，默认：8192，-1 = 无限制，0 = 禁用）" {
        arg <N>
    }
    flag "--cache-prompt --no-cache-prompt" help=是否启用提示词缓存（默认：启用）
    flag --cache-reuse help="通过 KV 移位尝试从缓存中重用的最小块大小，需要启用提示词缓存（默认：0）" {
        arg <N>
    }
    flag --swa-full help="使用完整大小的 SWA 缓存（默认：false）"
    flag --rpc help="RPC 服务器列表，逗号分隔（host:port）" {
        arg <SERVERS>
    }
    flag --mlock help="已弃用，请使用 `--load-mode`：强制系统将模型保留在内存中，而不是交换或压缩"
    flag "--mmap --no-mmap" help="已弃用，请使用 `--load-mode`：是否将模型映射到内存（如果禁用 mmap，加载较慢，但如果未使用 mlock，可能会减少页面换出）"
    flag --load-mode help=模型加载模式（默认：auto） {
        arg <MODE>
    }
    flag --lazy-mode help=按需读取特定张量（默认：auto） {
        arg <MODE>
    }
    flag --device help="用于卸载的设备列表，逗号分隔（none = 不卸载）" {
        arg <DEV1_DEV2>
    }
    flag "--gpu-layers --n-gpu-layers" help="存储在显存中的最大层数，精确数字、'auto' 或 'all'（默认：auto）" {
        arg <N>
    }
    flag --tensor-split help="各 GPU 卸载比例，逗号分隔，如 3,1" {
        arg <N0_N1_N2>
    }
    flag --main-gpu help="用于模型的 GPU（默认：0）" {
        arg <INDEX>
    }
    flag --fit help=是否自动调整参数以适应显存（默认：on） {
        arg <ON_OFF>
    }
    flag --fit-ctx help="可由 --fit 设置的最小上下文大小（默认：4096）" {
        arg <N>
    }
    flag --lora help="LoRA 适配器路径（逗号分隔可加载多个）" {
        arg <FNAME>
    }
    flag --lora-scaled help="带缩放系数的 LoRA 适配器路径（格式：FNAME:SCALE,...）" {
        arg <FNAME_SCALE>
    }
    flag --control-vector help=添加控制向量（逗号分隔可添加多个） {
        arg <FNAME>
    }
    flag --control-vector-layer-range help=应用控制向量的层范围（含首尾） {
        arg <START>
    }
    flag --lora-init-without-apply help="加载 LoRA 适配器但不应用它们（稍后通过 POST /lora-adapters 应用）（默认：禁用）"
    flag "--spec-draft-poll --poll-draft" help="草稿模型轮询等待（默认：与 --poll 相同）" {
        arg <N>
    }
    flag --spec-draft-n-max help="投机解码的草稿 token 数（默认：3）" {
        arg <N>
    }
    flag --spec-draft-n-min help="投机解码的最小草稿 token 数（默认：0）" {
        arg <N>
    }
    flag --spec-synth-len help=目标平均合成接受长度，包括目标令牌（仅用于基准测试） {
        arg <L>
    }
    flag --spec-synth-rates help=逗号分隔的无条件逐位置合成接受概率（仅用于基准测试） {
        arg <P0_P1>
    }
    flag "--spec-draft-p-split --draft-p-split" help=投机解码分割概率（默认：0.10） {
        arg <P>
    }
    flag "--spec-draft-p-min --draft-p-min" help=投机解码最小概率（贪心，默认：0.00） {
        arg <P>
    }
    flag "--spec-draft-backend-sampling --no-spec-draft-backend-sampling" help=将草稿采样卸载到后端（默认：启用）
    flag --spec-type help=投机解码类型列表，逗号分隔（默认：none） {
        arg <NONE_DRAFT_SIMPLE_DRAFT_EAGLE3_DRAFT_MTP_DRAFT_DFLASH_DRAFT_DSPARK_NGRAM_SIMPLE_NGRAM_MAP_K_NGRAM_MAP_K4V_NGRAM_MOD_NGRAM_CACHE>
    }
    flag --spec-ngram-mod-n-min help="基于 n-gram 投机解码的最小 token 数（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-mod-n-max help="基于 n-gram 投机解码的最大 token 数（默认：64）" {
        arg <N>
    }
    flag --spec-ngram-mod-n-match help="ngram-mod 查找长度（默认：24）" {
        arg <N>
    }
    flag --spec-ngram-simple-size-n help="ngram-simple 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-simple-size-m help="ngram-simple 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-simple-min-hits help="ngram-simple 最小命中数（默认：1）" {
        arg <N>
    }
    flag --spec-ngram-map-k-size-n help="ngram-map-k 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-map-k-size-m help="ngram-map-k 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-map-k-min-hits help="ngram-map-k 最小命中数（默认：1）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-size-n help="ngram-map-k4v 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-size-m help="ngram-map-k4v 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-min-hits help="ngram-map-k4v 最小命中数（默认：1）" {
        arg <N>
    }
    flag "--draft --draft-n --draft-max" help="该参数已移除。请使用 --spec-draft-n-max 或 --spec-ngram-mod-n-max" {
        arg <N>
    }
    flag "--draft-min --draft-n-min" help="该参数已移除。请使用 --spec-draft-n-min 或 --spec-ngram-mod-n-min" {
        arg <N>
    }
    flag --spec-ngram-size-n help="该参数已移除。请使用对应的 --spec-ngram-*-size-n 或 --spec-ngram-mod-n-match" {
        arg <N>
    }
    flag --spec-ngram-size-m help="该参数已移除。请使用对应的 --spec-ngram-*-size-m" {
        arg <N>
    }
    flag --spec-ngram-min-hits help="该参数已移除。请使用对应的 --spec-ngram-*-min-hits" {
        arg <N>
    }
    flag --samplers help=按顺序使用的采样器，用分号分隔 {
        arg <SAMPLERS>
    }
    flag "-s --seed" help="随机数种子（默认：-1，-1 为随机）" {
        arg <SEED>
    }
    flag --ignore-eos help="忽略流结束 token 并继续生成"
    flag "--temp --temperature" help=温度（默认：0.80） {
        arg <N>
    }
    flag --top-k help="top-k 采样（默认：40，0 = 禁用）" {
        arg <N>
    }
    flag --top-p help="top-p 采样（默认：0.95，1.0 = 禁用）" {
        arg <N>
    }
    flag --min-p help="min-p 采样（默认：0.05，0.0 = 禁用）" {
        arg <N>
    }
    flag "--top-nsigma --top-n-sigma" help="top-n-sigma 采样（默认：-1.00，禁用）" {
        arg <N>
    }
    flag --xtc-probability help="XTC 概率（默认：0.00，禁用）" {
        arg <N>
    }
    flag --xtc-threshold help="XTC 阈值（默认：0.10，禁用）" {
        arg <N>
    }
    flag "--typical --typical-p" help=局部典型采样（默认：1.00，禁用） {
        arg <N>
    }
    flag --repeat-last-n help="考虑惩罚的最近 token 数（默认：64，禁用）" {
        arg <N>
    }
    flag --repeat-penalty help="重复 token 惩罚（默认：1.00，禁用）" {
        arg <N>
    }
    flag --presence-penalty help=重复存在惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --frequency-penalty help=重复频率惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --dry-multiplier help="DRY 采样乘数（默认：0.00，禁用）" {
        arg <N>
    }
    flag --dry-base help="DRY 采样基础值（默认：1.75）" {
        arg <N>
    }
    flag --dry-allowed-length help="DRY 采样允许长度（默认：2）" {
        arg <N>
    }
    flag --dry-penalty-last-n help="最近 n 个 token 的 DRY 惩罚（默认：64，禁用）" {
        arg <N>
    }
    flag --dry-sequence-breaker help="添加 DRY 采样的序列中断器" {
        arg <STRING>
    }
    flag --adaptive-target help="adaptive-p：选择接近此概率的令牌（有效范围 0.0 到 1.0；负数 = 禁用）（默认：-1.00）" {
        arg <N>
    }
    flag --adaptive-decay help="adaptive-p：目标适应的衰减率。值越低反应越快，值越高越稳定。（有效范围 0.0 到 0.99）（默认：0.90）" {
        arg <N>
    }
    flag --dynatemp-range help=动态温度范围（默认：0.00，禁用） {
        arg <N>
    }
    flag --dynatemp-exp help=动态温度指数（默认：1.00） {
        arg <N>
    }
    flag --mirostat help="使用 Mirostat 采样（默认：0，禁用）" {
        arg <N>
    }
    flag --mirostat-lr help="Mirostat 学习率（默认：0.10）" {
        arg <N>
    }
    flag --mirostat-ent help="Mirostat 目标熵（默认：5.00）" {
        arg <N>
    }
    flag "-l --logit-bias" help="修改令牌在完成中出现的可能性，例如 `--logit-bias 15043+1` 增加令牌 ' Hello' 的可能性，或 `--logit-bias 15043-1` 降低令牌 ' Hello' 的可能性" {
        arg <TOKEN_ID_BIAS>
    }
    flag --grammar help="用于约束生成的 BNF 风格语法" {
        arg <GRAMMAR>
    }
    flag --grammar-file help=语法文件路径 {
        arg <FNAME>
    }
    flag "-j --json-schema" help="用于约束生成的 JSON Schema" {
        arg <SCHEMA>
    }
    flag --json-schema-file help="包含 JSON Schema 的文件" {
        arg <FILE>
    }
    flag --backend-sampling help=启用后端采样（实验性，默认：禁用）
    flag --mmproj help=多模态投影器文件路径 {
        arg <FILE>
    }
    flag --mmproj-url help="多模态投影器文件 URL" {
        arg <URL>
    }
    flag "--mmproj-offload --no-mmproj-offload" help="是否为多模态投影器启用 GPU 卸载（默认：启用）"
    flag --mmproj-device help=多模态投影器使用的设备 {
        arg <DEVICE>
    }
    flag --image-min-tokens help="每张图像的最小 token 数（仅用于动态分辨率的视觉模型）" {
        arg <N>
    }
    flag --image-max-tokens help="每张图像的最大 token 数（仅用于动态分辨率的视觉模型）" {
        arg <N>
    }
    flag --mtmd-batch-max-tokens help="编码图像时每批最大图像 token 数（默认：1024）" {
        arg <N>
    }
    flag --video-fps help=目标视频帧率（默认：4.0） {
        arg <N>
    }
    flag --video-timestamp-interval help=文本时间戳之间的间隔（毫秒，默认：5000） {
        arg <N>
    }
    flag --video-ffmpeg-dir help="ffmpeg 和 ffprobe 所在目录路径（默认：在 PATH 中搜索）" {
        arg <DIR>
    }
    flag --reasoning help="在对话中使用推理/思考（默认：auto）" {
        arg <ON_OFF_AUTO>
    }
    flag --reasoning-effort help=推理努力级别（默认：default） {
        arg <LEVEL>
    }
    flag --reasoning-budget help="思考的 token 预算（默认：-1，无限制）" {
        arg <N>
    }
    flag --reasoning-budget-message help=推理预算耗尽时注入的消息（默认：无） {
        arg <MESSAGE>
    }
    flag "--reasoning-preserve --no-reasoning-preserve" help=在完整历史中保留推理轨迹，而不仅仅是最后一条助手消息（默认：启用）
    flag --parallel help=服务器槽位数（默认：-1，自动） {
        arg <N>
    }
    flag --host help="监听 IP 地址（默认：127.0.0.1）" {
        arg <HOST>
    }
    flag --port help=监听端口（默认：8080） {
        arg <PORT>
    }
    flag --reuse-port help="允许多个 socket 绑定到同一端口（默认：禁用）"
    flag --path help=静态文件服务路径 {
        arg <PATH>
    }
    flag --cors-origins help="CORS 允许的来源列表（默认：*）" {
        arg <ORIGINS>
    }
    flag --cors-methods help="CORS 允许的方法列表（默认：GET, POST, DELETE, OPTIONS）" {
        arg <METHODS>
    }
    flag --cors-headers help="CORS 允许的头部列表（默认：*）" {
        arg <HEADERS>
    }
    flag --api-prefix help=服务器提供服务的前缀路径 {
        arg <PREFIX>
    }
    flag --tools help="实验性：为 AI 代理启用内置工具（默认：禁用）" {
        arg <TOOL1_TOOL2>
    }
    flag --tools-runtime help=实验性：在独立运行时环境中运行工具（默认：无） {
        arg <OPTION>
    }
    flag --mcp-servers-config help="实验性：MCP 服务器定义 JSON 文件路径（默认：无）" {
        arg <PATH>
    }
    flag --mcp-servers-json help="实验性：内联 MCP 服务器定义 JSON（默认：无）" {
        arg <JSON>
    }
    flag "--agent --no-agent" help="是否启用 CORS 代理和所有内置工具（默认：禁用）"
    flag "--ui --webui --no-ui --no-webui" help="是否启用 Web UI（默认：启用）"
    flag "--embedding --embeddings" help=仅支持嵌入用例（默认：禁用）
    flag "--rerank --reranking" help=启用服务器重排序端点（默认：禁用）
    flag --api-key help="用于身份验证的 API 密钥" {
        arg <KEY>
    }
    flag --api-key-file help="包含 API 密钥的文件路径" {
        arg <FNAME>
    }
    flag --ssl-key-file help="PEM 编码的 SSL 私钥文件路径" {
        arg <FNAME>
    }
    flag --ssl-cert-file help="PEM 编码的 SSL 证书文件路径" {
        arg <FNAME>
    }
    flag --timeout help=服务器读写超时（秒，默认：3600） {
        arg <N>
    }
    flag --sse-ping-interval help="服务器 SSE 心跳间隔（秒，默认：30）" {
        arg <N>
    }
    flag --threads-http help="处理 HTTP 请求的线程数（默认：-1）" {
        arg <N>
    }
    flag --metrics help="启用 Prometheus 兼容的指标端点（默认：禁用）"
    flag --props help="启用通过 POST /props 更改全局属性（默认：禁用）"
    flag "--slots --no-slots" help=暴露槽位监控端点（默认：启用）
    flag --slot-save-path help="保存槽位 KV 缓存的路径（默认：禁用）" {
        arg <PATH>
    }
    flag --media-path help=加载本地媒体文件的目录（默认：禁用） {
        arg <PATH>
    }
    flag --models-dir help=路由器服务器的模型目录（默认：禁用） {
        arg <PATH>
    }
    flag --models-preset help="路由器服务器模型预设 INI 文件路径（默认：禁用）" {
        arg <PATH>
    }
    flag --models-max help="路由器服务器同时加载的最大模型数（默认：4，0 = 无限制）" {
        arg <N>
    }
    flag "--models-autoload --no-models-autoload" help=路由器服务器是否自动加载模型（默认：启用）
    flag --sleep-idle-seconds help=服务器空闲休眠秒数（默认：-1，禁用） {
        arg <SECONDS>
    }
    flag "-r --reverse-prompt" help="在 PROMPT 处停止生成，交互模式下返回控制权" {
        arg <PROMPT>
    }
    flag --special help="启用特殊 token 输出（默认：false）"
    flag "--warmup --no-warmup" help=是否用空运行进行预热（默认：启用）
    flag --chat-template-kwargs help="设置 JSON 模板解析器的额外参数" {
        arg <STRING>
    }
    flag "--jinja --no-jinja" help="是否使用 Jinja 模板引擎（默认：启用）"
    flag --log-disable help=禁用日志
    flag --log-file help=日志输出到文件 {
        arg <FNAME>
    }
    flag "--log-jsonl --no-log-jsonl" help="以 JSONL 格式记录日志（默认：禁用）"
    flag --log-colors help=设置彩色日志（默认：auto） {
        arg <ON_OFF_AUTO>
    }
    flag "-v --verbose --log-verbose" help=设置最大详细级别（用于调试）
    flag --offline help=离线模式：强制使用缓存，禁止网络访问
    flag "--verbosity --log-verbosity" help="详细级别阈值（默认：3，0=通用输出, 1=错误, 2=警告, 3=信息, 4=跟踪, 5=调试）" {
        arg <N>
    }
    flag "--log-prefix --no-log-prefix" help=在日志消息中启用前缀
    flag "--log-timestamps --no-log-timestamps" help=在日志消息中启用时间戳
    flag --log-prompts-dir help=将提示词记录到目录（仅用于调试，默认：禁用） {
        arg <PATH>
    }
    flag --threads-batch help="批处理和提示词处理的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag --cpu-range-batch help="批处理 CPU 亲和性范围。与 --cpu-mask-batch 互补" {
        arg <LO_HI>
    }
    flag --flash-attn help="设置闪存注意力使用（'on'、'off' 或 'auto'，默认：'auto'）" {
        arg <ON_OFF_AUTO>
    }
    flag "--kv-offload --no-kv-offload" help="是否启用 KV 缓存卸载（默认：启用）"
    flag "--direct-io --no-direct-io" help="已弃用，请使用 `--load-mode`：如果可用，使用 DirectIO"
    flag --numa help="尝试优化某些 NUMA 系统上的性能 - distribute：在所有节点上均匀分布执行 - isolate：仅在执行开始的节点上的 CPU 上生成线程 - numactl：使用 numactl 提供的 CPU 映射。如果之前未运行此命令，建议在使用此命令前丢弃系统页面缓存。参见 https://github.com/ggml-org/llama.cpp/issues/1437" {
        arg <TYPE>
    }
    flag --list-devices help=列出可用设备并退出
    flag --split-mode help="多 GPU 模型分割方式（默认：layer）" {
        arg <NONE_LAYER_ROW_TENSOR>
    }
    flag --fit-target help=每个设备的目标余量（MiB），默认：1024 {
        arg <MIB0_MIB1_MIB2>
    }
    flag --control-vector-scaled help=添加带缩放的控制向量（格式：FNAME:SCALE,...） {
        arg <FNAME_SCALE>
    }
    flag --docker-repo help="Docker Hub 模型仓库。repo 可选，默认为 ai/。quant 可选，默认为 :latest。示例：gemma3（默认：未使用）" {
        arg <REPO_MODEL_QUANT>
    }
    flag --hf-repo help="Hugging Face 模型仓库；quant 可选，不区分大小写，默认 Q4_K_M，如果 Q4_K_M 不存在则回退到仓库中的第一个文件。mmproj 如果可用也会自动下载。要禁用，请添加 --no-mmproj。示例：ggml-org/GLM-4.7-Flash-GGUF:Q4_K_M（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag "--spec-draft-type-k --cache-type-k-draft" help="草稿模型 K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--spec-draft-type-v --cache-type-v-draft" help="草稿模型 V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--sampler-seq --sampling-seq" help=采样器的简化序列（默认：edskypmxt） {
        arg <SEQUENCE>
    }
    flag "--spec-draft-hf --hf-repo-draft" help="与 --hf-repo 相同，但用于草稿模型（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag "--spec-draft-threads --threads-draft" help="草稿模型生成时的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag "--spec-draft-threads-batch --threads-batch-draft" help="草稿模型批处理的线程数（默认：与 --threads-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-cpu-mask --cpu-mask-draft" help="草稿模型 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag "--spec-draft-cpu-range --cpu-range-draft" help="草稿模型 CPU 亲和性范围" {
        arg <LO_HI>
    }
    flag "--spec-draft-cpu-strict --cpu-strict-draft" help="草稿模型严格 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <N>
    }
    flag "--spec-draft-prio --prio-draft" help="set draft process/thread priority : 0-normal, 1-medium, 2-high, 3-realtime (default: 0)" {
        arg <N>
    }
    flag "--spec-draft-cpu-mask-batch --cpu-mask-batch-draft" help="草稿模型 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag "--spec-draft-cpu-strict-batch --cpu-strict-batch-draft" help="草稿模型严格 CPU 放置（默认：与 --cpu-strict-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-prio-batch --prio-batch-draft" help="草稿模型进程/线程优先级（默认：0）" {
        arg <N>
    }
    flag "--spec-draft-poll-batch --poll-batch-draft" help="草稿模型轮询等待（默认：与 --poll-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-cpu-moe --cpu-moe-draft" help="将草稿模型的所有 MoE 权重保留在 CPU 中"
    flag "--spec-draft-n-cpu-moe --spec-draft-ncmoe --n-cpu-moe-draft" help="将草稿模型前 N 层的 MoE 权重保留在 CPU 中" {
        arg <N>
    }
    flag "--spec-draft-device --device-draft" help="草稿模型卸载设备列表（默认：跟随 --device）" {
        arg <DEV1_DEV2>
    }
    flag "--spec-draft-ngl --gpu-layers-draft --n-gpu-layers-draft" help=草稿模型显存层数（默认：auto） {
        arg <N>
    }
    flag "--spec-draft-model --model-draft" help=投机解码草稿模型（默认：未使用） {
        arg <FNAME>
    }
    flag --lookup-cache-static help="path to static lookup cache to use for lookup decoding (not updated by generation)" {
        arg <FNAME>
    }
    flag --lookup-cache-dynamic help="path to dynamic lookup cache to use for lookup decoding (updated by generation)" {
        arg <FNAME>
    }
    flag "--ctx-checkpoints --swa-checkpoints" help=每个槽位要创建的最大上下文检查点数（默认：32） {
        arg <N>
    }
    flag "--kv-unified --no-kv-unified" help="使用跨所有序列共享的统一 KV 缓冲区"
    flag "--cache-idle-slots --no-cache-idle-slots" help=空闲槽位保存到提示词缓存（默认：启用）
    flag "--context-shift --no-context-shift" help=是否在无限文本生成时使用上下文移位（默认：禁用）
    flag --spm-infill help="use Suffix/Prefix/Middle pattern for infill (instead of Prefix/Suffix/Middle) as some models prefer this. (default: disabled)"
    flag --pooling help=嵌入池化类型（默认：使用模型默认） {
        arg <NONE_MEAN_CLS_LAST_RANK>
    }
    flag "--cont-batching --no-cont-batching" help=是否启用连续批处理（默认：启用）
    flag "--mmproj-auto --no-mmproj --no-mmproj-auto" help=是否使用多模态投影器文件（默认：启用）
    flag "-a --alias" help="设置模型名称别名，逗号分隔（供 API 使用）" {
        arg <STRING>
    }
    flag --tags help=设置模型标签，逗号分隔（仅信息，不用于路由） {
        arg <STRING>
    }
    flag --embd-normalize help=嵌入归一化（默认：2） {
        arg <N>
    }
    flag "--cors-credentials --no-cors-credentials" help="是否允许 CORS 凭证（默认：启用）"
    flag "--ui-config --webui-config" help="JSON that provides default UI settings (overrides UI defaults)" {
        arg <JSON>
    }
    flag "--ui-config-file --webui-config-file" help="JSON file that provides default UI settings (overrides UI defaults)" {
        arg <PATH>
    }
    flag "--ui-mcp-proxy --webui-mcp-proxy --no-ui-mcp-proxy --no-webui-mcp-proxy" help="实验性：是否启用 MCP CORS 代理（默认：禁用）"
    flag --reasoning-format help="控制是否允许从响应中提取思考标签，以及以哪种格式返回；选项之一：- none：将思考保留在 `message.content` 中未解析 - deepseek：将思考放在 `message.reasoning_content` 中 - deepseek-legacy：在 `message.content` 中保留 `<think>` 标签，同时填充 `message.reasoning_content`（默认：auto）" {
        arg <FORMAT>
    }
    flag --chat-template help="设置自定义 Jinja 聊天模板（默认：从模型元数据获取的模板）。如果指定了 suffix/prefix，模板将被禁用。仅接受常用模板（除非在此标志之前设置了 --jinja）：内置模板列表： bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
        arg <JINJA_TEMPLATE>
    }
    flag --chat-template-file help="设置自定义 Jinja 聊天模板文件（默认：从模型元数据获取的模板）。如果指定了 suffix/prefix，模板将被禁用。仅接受常用模板（除非在此标志之前设置了 --jinja）：内置模板列表： bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
        arg <JINJA_TEMPLATE_FILE>
    }
    flag "--skip-chat-parsing --no-skip-chat-parsing" help="强制使用纯内容解析器，即使指定了 Jinja 模板；模型将在内容部分输出所有内容，包括任何推理和/或工具调用（默认：禁用）"
    flag "--prefill-assistant --no-prefill-assistant" help=是否预填充助手响应（默认：启用）
    flag --slot-prompt-similarity help=请求与槽位的匹配阈值（默认：0.10） {
        arg <SIMILARITY>
    }
}
cmd cli help=命令行交互界面 {
    flag "-h --help --usage" help=打印用法并退出
    flag --version help=显示版本和构建信息
    flag --cache-list help=列出缓存中的模型
    flag --completion-bash help="输出可 source 的 bash 补全脚本"
    flag "-t --threads" help="生成时使用的 CPU 线程数（默认：-1）" {
        arg <N>
    }
    flag "-C --cpu-mask" help="CPU 亲和性掩码：任意长度十六进制。与 cpu-range 互补（默认：\"\")" {
        arg <M>
    }
    flag --cpu-range help="CPU 亲和性范围。与 --cpu-mask 互补" {
        arg <LO_HI>
    }
    flag --cpu-strict help="使用严格的 CPU 放置（默认：0）" {
        arg <N>
    }
    flag --prio help="设置进程/线程优先级：low(-1), normal(0), medium(1), high(2), realtime(3)（默认：0）" {
        arg <N>
    }
    flag --poll help="使用轮询级别等待工作（0 - 不轮询，默认：50）" {
        arg <0_100>
    }
    flag --cpu-mask-batch help="批处理 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag --cpu-strict-batch help="使用严格的 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <N>
    }
    flag --prio-batch help="设置进程/线程优先级：0-normal, 1-medium, 2-high, 3-realtime（默认：0）" {
        arg <N>
    }
    flag --poll-batch help="使用轮询等待工作（默认：与 --poll 相同）" {
        arg <N>
    }
    flag "-c --ctx-size" help="提示词上下文大小（默认：0，0 = 从模型加载）" {
        arg <N>
    }
    flag "-n --predict --n-predict" help="预测的 token 数量（默认：-1，-1 = 无限）" {
        arg <N>
    }
    flag "-b --batch-size" help=逻辑最大批处理大小（默认：2048） {
        arg <N>
    }
    flag --ubatch-size help=物理最大批处理大小（默认：512） {
        arg <N>
    }
    flag --keep help="从初始提示词中保留的 token 数（默认：0，-1 = 全部）" {
        arg <N>
    }
    flag "--perf --no-perf" help="是否启用内部 libllama 性能计时（默认：false）"
    flag "-e --escape --no-escape" help="是否处理转义序列 (\\n, \\r, \\t, \\', \\\", \\\\) （默认：true）"
    flag --parallel help=并行解码的序列数（默认：1） {
        arg <N>
    }
    flag --override-tensor help=覆盖张量缓冲区类型 {
        arg <TENSOR>
    }
    flag --cpu-moe help="将所有 MoE 权重保留在 CPU 中"
    flag --n-cpu-moe help="将前 N 层的 MoE 权重保留在 CPU 中" {
        arg <N>
    }
    flag --n-cpu-ffn help="将前 N 层的密集 FFN 权重保留在 CPU 中" {
        arg <N>
    }
    flag --check-tensors help=检查模型张量数据中的无效值（默认：false）
    flag --override-kv help=按键覆盖模型元数据的高级选项 {
        arg <KEY_TYPE_VALUE>
    }
    flag "--op-offload --no-op-offload" help=是否将主机张量操作卸载到设备（默认：true）
    flag "-m --model" help=要加载的模型路径 {
        arg <FNAME>
    }
    flag --model-url help="模型下载 URL（默认：未使用）" {
        arg <MODEL_URL>
    }
    flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
        arg <TOKEN>
    }
    flag "--spec-draft-override-tensor --override-tensor-draft" help=覆盖草稿模型的张量缓冲区类型 {
        arg <TENSOR>
    }
    flag --gpt-oss-20b-default help="使用 gpt-oss-20b（可能从网络下载权重）"
    flag --gpt-oss-120b-default help="使用 gpt-oss-120b（可能从网络下载权重）"
    flag --vision-gemma-4b-default help="使用 Gemma 3 4B QAT（可能从网络下载权重）"
    flag --vision-gemma-12b-default help="使用 Gemma 3 12B QAT（可能从网络下载权重）"
    flag --spec-default help=启用默认投机解码配置
    flag --rope-scaling help="RoPE 频率缩放方法（默认：linear）" {
        arg <NONE_LINEAR_YARN>
    }
    flag --rope-scale help="RoPE 上下文缩放因子" {
        arg <N>
    }
    flag --rope-freq-base help="RoPE 基础频率（默认：从模型加载）" {
        arg <N>
    }
    flag --rope-freq-scale help="RoPE 频率缩放因子" {
        arg <N>
    }
    flag --yarn-orig-ctx help="YaRN：模型原始上下文大小（默认：0 = 模型训练上下文大小）" {
        arg <N>
    }
    flag --yarn-ext-factor help=YaRN：外推混合因子（默认：-1.00） {
        arg <N>
    }
    flag --yarn-attn-factor help="YaRN：缩放 sqrt(t) 或注意力幅度（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-slow help="YaRN：高修正维度或 alpha（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-fast help="YaRN：低修正维度或 beta（默认：-1.00）" {
        arg <N>
    }
    flag "--repack --no-repack" help=是否启用权重重打包（默认：启用）
    flag --no-host help=绕过主机缓冲区以允许使用额外缓冲区
    flag --cache-type-k help="K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --cache-type-v help="V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --defrag-thold help="KV 缓存碎片整理阈值（已弃用）" {
        arg <N>
    }
    flag --cache-ram help="设置最大缓存大小（MiB，默认：8192，-1 = 无限制，0 = 禁用）" {
        arg <N>
    }
    flag --swa-full help="使用完整大小的 SWA 缓存（默认：false）"
    flag --rpc help="RPC 服务器列表，逗号分隔（host:port）" {
        arg <SERVERS>
    }
    flag --mlock help="已弃用，请使用 `--load-mode`：强制系统将模型保留在内存中，而不是交换或压缩"
    flag "--mmap --no-mmap" help="已弃用，请使用 `--load-mode`：是否将模型映射到内存（如果禁用 mmap，加载较慢，但如果未使用 mlock，可能会减少页面换出）"
    flag --load-mode help=模型加载模式（默认：auto） {
        arg <MODE>
    }
    flag --lazy-mode help=按需读取特定张量（默认：auto） {
        arg <MODE>
    }
    flag --device help="用于卸载的设备列表，逗号分隔（none = 不卸载）" {
        arg <DEV1_DEV2>
    }
    flag "--gpu-layers --n-gpu-layers" help="存储在显存中的最大层数，精确数字、'auto' 或 'all'（默认：auto）" {
        arg <N>
    }
    flag --tensor-split help="各 GPU 卸载比例，逗号分隔，如 3,1" {
        arg <N0_N1_N2>
    }
    flag --main-gpu help="用于模型的 GPU（默认：0）" {
        arg <INDEX>
    }
    flag --fit help=是否自动调整参数以适应显存（默认：on） {
        arg <ON_OFF>
    }
    flag --fit-ctx help="可由 --fit 设置的最小上下文大小（默认：4096）" {
        arg <N>
    }
    flag --lora help="LoRA 适配器路径（逗号分隔可加载多个）" {
        arg <FNAME>
    }
    flag --lora-scaled help="带缩放系数的 LoRA 适配器路径（格式：FNAME:SCALE,...）" {
        arg <FNAME_SCALE>
    }
    flag --control-vector help=添加控制向量（逗号分隔可添加多个） {
        arg <FNAME>
    }
    flag --control-vector-layer-range help=应用控制向量的层范围（含首尾） {
        arg <START>
    }
    flag "--spec-draft-poll --poll-draft" help="草稿模型轮询等待（默认：与 --poll 相同）" {
        arg <N>
    }
    flag --spec-draft-n-max help="投机解码的草稿 token 数（默认：3）" {
        arg <N>
    }
    flag --spec-draft-n-min help="投机解码的最小草稿 token 数（默认：0）" {
        arg <N>
    }
    flag --spec-synth-len help=目标平均合成接受长度，包括目标令牌（仅用于基准测试） {
        arg <L>
    }
    flag --spec-synth-rates help=逗号分隔的无条件逐位置合成接受概率（仅用于基准测试） {
        arg <P0_P1>
    }
    flag "--spec-draft-p-split --draft-p-split" help=投机解码分割概率（默认：0.10） {
        arg <P>
    }
    flag "--spec-draft-p-min --draft-p-min" help=投机解码最小概率（贪心，默认：0.00） {
        arg <P>
    }
    flag "--spec-draft-backend-sampling --no-spec-draft-backend-sampling" help=将草稿采样卸载到后端（默认：启用）
    flag --spec-type help=投机解码类型列表，逗号分隔（默认：none） {
        arg <NONE_DRAFT_SIMPLE_DRAFT_EAGLE3_DRAFT_MTP_DRAFT_DFLASH_DRAFT_DSPARK_NGRAM_SIMPLE_NGRAM_MAP_K_NGRAM_MAP_K4V_NGRAM_MOD_NGRAM_CACHE>
    }
    flag --spec-ngram-mod-n-min help="基于 n-gram 投机解码的最小 token 数（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-mod-n-max help="基于 n-gram 投机解码的最大 token 数（默认：64）" {
        arg <N>
    }
    flag --spec-ngram-mod-n-match help="ngram-mod 查找长度（默认：24）" {
        arg <N>
    }
    flag --spec-ngram-simple-size-n help="ngram-simple 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-simple-size-m help="ngram-simple 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-simple-min-hits help="ngram-simple 最小命中数（默认：1）" {
        arg <N>
    }
    flag --spec-ngram-map-k-size-n help="ngram-map-k 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-map-k-size-m help="ngram-map-k 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-map-k-min-hits help="ngram-map-k 最小命中数（默认：1）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-size-n help="ngram-map-k4v 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-size-m help="ngram-map-k4v 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-min-hits help="ngram-map-k4v 最小命中数（默认：1）" {
        arg <N>
    }
    flag "--draft --draft-n --draft-max" help="该参数已移除。请使用 --spec-draft-n-max 或 --spec-ngram-mod-n-max" {
        arg <N>
    }
    flag "--draft-min --draft-n-min" help="该参数已移除。请使用 --spec-draft-n-min 或 --spec-ngram-mod-n-min" {
        arg <N>
    }
    flag --samplers help=按顺序使用的采样器，用分号分隔 {
        arg <SAMPLERS>
    }
    flag "-s --seed" help="随机数种子（默认：-1，-1 为随机）" {
        arg <SEED>
    }
    flag --ignore-eos help="忽略流结束 token 并继续生成"
    flag "--temp --temperature" help=温度（默认：0.80） {
        arg <N>
    }
    flag --top-k help="top-k 采样（默认：40，0 = 禁用）" {
        arg <N>
    }
    flag --top-p help="top-p 采样（默认：0.95，1.0 = 禁用）" {
        arg <N>
    }
    flag --min-p help="min-p 采样（默认：0.05，0.0 = 禁用）" {
        arg <N>
    }
    flag "--top-nsigma --top-n-sigma" help="top-n-sigma 采样（默认：-1.00，禁用）" {
        arg <N>
    }
    flag --xtc-probability help="XTC 概率（默认：0.00，禁用）" {
        arg <N>
    }
    flag --xtc-threshold help="XTC 阈值（默认：0.10，禁用）" {
        arg <N>
    }
    flag "--typical --typical-p" help=局部典型采样（默认：1.00，禁用） {
        arg <N>
    }
    flag --repeat-last-n help="考虑惩罚的最近 token 数（默认：64，禁用）" {
        arg <N>
    }
    flag --repeat-penalty help="重复 token 惩罚（默认：1.00，禁用）" {
        arg <N>
    }
    flag --presence-penalty help=重复存在惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --frequency-penalty help=重复频率惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --dry-multiplier help="DRY 采样乘数（默认：0.00，禁用）" {
        arg <N>
    }
    flag --dry-base help="DRY 采样基础值（默认：1.75）" {
        arg <N>
    }
    flag --dry-allowed-length help="DRY 采样允许长度（默认：2）" {
        arg <N>
    }
    flag --dry-penalty-last-n help="最近 n 个 token 的 DRY 惩罚（默认：64，禁用）" {
        arg <N>
    }
    flag --dry-sequence-breaker help="添加 DRY 采样的序列中断器" {
        arg <STRING>
    }
    flag --adaptive-target help="adaptive-p：选择接近此概率的令牌（有效范围 0.0 到 1.0；负数 = 禁用）（默认：-1.00）" {
        arg <N>
    }
    flag --adaptive-decay help="adaptive-p：目标适应的衰减率。值越低反应越快，值越高越稳定。（有效范围 0.0 到 0.99）（默认：0.90）" {
        arg <N>
    }
    flag --dynatemp-range help=动态温度范围（默认：0.00，禁用） {
        arg <N>
    }
    flag --dynatemp-exp help=动态温度指数（默认：1.00） {
        arg <N>
    }
    flag --mirostat help="使用 Mirostat 采样（默认：0，禁用）" {
        arg <N>
    }
    flag --mirostat-lr help="Mirostat 学习率（默认：0.10）" {
        arg <N>
    }
    flag --mirostat-ent help="Mirostat 目标熵（默认：5.00）" {
        arg <N>
    }
    flag "-l --logit-bias" help="修改令牌在完成中出现的可能性，例如 `--logit-bias 15043+1` 增加令牌 ' Hello' 的可能性，或 `--logit-bias 15043-1` 降低令牌 ' Hello' 的可能性" {
        arg <TOKEN_ID_BIAS>
    }
    flag --grammar help="用于约束生成的 BNF 风格语法" {
        arg <GRAMMAR>
    }
    flag --grammar-file help=语法文件路径 {
        arg <FNAME>
    }
    flag "-j --json-schema" help="用于约束生成的 JSON Schema" {
        arg <SCHEMA>
    }
    flag --json-schema-file help="包含 JSON Schema 的文件" {
        arg <FILE>
    }
    flag --backend-sampling help=启用后端采样（实验性，默认：禁用）
    flag --mmproj help=多模态投影器文件路径 {
        arg <FILE>
    }
    flag --mmproj-url help="多模态投影器文件 URL" {
        arg <URL>
    }
    flag "--mmproj-offload --no-mmproj-offload" help="是否为多模态投影器启用 GPU 卸载（默认：启用）"
    flag --mmproj-device help=多模态投影器使用的设备 {
        arg <DEVICE>
    }
    flag "--image --audio --video" help=图像、音频或视频文件路径（多模态模型使用） {
        arg <FILE>
    }
    flag --image-min-tokens help="每张图像的最小 token 数（仅用于动态分辨率的视觉模型）" {
        arg <N>
    }
    flag --image-max-tokens help="每张图像的最大 token 数（仅用于动态分辨率的视觉模型）" {
        arg <N>
    }
    flag --video-fps help=目标视频帧率（默认：4.0） {
        arg <N>
    }
    flag --video-timestamp-interval help=文本时间戳之间的间隔（毫秒，默认：5000） {
        arg <N>
    }
    flag --video-ffmpeg-dir help="ffmpeg 和 ffprobe 所在目录路径（默认：在 PATH 中搜索）" {
        arg <DIR>
    }
    flag --reasoning help="在对话中使用推理/思考（默认：auto）" {
        arg <ON_OFF_AUTO>
    }
    flag --reasoning-effort help=推理努力级别（默认：default） {
        arg <LEVEL>
    }
    flag --reasoning-budget help="思考的 token 预算（默认：-1，无限制）" {
        arg <N>
    }
    flag --reasoning-budget-message help=推理预算耗尽时注入的消息（默认：无） {
        arg <MESSAGE>
    }
    flag "--reasoning-preserve --no-reasoning-preserve" help=在完整历史中保留推理轨迹，而不仅仅是最后一条助手消息（默认：启用）
    flag "-p --prompt" help="起始生成的提示词；系统消息请用 -sys" {
        arg <PROMPT>
    }
    flag "-f --file" help=包含提示词的文件（默认：无） {
        arg <FNAME>
    }
    flag --binary-file help=包含提示词的二进制文件（默认：无） {
        arg <FNAME>
    }
    flag --server-base help=连接到此服务器而非启动新服务器 {
        arg <URL>
    }
    flag --verbose-prompt help=在生成前打印详细提示词（默认：false）
    flag "--display-prompt --no-display-prompt" help=是否在生成时打印提示词（默认：true）
    flag --color help="Colorize output to distinguish prompt and user input from generations ('on', 'off', or 'auto', default: 'auto') 'auto' enables colors when output is to a terminal" {
        arg <ON_OFF_AUTO>
    }
    flag --system-prompt help=用于模型的系统提示词 {
        arg <PROMPT>
    }
    flag "--show-timings --no-show-timings" help=是否在每次响应后显示计时信息（默认：true）
    flag "-r --reverse-prompt" help="在 PROMPT 处停止生成，交互模式下返回控制权" {
        arg <PROMPT>
    }
    flag --special help="启用特殊 token 输出（默认：false）"
    flag --single-turn help=仅运行单轮对话（默认：false）
    flag --multiline-input help="allows you to write or paste multiple lines without ending each in '\\'"
    flag "--warmup --no-warmup" help=是否用空运行进行预热（默认：启用）
    flag --chat-template-kwargs help="设置 JSON 模板解析器的额外参数" {
        arg <STRING>
    }
    flag "--jinja --no-jinja" help="是否使用 Jinja 模板引擎（默认：启用）"
    flag --log-disable help=禁用日志
    flag --log-file help=日志输出到文件 {
        arg <FNAME>
    }
    flag "--log-jsonl --no-log-jsonl" help="以 JSONL 格式记录日志（默认：禁用）"
    flag --log-colors help=设置彩色日志（默认：auto） {
        arg <ON_OFF_AUTO>
    }
    flag "-v --verbose --log-verbose" help=设置最大详细级别（用于调试）
    flag --offline help=离线模式：强制使用缓存，禁止网络访问
    flag "--verbosity --log-verbosity" help="详细级别阈值（默认：1，0=通用输出, 1=错误, 2=警告, 3=信息, 4=跟踪, 5=调试）" {
        arg <N>
    }
    flag "--log-prefix --no-log-prefix" help=在日志消息中启用前缀
    flag "--log-timestamps --no-log-timestamps" help=在日志消息中启用时间戳
    flag --log-prompts-dir help=将提示词记录到目录（仅用于调试，默认：禁用） {
        arg <PATH>
    }
    flag --threads-batch help="批处理和提示词处理的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag --cpu-range-batch help="批处理 CPU 亲和性范围。与 --cpu-mask-batch 互补" {
        arg <LO_HI>
    }
    flag --flash-attn help="设置闪存注意力使用（'on'、'off' 或 'auto'，默认：'auto'）" {
        arg <ON_OFF_AUTO>
    }
    flag "--kv-offload --no-kv-offload" help="是否启用 KV 缓存卸载（默认：启用）"
    flag "--direct-io --no-direct-io" help="已弃用，请使用 `--load-mode`：如果可用，使用 DirectIO"
    flag --numa help="尝试优化某些 NUMA 系统上的性能 - distribute：在所有节点上均匀分布执行 - isolate：仅在执行开始的节点上的 CPU 上生成线程 - numactl：使用 numactl 提供的 CPU 映射。如果之前未运行此命令，建议在使用此命令前丢弃系统页面缓存。参见 https://github.com/ggml-org/llama.cpp/issues/1437" {
        arg <TYPE>
    }
    flag --list-devices help=列出可用设备并退出
    flag --split-mode help="多 GPU 模型分割方式（默认：layer）" {
        arg <NONE_LAYER_ROW_TENSOR>
    }
    flag --fit-target help=每个设备的目标余量（MiB），默认：1024 {
        arg <MIB0_MIB1_MIB2>
    }
    flag --control-vector-scaled help=添加带缩放的控制向量（格式：FNAME:SCALE,...） {
        arg <FNAME_SCALE>
    }
    flag --docker-repo help="Docker Hub 模型仓库。repo 可选，默认为 ai/。quant 可选，默认为 :latest。示例：gemma3（默认：未使用）" {
        arg <REPO_MODEL_QUANT>
    }
    flag --hf-repo help="Hugging Face 模型仓库；quant 可选，不区分大小写，默认 Q4_K_M，如果 Q4_K_M 不存在则回退到仓库中的第一个文件。mmproj 如果可用也会自动下载。要禁用，请添加 --no-mmproj。示例：ggml-org/GLM-4.7-Flash-GGUF:Q4_K_M（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag "--spec-draft-type-k --cache-type-k-draft" help="草稿模型 K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--spec-draft-type-v --cache-type-v-draft" help="草稿模型 V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--sampler-seq --sampling-seq" help=采样器的简化序列（默认：edskypmxt） {
        arg <SEQUENCE>
    }
    flag "--spec-draft-hf --hf-repo-draft" help="与 --hf-repo 相同，但用于草稿模型（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag "--spec-draft-threads --threads-draft" help="草稿模型生成时的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag "--spec-draft-threads-batch --threads-batch-draft" help="草稿模型批处理的线程数（默认：与 --threads-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-cpu-mask --cpu-mask-draft" help="草稿模型 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag "--spec-draft-cpu-range --cpu-range-draft" help="草稿模型 CPU 亲和性范围" {
        arg <LO_HI>
    }
    flag "--spec-draft-cpu-strict --cpu-strict-draft" help="草稿模型严格 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <N>
    }
    flag "--spec-draft-prio --prio-draft" help="set draft process/thread priority : 0-normal, 1-medium, 2-high, 3-realtime (default: 0)" {
        arg <N>
    }
    flag "--spec-draft-cpu-mask-batch --cpu-mask-batch-draft" help="草稿模型 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag "--spec-draft-cpu-strict-batch --cpu-strict-batch-draft" help="草稿模型严格 CPU 放置（默认：与 --cpu-strict-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-prio-batch --prio-batch-draft" help="草稿模型进程/线程优先级（默认：0）" {
        arg <N>
    }
    flag "--spec-draft-poll-batch --poll-batch-draft" help="草稿模型轮询等待（默认：与 --poll-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-cpu-moe --cpu-moe-draft" help="将草稿模型的所有 MoE 权重保留在 CPU 中"
    flag "--spec-draft-n-cpu-moe --spec-draft-ncmoe --n-cpu-moe-draft" help="将草稿模型前 N 层的 MoE 权重保留在 CPU 中" {
        arg <N>
    }
    flag "--spec-draft-device --device-draft" help="草稿模型卸载设备列表（默认：跟随 --device）" {
        arg <DEV1_DEV2>
    }
    flag "--spec-draft-ngl --gpu-layers-draft --n-gpu-layers-draft" help=草稿模型显存层数（默认：auto） {
        arg <N>
    }
    flag "--spec-draft-model --model-draft" help=投机解码草稿模型（默认：未使用） {
        arg <FNAME>
    }
    flag "--ctx-checkpoints --swa-checkpoints" help=每个槽位要创建的最大上下文检查点数（默认：32） {
        arg <N>
    }
    flag "--context-shift --no-context-shift" help=是否在无限文本生成时使用上下文移位（默认：禁用）
    flag --system-prompt-file help="a file containing the system prompt (default: none)" {
        arg <FNAME>
    }
    flag "--mmproj-auto --no-mmproj --no-mmproj-auto" help=是否使用多模态投影器文件（默认：启用）
    flag "-o --output --output-file" help=输出文件（默认：空） {
        arg <FNAME>
    }
    flag --reasoning-format help="控制是否允许从响应中提取思考标签，以及以哪种格式返回；选项之一：- none：将思考保留在 `message.content` 中未解析 - deepseek：将思考放在 `message.reasoning_content` 中 - deepseek-legacy：在 `message.content` 中保留 `<think>` 标签，同时填充 `message.reasoning_content`（默认：auto）" {
        arg <FORMAT>
    }
    flag --chat-template help="设置自定义 Jinja 聊天模板（默认：从模型元数据获取的模板）。如果指定了 suffix/prefix，模板将被禁用。仅接受常用模板（除非在此标志之前设置了 --jinja）：内置模板列表： bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
        arg <JINJA_TEMPLATE>
    }
    flag --chat-template-file help="设置自定义 Jinja 聊天模板文件（默认：从模型元数据获取的模板）。如果指定了 suffix/prefix，模板将被禁用。仅接受常用模板（除非在此标志之前设置了 --jinja）：内置模板列表： bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
        arg <JINJA_TEMPLATE_FILE>
    }
    flag "--skip-chat-parsing --no-skip-chat-parsing" help="强制使用纯内容解析器，即使指定了 Jinja 模板；模型将在内容部分输出所有内容，包括任何推理和/或工具调用（默认：禁用）"
    flag --simple-io help="使用基本 I/O 以获得更好的兼容性"
}
cmd download help=下载模型 {
    flag "-h --help --usage" help=打印用法并退出
    flag "-m --model" help=要加载的模型路径 {
        arg <FNAME>
    }
    flag --model-url help="模型下载 URL（默认：未使用）" {
        arg <MODEL_URL>
    }
    flag --docker-repo help="Docker Hub 模型仓库（repo 可选，默认 ai/）" {
        arg <REPO>
    }
    flag --hf-repo help="Hugging Face 模型仓库；quant 可选，默认 Q4_K_M" {
        arg "<user/model:quant>"
    }
    flag --hf-file help="Hugging Face 模型文件（覆盖 --hf-repo 中的 quant）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量）" {
        arg <TOKEN>
    }
    flag --mtp help="同时下载 MTP 头（如果可用）"
    flag --dflash help="同时下载 DFlash sidecar（如果可用）"
    flag --eagle3 help="同时下载 Eagle3 sidecar（如果可用）"
    flag --no-mmproj help="禁用自动下载 mmproj"
    flag --offline help=离线模式：强制使用缓存，禁止网络访问
}
cmd update help="更新 llama 到最新版本"
cmd completion help=文本补全 {
    flag "-h --help --usage" help=打印用法并退出
    flag --version help=显示版本和构建信息
    flag --cache-list help=列出缓存中的模型
    flag --completion-bash help="输出可 source 的 bash 补全脚本"
    flag "-t --threads" help="生成时使用的 CPU 线程数（默认：-1）" {
        arg <N>
    }
    flag "-C --cpu-mask" help="CPU 亲和性掩码：任意长度十六进制。与 cpu-range 互补（默认：\"\")" {
        arg <M>
    }
    flag --cpu-range help="CPU 亲和性范围。与 --cpu-mask 互补" {
        arg <LO_HI>
    }
    flag --cpu-strict help="使用严格的 CPU 放置（默认：0）" {
        arg <N>
    }
    flag --prio help="设置进程/线程优先级：low(-1), normal(0), medium(1), high(2), realtime(3)（默认：0）" {
        arg <N>
    }
    flag --poll help="使用轮询级别等待工作（0 - 不轮询，默认：50）" {
        arg <0_100>
    }
    flag --cpu-mask-batch help="批处理 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag --cpu-strict-batch help="使用严格的 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <N>
    }
    flag --prio-batch help="设置进程/线程优先级：0-normal, 1-medium, 2-high, 3-realtime（默认：0）" {
        arg <N>
    }
    flag --poll-batch help="使用轮询等待工作（默认：与 --poll 相同）" {
        arg <N>
    }
    flag "-c --ctx-size" help="提示词上下文大小（默认：0，0 = 从模型加载）" {
        arg <N>
    }
    flag "-n --predict --n-predict" help="预测的 token 数量（默认：-1，-1 = 无限）" {
        arg <N>
    }
    flag "-b --batch-size" help=逻辑最大批处理大小（默认：2048） {
        arg <N>
    }
    flag --ubatch-size help=物理最大批处理大小（默认：512） {
        arg <N>
    }
    flag --keep help="从初始提示词中保留的 token 数（默认：0，-1 = 全部）" {
        arg <N>
    }
    flag "--perf --no-perf" help="是否启用内部 libllama 性能计时（默认：false）"
    flag "-e --escape --no-escape" help="是否处理转义序列 (\\n, \\r, \\t, \\', \\\", \\\\) （默认：true）"
    flag --parallel help=并行解码的序列数（默认：1） {
        arg <N>
    }
    flag --override-tensor help=覆盖张量缓冲区类型 {
        arg <TENSOR>
    }
    flag --cpu-moe help="将所有 MoE 权重保留在 CPU 中"
    flag --n-cpu-moe help="将前 N 层的 MoE 权重保留在 CPU 中" {
        arg <N>
    }
    flag --n-cpu-ffn help="将前 N 层的密集 FFN 权重保留在 CPU 中" {
        arg <N>
    }
    flag --check-tensors help=检查模型张量数据中的无效值（默认：false）
    flag --override-kv help=按键覆盖模型元数据的高级选项 {
        arg <KEY_TYPE_VALUE>
    }
    flag "--op-offload --no-op-offload" help=是否将主机张量操作卸载到设备（默认：true）
    flag "-m --model" help=要加载的模型路径 {
        arg <FNAME>
    }
    flag --model-url help="模型下载 URL（默认：未使用）" {
        arg <MODEL_URL>
    }
    flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
        arg <TOKEN>
    }
    flag "--spec-draft-override-tensor --override-tensor-draft" help=覆盖草稿模型的张量缓冲区类型 {
        arg <TENSOR>
    }
    flag --gpt-oss-20b-default help="使用 gpt-oss-20b（可能从网络下载权重）"
    flag --gpt-oss-120b-default help="使用 gpt-oss-120b（可能从网络下载权重）"
    flag --vision-gemma-4b-default help="使用 Gemma 3 4B QAT（可能从网络下载权重）"
    flag --vision-gemma-12b-default help="使用 Gemma 3 12B QAT（可能从网络下载权重）"
    flag --spec-default help=启用默认投机解码配置
    flag --rope-scaling help="RoPE 频率缩放方法（默认：linear）" {
        arg <NONE_LINEAR_YARN>
    }
    flag --rope-scale help="RoPE 上下文缩放因子" {
        arg <N>
    }
    flag --rope-freq-base help="RoPE 基础频率（默认：从模型加载）" {
        arg <N>
    }
    flag --rope-freq-scale help="RoPE 频率缩放因子" {
        arg <N>
    }
    flag --yarn-orig-ctx help="YaRN：模型原始上下文大小（默认：0 = 模型训练上下文大小）" {
        arg <N>
    }
    flag --yarn-ext-factor help=YaRN：外推混合因子（默认：-1.00） {
        arg <N>
    }
    flag --yarn-attn-factor help="YaRN：缩放 sqrt(t) 或注意力幅度（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-slow help="YaRN：高修正维度或 alpha（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-fast help="YaRN：低修正维度或 beta（默认：-1.00）" {
        arg <N>
    }
    flag "--repack --no-repack" help=是否启用权重重打包（默认：启用）
    flag --no-host help=绕过主机缓冲区以允许使用额外缓冲区
    flag --cache-type-k help="K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --cache-type-v help="V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --defrag-thold help="KV 缓存碎片整理阈值（已弃用）" {
        arg <N>
    }
    flag --cache-ram help="设置最大缓存大小（MiB，默认：8192，-1 = 无限制，0 = 禁用）" {
        arg <N>
    }
    flag --swa-full help="使用完整大小的 SWA 缓存（默认：false）"
    flag --rpc help="RPC 服务器列表，逗号分隔（host:port）" {
        arg <SERVERS>
    }
    flag --mlock help="已弃用，请使用 `--load-mode`：强制系统将模型保留在内存中，而不是交换或压缩"
    flag "--mmap --no-mmap" help="已弃用，请使用 `--load-mode`：是否将模型映射到内存（如果禁用 mmap，加载较慢，但如果未使用 mlock，可能会减少页面换出）"
    flag --load-mode help=模型加载模式（默认：auto） {
        arg <MODE>
    }
    flag --lazy-mode help=按需读取特定张量（默认：auto） {
        arg <MODE>
    }
    flag --device help="用于卸载的设备列表，逗号分隔（none = 不卸载）" {
        arg <DEV1_DEV2>
    }
    flag "--gpu-layers --n-gpu-layers" help="存储在显存中的最大层数，精确数字、'auto' 或 'all'（默认：auto）" {
        arg <N>
    }
    flag --tensor-split help="各 GPU 卸载比例，逗号分隔，如 3,1" {
        arg <N0_N1_N2>
    }
    flag --main-gpu help="用于模型的 GPU（默认：0）" {
        arg <INDEX>
    }
    flag --fit help=是否自动调整参数以适应显存（默认：on） {
        arg <ON_OFF>
    }
    flag --fit-ctx help="可由 --fit 设置的最小上下文大小（默认：4096）" {
        arg <N>
    }
    flag --lora help="LoRA 适配器路径（逗号分隔可加载多个）" {
        arg <FNAME>
    }
    flag --lora-scaled help="带缩放系数的 LoRA 适配器路径（格式：FNAME:SCALE,...）" {
        arg <FNAME_SCALE>
    }
    flag --control-vector help=添加控制向量（逗号分隔可添加多个） {
        arg <FNAME>
    }
    flag --control-vector-layer-range help=应用控制向量的层范围（含首尾） {
        arg <START>
    }
    flag "--spec-draft-poll --poll-draft" help="草稿模型轮询等待（默认：与 --poll 相同）" {
        arg <N>
    }
    flag --spec-draft-n-max help="投机解码的草稿 token 数（默认：3）" {
        arg <N>
    }
    flag --spec-draft-n-min help="投机解码的最小草稿 token 数（默认：0）" {
        arg <N>
    }
    flag --spec-synth-len help=目标平均合成接受长度，包括目标令牌（仅用于基准测试） {
        arg <L>
    }
    flag --spec-synth-rates help=逗号分隔的无条件逐位置合成接受概率（仅用于基准测试） {
        arg <P0_P1>
    }
    flag "--spec-draft-p-split --draft-p-split" help=投机解码分割概率（默认：0.10） {
        arg <P>
    }
    flag "--spec-draft-p-min --draft-p-min" help=投机解码最小概率（贪心，默认：0.00） {
        arg <P>
    }
    flag "--spec-draft-backend-sampling --no-spec-draft-backend-sampling" help=将草稿采样卸载到后端（默认：启用）
    flag --spec-type help=投机解码类型列表，逗号分隔（默认：none） {
        arg <NONE_DRAFT_SIMPLE_DRAFT_EAGLE3_DRAFT_MTP_DRAFT_DFLASH_DRAFT_DSPARK_NGRAM_SIMPLE_NGRAM_MAP_K_NGRAM_MAP_K4V_NGRAM_MOD_NGRAM_CACHE>
    }
    flag --spec-ngram-mod-n-min help="基于 n-gram 投机解码的最小 token 数（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-mod-n-max help="基于 n-gram 投机解码的最大 token 数（默认：64）" {
        arg <N>
    }
    flag --spec-ngram-mod-n-match help="ngram-mod 查找长度（默认：24）" {
        arg <N>
    }
    flag --spec-ngram-simple-size-n help="ngram-simple 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-simple-size-m help="ngram-simple 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-simple-min-hits help="ngram-simple 最小命中数（默认：1）" {
        arg <N>
    }
    flag --spec-ngram-map-k-size-n help="ngram-map-k 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-map-k-size-m help="ngram-map-k 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-map-k-min-hits help="ngram-map-k 最小命中数（默认：1）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-size-n help="ngram-map-k4v 查找 n-gram 长度（默认：12）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-size-m help="ngram-map-k4v 草稿 m-gram 长度（默认：48）" {
        arg <N>
    }
    flag --spec-ngram-map-k4v-min-hits help="ngram-map-k4v 最小命中数（默认：1）" {
        arg <N>
    }
    flag "--draft --draft-n --draft-max" help="该参数已移除。请使用 --spec-draft-n-max 或 --spec-ngram-mod-n-max" {
        arg <N>
    }
    flag "--draft-min --draft-n-min" help="该参数已移除。请使用 --spec-draft-n-min 或 --spec-ngram-mod-n-min" {
        arg <N>
    }
    flag --samplers help=按顺序使用的采样器，用分号分隔 {
        arg <SAMPLERS>
    }
    flag "-s --seed" help="随机数种子（默认：-1，-1 为随机）" {
        arg <SEED>
    }
    flag --ignore-eos help="忽略流结束 token 并继续生成"
    flag "--temp --temperature" help=温度（默认：0.80） {
        arg <N>
    }
    flag --top-k help="top-k 采样（默认：40，0 = 禁用）" {
        arg <N>
    }
    flag --top-p help="top-p 采样（默认：0.95，1.0 = 禁用）" {
        arg <N>
    }
    flag --min-p help="min-p 采样（默认：0.05，0.0 = 禁用）" {
        arg <N>
    }
    flag "--top-nsigma --top-n-sigma" help="top-n-sigma 采样（默认：-1.00，禁用）" {
        arg <N>
    }
    flag --xtc-probability help="XTC 概率（默认：0.00，禁用）" {
        arg <N>
    }
    flag --xtc-threshold help="XTC 阈值（默认：0.10，禁用）" {
        arg <N>
    }
    flag "--typical --typical-p" help=局部典型采样（默认：1.00，禁用） {
        arg <N>
    }
    flag --repeat-last-n help="考虑惩罚的最近 token 数（默认：64，禁用）" {
        arg <N>
    }
    flag --repeat-penalty help="重复 token 惩罚（默认：1.00，禁用）" {
        arg <N>
    }
    flag --presence-penalty help=重复存在惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --frequency-penalty help=重复频率惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --dry-multiplier help="DRY 采样乘数（默认：0.00，禁用）" {
        arg <N>
    }
    flag --dry-base help="DRY 采样基础值（默认：1.75）" {
        arg <N>
    }
    flag --dry-allowed-length help="DRY 采样允许长度（默认：2）" {
        arg <N>
    }
    flag --dry-penalty-last-n help="最近 n 个 token 的 DRY 惩罚（默认：64，禁用）" {
        arg <N>
    }
    flag --dry-sequence-breaker help="添加 DRY 采样的序列中断器" {
        arg <STRING>
    }
    flag --adaptive-target help="adaptive-p：选择接近此概率的令牌（有效范围 0.0 到 1.0；负数 = 禁用）（默认：-1.00）" {
        arg <N>
    }
    flag --adaptive-decay help="adaptive-p：目标适应的衰减率。值越低反应越快，值越高越稳定。（有效范围 0.0 到 0.99）（默认：0.90）" {
        arg <N>
    }
    flag --dynatemp-range help=动态温度范围（默认：0.00，禁用） {
        arg <N>
    }
    flag --dynatemp-exp help=动态温度指数（默认：1.00） {
        arg <N>
    }
    flag --mirostat help="使用 Mirostat 采样（默认：0，禁用）" {
        arg <N>
    }
    flag --mirostat-lr help="Mirostat 学习率（默认：0.10）" {
        arg <N>
    }
    flag --mirostat-ent help="Mirostat 目标熵（默认：5.00）" {
        arg <N>
    }
    flag "-l --logit-bias" help="修改令牌在完成中出现的可能性，例如 `--logit-bias 15043+1` 增加令牌 ' Hello' 的可能性，或 `--logit-bias 15043-1` 降低令牌 ' Hello' 的可能性" {
        arg <TOKEN_ID_BIAS>
    }
    flag --grammar help="用于约束生成的 BNF 风格语法" {
        arg <GRAMMAR>
    }
    flag --grammar-file help=语法文件路径 {
        arg <FNAME>
    }
    flag "-j --json-schema" help="用于约束生成的 JSON Schema" {
        arg <SCHEMA>
    }
    flag --json-schema-file help="包含 JSON Schema 的文件" {
        arg <FILE>
    }
    flag --backend-sampling help=启用后端采样（实验性，默认：禁用）
    flag --mmproj help=多模态投影器文件路径 {
        arg <FILE>
    }
    flag --mmproj-url help="多模态投影器文件 URL" {
        arg <URL>
    }
    flag "--mmproj-offload --no-mmproj-offload" help="是否为多模态投影器启用 GPU 卸载（默认：启用）"
    flag --mmproj-device help=多模态投影器使用的设备 {
        arg <DEVICE>
    }
    flag "--image --audio --video" help=图像、音频或视频文件路径（多模态模型使用） {
        arg <FILE>
    }
    flag --image-min-tokens help="每张图像的最小 token 数（仅用于动态分辨率的视觉模型）" {
        arg <N>
    }
    flag --image-max-tokens help="每张图像的最大 token 数（仅用于动态分辨率的视觉模型）" {
        arg <N>
    }
    flag --video-fps help=目标视频帧率（默认：4.0） {
        arg <N>
    }
    flag --video-timestamp-interval help=文本时间戳之间的间隔（毫秒，默认：5000） {
        arg <N>
    }
    flag --video-ffmpeg-dir help="ffmpeg 和 ffprobe 所在目录路径（默认：在 PATH 中搜索）" {
        arg <DIR>
    }
    flag --reasoning help="在对话中使用推理/思考（默认：auto）" {
        arg <ON_OFF_AUTO>
    }
    flag --reasoning-effort help=推理努力级别（默认：default） {
        arg <LEVEL>
    }
    flag --reasoning-budget help="思考的 token 预算（默认：-1，无限制）" {
        arg <N>
    }
    flag --reasoning-budget-message help=推理预算耗尽时注入的消息（默认：无） {
        arg <MESSAGE>
    }
    flag "--reasoning-preserve --no-reasoning-preserve" help=在完整历史中保留推理轨迹，而不仅仅是最后一条助手消息（默认：启用）
    flag "-p --prompt" help="起始生成的提示词；系统消息请用 -sys" {
        arg <PROMPT>
    }
    flag "-f --file" help=包含提示词的文件（默认：无） {
        arg <FNAME>
    }
    flag --binary-file help=包含提示词的二进制文件（默认：无） {
        arg <FNAME>
    }
    flag --server-base help=连接到此服务器而非启动新服务器 {
        arg <URL>
    }
    flag --verbose-prompt help=在生成前打印详细提示词（默认：false）
    flag "--display-prompt --no-display-prompt" help=是否在生成时打印提示词（默认：true）
    flag --color help="Colorize output to distinguish prompt and user input from generations ('on', 'off', or 'auto', default: 'auto') 'auto' enables colors when output is to a terminal" {
        arg <ON_OFF_AUTO>
    }
    flag --system-prompt help=用于模型的系统提示词 {
        arg <PROMPT>
    }
    flag "--show-timings --no-show-timings" help=是否在每次响应后显示计时信息（默认：true）
    flag "-r --reverse-prompt" help="在 PROMPT 处停止生成，交互模式下返回控制权" {
        arg <PROMPT>
    }
    flag --special help="启用特殊 token 输出（默认：false）"
    flag --single-turn help=仅运行单轮对话（默认：false）
    flag --multiline-input help="allows you to write or paste multiple lines without ending each in '\\'"
    flag "--warmup --no-warmup" help=是否用空运行进行预热（默认：启用）
    flag --chat-template-kwargs help="设置 JSON 模板解析器的额外参数" {
        arg <STRING>
    }
    flag "--jinja --no-jinja" help="是否使用 Jinja 模板引擎（默认：启用）"
    flag --log-disable help=禁用日志
    flag --log-file help=日志输出到文件 {
        arg <FNAME>
    }
    flag "--log-jsonl --no-log-jsonl" help="以 JSONL 格式记录日志（默认：禁用）"
    flag --log-colors help=设置彩色日志（默认：auto） {
        arg <ON_OFF_AUTO>
    }
    flag "-v --verbose --log-verbose" help=设置最大详细级别（用于调试）
    flag --offline help=离线模式：强制使用缓存，禁止网络访问
    flag "--verbosity --log-verbosity" help="详细级别阈值（默认：1，0=通用输出, 1=错误, 2=警告, 3=信息, 4=跟踪, 5=调试）" {
        arg <N>
    }
    flag "--log-prefix --no-log-prefix" help=在日志消息中启用前缀
    flag "--log-timestamps --no-log-timestamps" help=在日志消息中启用时间戳
    flag --log-prompts-dir help=将提示词记录到目录（仅用于调试，默认：禁用） {
        arg <PATH>
    }
    flag --threads-batch help="批处理和提示词处理的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag --cpu-range-batch help="批处理 CPU 亲和性范围。与 --cpu-mask-batch 互补" {
        arg <LO_HI>
    }
    flag --flash-attn help="设置闪存注意力使用（'on'、'off' 或 'auto'，默认：'auto'）" {
        arg <ON_OFF_AUTO>
    }
    flag "--kv-offload --no-kv-offload" help="是否启用 KV 缓存卸载（默认：启用）"
    flag "--direct-io --no-direct-io" help="已弃用，请使用 `--load-mode`：如果可用，使用 DirectIO"
    flag --numa help="尝试优化某些 NUMA 系统上的性能 - distribute：在所有节点上均匀分布执行 - isolate：仅在执行开始的节点上的 CPU 上生成线程 - numactl：使用 numactl 提供的 CPU 映射。如果之前未运行此命令，建议在使用此命令前丢弃系统页面缓存。参见 https://github.com/ggml-org/llama.cpp/issues/1437" {
        arg <TYPE>
    }
    flag --list-devices help=列出可用设备并退出
    flag --split-mode help="多 GPU 模型分割方式（默认：layer）" {
        arg <NONE_LAYER_ROW_TENSOR>
    }
    flag --fit-target help=每个设备的目标余量（MiB），默认：1024 {
        arg <MIB0_MIB1_MIB2>
    }
    flag --control-vector-scaled help=添加带缩放的控制向量（格式：FNAME:SCALE,...） {
        arg <FNAME_SCALE>
    }
    flag --docker-repo help="Docker Hub 模型仓库。repo 可选，默认为 ai/。quant 可选，默认为 :latest。示例：gemma3（默认：未使用）" {
        arg <REPO_MODEL_QUANT>
    }
    flag --hf-repo help="Hugging Face 模型仓库；quant 可选，不区分大小写，默认 Q4_K_M，如果 Q4_K_M 不存在则回退到仓库中的第一个文件。mmproj 如果可用也会自动下载。要禁用，请添加 --no-mmproj。示例：ggml-org/GLM-4.7-Flash-GGUF:Q4_K_M（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag "--spec-draft-type-k --cache-type-k-draft" help="草稿模型 K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--spec-draft-type-v --cache-type-v-draft" help="草稿模型 V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--sampler-seq --sampling-seq" help=采样器的简化序列（默认：edskypmxt） {
        arg <SEQUENCE>
    }
    flag "--spec-draft-hf --hf-repo-draft" help="与 --hf-repo 相同，但用于草稿模型（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag "--spec-draft-threads --threads-draft" help="草稿模型生成时的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag "--spec-draft-threads-batch --threads-batch-draft" help="草稿模型批处理的线程数（默认：与 --threads-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-cpu-mask --cpu-mask-draft" help="草稿模型 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag "--spec-draft-cpu-range --cpu-range-draft" help="草稿模型 CPU 亲和性范围" {
        arg <LO_HI>
    }
    flag "--spec-draft-cpu-strict --cpu-strict-draft" help="草稿模型严格 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <N>
    }
    flag "--spec-draft-prio --prio-draft" help="set draft process/thread priority : 0-normal, 1-medium, 2-high, 3-realtime (default: 0)" {
        arg <N>
    }
    flag "--spec-draft-cpu-mask-batch --cpu-mask-batch-draft" help="草稿模型 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag "--spec-draft-cpu-strict-batch --cpu-strict-batch-draft" help="草稿模型严格 CPU 放置（默认：与 --cpu-strict-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-prio-batch --prio-batch-draft" help="草稿模型进程/线程优先级（默认：0）" {
        arg <N>
    }
    flag "--spec-draft-poll-batch --poll-batch-draft" help="草稿模型轮询等待（默认：与 --poll-draft 相同）" {
        arg <N>
    }
    flag "--spec-draft-cpu-moe --cpu-moe-draft" help="将草稿模型的所有 MoE 权重保留在 CPU 中"
    flag "--spec-draft-n-cpu-moe --spec-draft-ncmoe --n-cpu-moe-draft" help="将草稿模型前 N 层的 MoE 权重保留在 CPU 中" {
        arg <N>
    }
    flag "--spec-draft-device --device-draft" help="草稿模型卸载设备列表（默认：跟随 --device）" {
        arg <DEV1_DEV2>
    }
    flag "--spec-draft-ngl --gpu-layers-draft --n-gpu-layers-draft" help=草稿模型显存层数（默认：auto） {
        arg <N>
    }
    flag "--spec-draft-model --model-draft" help=投机解码草稿模型（默认：未使用） {
        arg <FNAME>
    }
    flag "--ctx-checkpoints --swa-checkpoints" help=每个槽位要创建的最大上下文检查点数（默认：32） {
        arg <N>
    }
    flag "--context-shift --no-context-shift" help=是否在无限文本生成时使用上下文移位（默认：禁用）
    flag --system-prompt-file help="a file containing the system prompt (default: none)" {
        arg <FNAME>
    }
    flag "--mmproj-auto --no-mmproj --no-mmproj-auto" help=是否使用多模态投影器文件（默认：启用）
    flag "-o --output --output-file" help=输出文件（默认：空） {
        arg <FNAME>
    }
    flag --reasoning-format help="控制是否允许从响应中提取思考标签，以及以哪种格式返回；选项之一：- none：将思考保留在 `message.content` 中未解析 - deepseek：将思考放在 `message.reasoning_content` 中 - deepseek-legacy：在 `message.content` 中保留 `<think>` 标签，同时填充 `message.reasoning_content`（默认：auto）" {
        arg <FORMAT>
    }
    flag --chat-template help="设置自定义 Jinja 聊天模板（默认：从模型元数据获取的模板）。如果指定了 suffix/prefix，模板将被禁用。仅接受常用模板（除非在此标志之前设置了 --jinja）：内置模板列表： bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
        arg <JINJA_TEMPLATE>
    }
    flag --chat-template-file help="设置自定义 Jinja 聊天模板文件（默认：从模型元数据获取的模板）。如果指定了 suffix/prefix，模板将被禁用。仅接受常用模板（除非在此标志之前设置了 --jinja）：内置模板列表： bailing, bailing-think, bailing2, chatglm3, chatglm4, chatml, command-r, deepseek, deepseek-ocr, deepseek2, deepseek3, exaone-moe, exaone3, exaone4, falcon3, gemma, gigachat, glmedge, gpt-oss, granite, granite-4.0, granite-4.1, grok-2, hunyuan-dense, hunyuan-moe, hunyuan-vl, kimi-k2, llama2, llama2-sys, llama2-sys-bos, llama2-sys-strip, llama3, llama4, megrez, minicpm, mistral-v1, mistral-v3, mistral-v3-tekken, mistral-v7, mistral-v7-tekken, monarch, openchat, orion, pangu-embedded, phi3, phi4, rwkv-world, seed_oss, smolvlm, solar-open, vicuna, vicuna-orca, yandex, zephyr" {
        arg <JINJA_TEMPLATE_FILE>
    }
    flag "--skip-chat-parsing --no-skip-chat-parsing" help="强制使用纯内容解析器，即使指定了 Jinja 模板；模型将在内容部分输出所有内容，包括任何推理和/或工具调用（默认：禁用）"
    flag --simple-io help="使用基本 I/O 以获得更好的兼容性"
}
cmd bench help=模型性能基准测试 {
    flag --numa help="NUMA 模式（默认：禁用）" {
        arg <TYPE>
    }
    flag "-r --repetitions" help=每个测试的重复次数（默认：5） {
        arg <N>
    }
    flag --prio help="进程/线程优先级（默认：0）" {
        arg <N>
    }
    flag --delay help=每次测试间的延迟（默认：0） {
        arg <0_N>
    }
    flag --list-devices help=列出可用设备并退出
    flag --hf-repo help="Hugging Face 模型仓库；quant 可选，不区分大小写，默认 Q4_K_M，如果 Q4_K_M 不存在则回退到仓库中的第一个文件。示例：ggml-org/GLM-4.7-Flash-GGUF:Q4_K_M（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
        arg <TOKEN>
    }
    flag --rpc help="注册 RPC 设备（逗号分隔）" {
        arg <RPC_SERVERS>
    }
    flag "-o --output" help="stdout 输出格式（默认：md）" {
        arg <CSV_JSON_JSONL_MD_SQL>
    }
    flag --output-err help="stderr 输出格式（默认：无）" {
        arg <CSV_JSON_JSONL_MD_SQL>
    }
    flag "-v --verbose" help=详细输出
    flag --progress help=打印测试进度指示器
    flag --no-warmup help=跳过基准测试前的预热运行
    flag --fit-target help=适配设备内存的余量（MiB，默认：关闭） {
        arg <MIB>
    }
    flag --fit-ctx help="--fit-target 的最小上下文大小（默认：4096）" {
        arg <N>
    }
    flag --offline help=离线模式：强制使用缓存，禁止网络访问（默认：禁用）
    flag "-h --help" help=""
    flag "-m --model" help="(default: models/7B/ggml-model-q4_0.gguf)" {
        arg <FILENAME>
    }
    flag "-p --n-prompt" help="(default: 512)" {
        arg <N>
    }
    flag "-n --n-gen" help="(default: 128)" {
        arg <N>
    }
    flag "-d --n-depth" help="(default: 0)" {
        arg <N>
    }
    flag "-b --batch-size" help="(default: 2048)" {
        arg <N>
    }
    flag --ubatch-size help="(default: 512)" {
        arg <N>
    }
    flag --cache-type-k help="(default: f16)" {
        arg <T>
    }
    flag --cache-type-v help="(default: f16)" {
        arg <T>
    }
    flag "-t --threads" help="(default: 10)" {
        arg <N>
    }
    flag "-C --cpu-mask" help="(default: 0x0)" {
        arg <HEX_HEX>
    }
    flag --cpu-strict help="(default: 0)" {
        arg <N>
    }
    flag --poll help="(default: 50)" {
        arg <0_100>
    }
    flag --n-gpu-layers help="(default: -1)" {
        arg <N>
    }
    flag --n-cpu-moe help="(default: 0)" {
        arg <N>
    }
    flag --split-mode help="(default: layer)" {
        arg <NONE_LAYER_ROW_TENSOR>
    }
    flag --main-gpu help="(default: 0)" {
        arg <I>
    }
    flag --no-kv-offload help="(default: 0)" {
        arg <N>
    }
    flag --flash-attn help="(default: auto)" {
        arg <ON_OFF_AUTO>
    }
    flag --device help="(default: auto)" {
        arg <DEV0_DEV1>
    }
    flag --load-mode help="" {
        arg <AUTO_NONE_MMAP_MLOCK_MMAP_MLOCK_DIO>
    }
    flag --lazy-mode help="(default: auto)" {
        arg <ON_AUTO_OFF>
    }
    flag --mmap help="(DEPRECATED IN FAVOUR OF --load-mode)" {
        arg <N>
    }
    flag --direct-io help="(DEPRECATED IN FAVOUR OF --load-mode)" {
        arg <N>
    }
    flag --embeddings help="(default: 0)" {
        arg <N>
    }
    flag --tensor-split help="(default: 0)" {
        arg <TS0_TS1>
    }
    flag --override-tensor help="(default: disabled)" {
        arg <TENSOR>
    }
    flag --no-op-offload help="(default: 0)" {
        arg <N>
    }
    flag --no-host help="(default: 0)" {
        arg <N>
    }
}
cmd batched-bench help=批处理解码性能基准测试 {
    flag "-h --help --usage" help=打印用法并退出
    flag --version help=显示版本和构建信息
    flag --cache-list help=列出缓存中的模型
    flag --completion-bash help="输出可 source 的 bash 补全脚本"
    flag "-t --threads" help="(default: 10)" {
        arg <N>
    }
    flag --threads-batch help="批处理和提示词处理的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag "-C --cpu-mask" help="(default: 0x0)" {
        arg <M>
    }
    flag --cpu-range help="CPU 亲和性范围。与 --cpu-mask 互补" {
        arg <LO-HI>
    }
    flag --cpu-strict help="(default: 0)" {
        arg <0_1>
    }
    flag --prio help="进程/线程优先级（默认：0）" {
        arg <N>
    }
    flag --poll help="(default: 50)" {
        arg <0_100>
    }
    flag --cpu-mask-batch help="批处理 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag --cpu-range-batch help="批处理 CPU 亲和性范围。与 --cpu-mask-batch 互补" {
        arg <LO-HI>
    }
    flag --cpu-strict-batch help="使用严格的 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <0_1>
    }
    flag --prio-batch help="设置进程/线程优先级：0-normal, 1-medium, 2-high, 3-realtime（默认：0）" {
        arg <N>
    }
    flag --poll-batch help="使用轮询等待工作（默认：与 --poll 相同）" {
        arg <0_1>
    }
    flag "-c --ctx-size" help="提示词上下文大小（默认：0，0 = 从模型加载）" {
        arg <N>
    }
    flag "-n --predict --n-predict" help="预测的 token 数量（默认：-1，-1 = 无限）" {
        arg <N>
    }
    flag "-b --batch-size" help="(default: 2048)" {
        arg <N>
    }
    flag --ubatch-size help="(default: 512)" {
        arg <N>
    }
    flag --keep help="从初始提示词中保留的 token 数（默认：0，-1 = 全部）" {
        arg <N>
    }
    flag --swa-full help="使用完整大小的 SWA 缓存（默认：false）"
    flag --flash-attn help="(default: auto)" {
        arg <ON_OFF_AUTO>
    }
    flag "-p --prompt" help="起始生成的提示词；系统消息请用 -sys" {
        arg <PROMPT>
    }
    flag "--perf --no-perf" help="是否启用内部 libllama 性能计时（默认：false）"
    flag "-f --file" help=包含提示词的文件（默认：无） {
        arg <FNAME>
    }
    flag --binary-file help=包含提示词的二进制文件（默认：无） {
        arg <FNAME>
    }
    flag "-e --escape --no-escape" help="是否处理转义序列 (\\\\n, \\\\r, \\\\t, \\\\', \\\\\", \\\\\\\\) （默认：true）"
    flag --rope-scaling help="RoPE 频率缩放方法（默认：linear）" {
        arg <NONE_LINEAR_YARN>
    }
    flag --rope-scale help="RoPE 上下文缩放因子" {
        arg <N>
    }
    flag --rope-freq-base help="RoPE 基础频率（默认：从模型加载）" {
        arg <N>
    }
    flag --rope-freq-scale help="RoPE 频率缩放因子" {
        arg <N>
    }
    flag --yarn-orig-ctx help="YaRN：模型原始上下文大小（默认：0 = 模型训练上下文大小）" {
        arg <N>
    }
    flag --yarn-ext-factor help=YaRN：外推混合因子（默认：-1.00） {
        arg <N>
    }
    flag --yarn-attn-factor help="YaRN：缩放 sqrt(t) 或注意力幅度（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-slow help="YaRN：高修正维度或 alpha（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-fast help="YaRN：低修正维度或 beta（默认：-1.00）" {
        arg <N>
    }
    flag "--kv-offload --no-kv-offload" help="是否启用 KV 缓存卸载（默认：启用）"
    flag "--repack --no-repack" help=是否启用权重重打包（默认：启用）
    flag --no-host help="(default: 0)"
    flag --cache-type-k help="(default: f16)" {
        arg <TYPE>
    }
    flag --cache-type-v help="(default: f16)" {
        arg <TYPE>
    }
    flag --defrag-thold help="KV 缓存碎片整理阈值（已弃用）" {
        arg <N>
    }
    flag --parallel help=并行解码的序列数（默认：1） {
        arg <N>
    }
    flag --rpc help="注册 RPC 设备（逗号分隔）" {
        arg <SERVERS>
    }
    flag --mlock help="已弃用，请使用 `--load-mode`：强制系统将模型保留在内存中，而不是交换或压缩"
    flag "--mmap --no-mmap" help="已弃用，请使用 `--load-mode`：是否将模型映射到内存（如果禁用 mmap，加载较慢，但如果未使用 mlock，可能会减少页面换出）"
    flag "--direct-io --no-direct-io" help="已弃用，请使用 `--load-mode`：如果可用，使用 DirectIO"
    flag --load-mode help=模型加载模式（默认：auto） {
        arg <MODE>
    }
    flag --lazy-mode help="(default: auto)" {
        arg <MODE>
    }
    flag --numa help="NUMA 模式（默认：禁用）" {
        arg <TYPE>
    }
    flag --device help="(default: auto)" {
        arg <DEV1_DEV2_>
    }
    flag --list-devices help=列出可用设备并退出
    flag --override-tensor help="(default: disabled)" {
        arg "<TENSOR_NAME_PATTERN>=<BUFFER_TYPE>_>"
    }
    flag --cpu-moe help="将所有 MoE 权重保留在 CPU 中"
    flag --n-cpu-moe help="(default: 0)" {
        arg <N>
    }
    flag --n-cpu-ffn help="将前 N 层的密集 FFN 权重保留在 CPU 中" {
        arg <N>
    }
    flag "--gpu-layers --n-gpu-layers" help="存储在显存中的最大层数，精确数字、'auto' 或 'all'（默认：auto）" {
        arg <N>
    }
    flag --split-mode help="(default: layer)" {
        arg <NONE_LAYER_ROW_TENSOR>
    }
    flag --tensor-split help="(default: 0)" {
        arg <N0_N1_N2_>
    }
    flag --main-gpu help="(default: 0)" {
        arg <INDEX>
    }
    flag --fit help=是否自动调整参数以适应显存（默认：on） {
        arg <ON_OFF>
    }
    flag --fit-target help=适配设备内存的余量（MiB，默认：关闭） {
        arg <MIB0_MIB1_MIB2_>
    }
    flag --fit-ctx help="--fit-target 的最小上下文大小（默认：4096）" {
        arg <N>
    }
    flag --check-tensors help=检查模型张量数据中的无效值（默认：false）
    flag --override-kv help=按键覆盖模型元数据的高级选项 {
        arg "<KEY=TYPE:VALUE_>"
    }
    flag "--op-offload --no-op-offload" help=是否将主机张量操作卸载到设备（默认：true）
    flag --lora help="LoRA 适配器路径（逗号分隔可加载多个）" {
        arg <FNAME>
    }
    flag --lora-scaled help="带缩放系数的 LoRA 适配器路径（格式：FNAME:SCALE,...）" {
        arg <FNAME:SCALE_>
    }
    flag --control-vector help=添加控制向量（逗号分隔可添加多个） {
        arg <FNAME>
    }
    flag --control-vector-scaled help=添加带缩放的控制向量（格式：FNAME:SCALE,...） {
        arg <FNAME:SCALE_>
    }
    flag --control-vector-layer-range help=应用控制向量的层范围（含首尾） {
        arg <START_END>
    }
    flag "-m --model" help="(default: models/7B/ggml-model-q4_0.gguf)" {
        arg <FNAME>
    }
    flag --model-url help="模型下载 URL（默认：未使用）" {
        arg <MODEL_URL>
    }
    flag --docker-repo help="Docker Hub 模型仓库。repo 可选，默认为 ai/。quant 可选，默认为 :latest。示例：gemma3（默认：未使用）" {
        arg "<REPO>_]<MODEL>[:QUANT>"
    }
    flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
        arg <TOKEN>
    }
    flag --log-disable help=禁用日志
    flag --log-file help=日志输出到文件 {
        arg <FNAME>
    }
    flag "--log-jsonl --no-log-jsonl" help="以 JSONL 格式记录日志（默认：禁用）"
    flag --log-colors help=设置彩色日志（默认：auto） {
        arg <ON_OFF_AUTO>
    }
    flag "-v --verbose --log-verbose" help=设置最大详细级别（用于调试）
    flag --offline help=离线模式：强制使用缓存，禁止网络访问（默认：禁用）
    flag "--verbosity --log-verbosity" help="详细级别阈值（默认：1，0=通用输出, 1=错误, 2=警告, 3=信息, 4=跟踪, 5=调试）" {
        arg <N>
    }
    flag "--log-prefix --no-log-prefix" help=在日志消息中启用前缀
    flag "--log-timestamps --no-log-timestamps" help=在日志消息中启用时间戳
    flag "--spec-draft-type-k --cache-type-k-draft" help="草稿模型 K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--spec-draft-type-v --cache-type-v-draft" help="草稿模型 V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --samplers help=按顺序使用的采样器，用分号分隔 {
        arg <SAMPLERS>
    }
    flag "-s --seed" help="随机数种子（默认：-1，-1 为随机）" {
        arg <SEED>
    }
    flag "--sampler-seq --sampling-seq" help=采样器的简化序列（默认：edskypmxt） {
        arg <SEQUENCE>
    }
    flag --ignore-eos help="忽略流结束 token 并继续生成"
    flag "--temp --temperature" help=温度（默认：0.80） {
        arg <N>
    }
    flag --top-k help="top-k 采样（默认：40，0 = 禁用）" {
        arg <N>
    }
    flag --top-p help="top-p 采样（默认：0.95，1.0 = 禁用）" {
        arg <N>
    }
    flag --min-p help="min-p 采样（默认：0.05，0.0 = 禁用）" {
        arg <N>
    }
    flag "--top-nsigma --top-n-sigma" help="top-n-sigma 采样（默认：-1.00，禁用）" {
        arg <N>
    }
    flag --xtc-probability help="XTC 概率（默认：0.00，禁用）" {
        arg <N>
    }
    flag --xtc-threshold help="XTC 阈值（默认：0.10，禁用）" {
        arg <N>
    }
    flag "--typical --typical-p" help=局部典型采样（默认：1.00，禁用） {
        arg <N>
    }
    flag --repeat-last-n help="考虑惩罚的最近 token 数（默认：64，禁用）" {
        arg <N>
    }
    flag --repeat-penalty help="重复 token 惩罚（默认：1.00，禁用）" {
        arg <N>
    }
    flag --presence-penalty help=重复存在惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --frequency-penalty help=重复频率惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --dry-multiplier help="DRY 采样乘数（默认：0.00，禁用）" {
        arg <N>
    }
    flag --dry-base help="DRY 采样基础值（默认：1.75）" {
        arg <N>
    }
    flag --dry-allowed-length help="DRY 采样允许长度（默认：2）" {
        arg <N>
    }
    flag --dry-penalty-last-n help="最近 n 个 token 的 DRY 惩罚（默认：64，禁用）" {
        arg <N>
    }
    flag --dry-sequence-breaker help="添加 DRY 采样的序列中断器" {
        arg <STRING>
    }
    flag --adaptive-target help="adaptive-p：选择接近此概率的令牌（有效范围 0.0 到 1.0；负数 = 禁用）（默认：-1.00）" {
        arg <N>
    }
    flag --adaptive-decay help="adaptive-p：目标适应的衰减率。值越低反应越快，值越高越稳定。（有效范围 0.0 到 0.99）（默认：0.90）" {
        arg <N>
    }
    flag --dynatemp-range help=动态温度范围（默认：0.00，禁用） {
        arg <N>
    }
    flag --dynatemp-exp help=动态温度指数（默认：1.00） {
        arg <N>
    }
    flag --mirostat help="使用 Mirostat 采样（默认：0，禁用）" {
        arg <N>
    }
    flag --mirostat-lr help="Mirostat 学习率（默认：0.10）" {
        arg <N>
    }
    flag --mirostat-ent help="Mirostat 目标熵（默认：5.00）" {
        arg <N>
    }
    flag "-l --logit-bias" help="修改令牌在完成中出现的可能性，例如 `--logit-bias 15043+1` 增加令牌 ' Hello' 的可能性，或 `--logit-bias 15043-1` 降低令牌 ' Hello' 的可能性" {
        arg "<TOKEN_ID(+_-)BIAS>"
    }
    flag --grammar help="用于约束生成的 BNF 风格语法" {
        arg <GRAMMAR>
    }
    flag --grammar-file help=语法文件路径 {
        arg <FNAME>
    }
    flag "-j --json-schema" help="用于约束生成的 JSON Schema" {
        arg <SCHEMA>
    }
    flag --json-schema-file help="包含 JSON Schema 的文件" {
        arg <FILE>
    }
    flag --backend-sampling help=启用后端采样（实验性，默认：禁用）
    flag "--kv-unified --no-kv-unified" help="使用跨所有序列共享的统一 KV 缓冲区"
    flag --output-format help="imatrix 文件输出格式（默认：gguf）" {
        arg <MD_JSONL>
    }
}
cmd fit-params help=计算模型适配设备内存的参数 {
    flag "-h --help --usage" help=打印用法并退出
    flag --version help=显示版本和构建信息
    flag --cache-list help=列出缓存中的模型
    flag --completion-bash help="输出可 source 的 bash 补全脚本"
    flag "-t --threads" help="(default: 10)" {
        arg <N>
    }
    flag --threads-batch help="批处理和提示词处理的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag "-C --cpu-mask" help="(default: 0x0)" {
        arg <M>
    }
    flag --cpu-range help="CPU 亲和性范围。与 --cpu-mask 互补" {
        arg <LO-HI>
    }
    flag --cpu-strict help="(default: 0)" {
        arg <0_1>
    }
    flag --prio help="进程/线程优先级（默认：0）" {
        arg <N>
    }
    flag --poll help="(default: 50)" {
        arg <0_100>
    }
    flag --cpu-mask-batch help="批处理 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag --cpu-range-batch help="批处理 CPU 亲和性范围。与 --cpu-mask-batch 互补" {
        arg <LO-HI>
    }
    flag --cpu-strict-batch help="使用严格的 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <0_1>
    }
    flag --prio-batch help="设置进程/线程优先级：0-normal, 1-medium, 2-high, 3-realtime（默认：0）" {
        arg <N>
    }
    flag --poll-batch help="使用轮询等待工作（默认：与 --poll 相同）" {
        arg <0_1>
    }
    flag "-c --ctx-size" help="提示词上下文大小（默认：0，0 = 从模型加载）" {
        arg <N>
    }
    flag "-n --predict --n-predict" help="预测的 token 数量（默认：-1，-1 = 无限）" {
        arg <N>
    }
    flag "-b --batch-size" help="(default: 2048)" {
        arg <N>
    }
    flag --ubatch-size help="(default: 512)" {
        arg <N>
    }
    flag --keep help="从初始提示词中保留的 token 数（默认：0，-1 = 全部）" {
        arg <N>
    }
    flag --swa-full help="使用完整大小的 SWA 缓存（默认：false）"
    flag --flash-attn help="(default: auto)" {
        arg <ON_OFF_AUTO>
    }
    flag "-p --prompt" help="起始生成的提示词；系统消息请用 -sys" {
        arg <PROMPT>
    }
    flag "--perf --no-perf" help="是否启用内部 libllama 性能计时（默认：false）"
    flag "-f --file" help=包含提示词的文件（默认：无） {
        arg <FNAME>
    }
    flag --binary-file help=包含提示词的二进制文件（默认：无） {
        arg <FNAME>
    }
    flag "-e --escape --no-escape" help="是否处理转义序列 (\\\\n, \\\\r, \\\\t, \\\\', \\\\\", \\\\\\\\) （默认：true）"
    flag --rope-scaling help="RoPE 频率缩放方法（默认：linear）" {
        arg <NONE_LINEAR_YARN>
    }
    flag --rope-scale help="RoPE 上下文缩放因子" {
        arg <N>
    }
    flag --rope-freq-base help="RoPE 基础频率（默认：从模型加载）" {
        arg <N>
    }
    flag --rope-freq-scale help="RoPE 频率缩放因子" {
        arg <N>
    }
    flag --yarn-orig-ctx help="YaRN：模型原始上下文大小（默认：0 = 模型训练上下文大小）" {
        arg <N>
    }
    flag --yarn-ext-factor help=YaRN：外推混合因子（默认：-1.00） {
        arg <N>
    }
    flag --yarn-attn-factor help="YaRN：缩放 sqrt(t) 或注意力幅度（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-slow help="YaRN：高修正维度或 alpha（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-fast help="YaRN：低修正维度或 beta（默认：-1.00）" {
        arg <N>
    }
    flag "--kv-offload --no-kv-offload" help="是否启用 KV 缓存卸载（默认：启用）"
    flag "--repack --no-repack" help=是否启用权重重打包（默认：启用）
    flag --no-host help="(default: 0)"
    flag --cache-type-k help="(default: f16)" {
        arg <TYPE>
    }
    flag --cache-type-v help="(default: f16)" {
        arg <TYPE>
    }
    flag --defrag-thold help="KV 缓存碎片整理阈值（已弃用）" {
        arg <N>
    }
    flag --parallel help=并行解码的序列数（默认：1） {
        arg <N>
    }
    flag --rpc help="注册 RPC 设备（逗号分隔）" {
        arg <SERVERS>
    }
    flag --mlock help="已弃用，请使用 `--load-mode`：强制系统将模型保留在内存中，而不是交换或压缩"
    flag "--mmap --no-mmap" help="已弃用，请使用 `--load-mode`：是否将模型映射到内存（如果禁用 mmap，加载较慢，但如果未使用 mlock，可能会减少页面换出）"
    flag "--direct-io --no-direct-io" help="已弃用，请使用 `--load-mode`：如果可用，使用 DirectIO"
    flag --load-mode help=模型加载模式（默认：auto） {
        arg <MODE>
    }
    flag --lazy-mode help="(default: auto)" {
        arg <MODE>
    }
    flag --numa help="NUMA 模式（默认：禁用）" {
        arg <TYPE>
    }
    flag --device help="(default: auto)" {
        arg <DEV1_DEV2_>
    }
    flag --list-devices help=列出可用设备并退出
    flag --override-tensor help="(default: disabled)" {
        arg "<TENSOR_NAME_PATTERN>=<BUFFER_TYPE>_>"
    }
    flag --cpu-moe help="将所有 MoE 权重保留在 CPU 中"
    flag --n-cpu-moe help="(default: 0)" {
        arg <N>
    }
    flag --n-cpu-ffn help="将前 N 层的密集 FFN 权重保留在 CPU 中" {
        arg <N>
    }
    flag "--gpu-layers --n-gpu-layers" help="存储在显存中的最大层数，精确数字、'auto' 或 'all'（默认：auto）" {
        arg <N>
    }
    flag --split-mode help="(default: layer)" {
        arg <NONE_LAYER_ROW_TENSOR>
    }
    flag --tensor-split help="(default: 0)" {
        arg <N0_N1_N2_>
    }
    flag --main-gpu help="(default: 0)" {
        arg <INDEX>
    }
    flag --fit help=是否自动调整参数以适应显存（默认：on） {
        arg <ON_OFF>
    }
    flag --fit-target help=适配设备内存的余量（MiB，默认：关闭） {
        arg <MIB0_MIB1_MIB2_>
    }
    flag --fit-ctx help="--fit-target 的最小上下文大小（默认：4096）" {
        arg <N>
    }
    flag --check-tensors help=检查模型张量数据中的无效值（默认：false）
    flag --override-kv help=按键覆盖模型元数据的高级选项 {
        arg "<KEY=TYPE:VALUE_>"
    }
    flag "--op-offload --no-op-offload" help=是否将主机张量操作卸载到设备（默认：true）
    flag --lora help="LoRA 适配器路径（逗号分隔可加载多个）" {
        arg <FNAME>
    }
    flag --lora-scaled help="带缩放系数的 LoRA 适配器路径（格式：FNAME:SCALE,...）" {
        arg <FNAME:SCALE_>
    }
    flag --control-vector help=添加控制向量（逗号分隔可添加多个） {
        arg <FNAME>
    }
    flag --control-vector-scaled help=添加带缩放的控制向量（格式：FNAME:SCALE,...） {
        arg <FNAME:SCALE_>
    }
    flag --control-vector-layer-range help=应用控制向量的层范围（含首尾） {
        arg <START_END>
    }
    flag "-m --model" help="(default: models/7B/ggml-model-q4_0.gguf)" {
        arg <FNAME>
    }
    flag --model-url help="模型下载 URL（默认：未使用）" {
        arg <MODEL_URL>
    }
    flag --docker-repo help="Docker Hub 模型仓库。repo 可选，默认为 ai/。quant 可选，默认为 :latest。示例：gemma3（默认：未使用）" {
        arg "<REPO>_]<MODEL>[:QUANT>"
    }
    flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
        arg <TOKEN>
    }
    flag --log-disable help=禁用日志
    flag --log-file help=日志输出到文件 {
        arg <FNAME>
    }
    flag "--log-jsonl --no-log-jsonl" help="以 JSONL 格式记录日志（默认：禁用）"
    flag --log-colors help=设置彩色日志（默认：auto） {
        arg <ON_OFF_AUTO>
    }
    flag "-v --verbose --log-verbose" help=设置最大详细级别（用于调试）
    flag --offline help=离线模式：强制使用缓存，禁止网络访问（默认：禁用）
    flag "--verbosity --log-verbosity" help="详细级别阈值（默认：1，0=通用输出, 1=错误, 2=警告, 3=信息, 4=跟踪, 5=调试）" {
        arg <N>
    }
    flag "--log-prefix --no-log-prefix" help=在日志消息中启用前缀
    flag "--log-timestamps --no-log-timestamps" help=在日志消息中启用时间戳
    flag "--spec-draft-type-k --cache-type-k-draft" help="草稿模型 K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--spec-draft-type-v --cache-type-v-draft" help="草稿模型 V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --samplers help=按顺序使用的采样器，用分号分隔 {
        arg <SAMPLERS>
    }
    flag "-s --seed" help="随机数种子（默认：-1，-1 为随机）" {
        arg <SEED>
    }
    flag "--sampler-seq --sampling-seq" help=采样器的简化序列（默认：edskypmxt） {
        arg <SEQUENCE>
    }
    flag --ignore-eos help="忽略流结束 token 并继续生成"
    flag "--temp --temperature" help=温度（默认：0.80） {
        arg <N>
    }
    flag --top-k help="top-k 采样（默认：40，0 = 禁用）" {
        arg <N>
    }
    flag --top-p help="top-p 采样（默认：0.95，1.0 = 禁用）" {
        arg <N>
    }
    flag --min-p help="min-p 采样（默认：0.05，0.0 = 禁用）" {
        arg <N>
    }
    flag "--top-nsigma --top-n-sigma" help="top-n-sigma 采样（默认：-1.00，禁用）" {
        arg <N>
    }
    flag --xtc-probability help="XTC 概率（默认：0.00，禁用）" {
        arg <N>
    }
    flag --xtc-threshold help="XTC 阈值（默认：0.10，禁用）" {
        arg <N>
    }
    flag "--typical --typical-p" help=局部典型采样（默认：1.00，禁用） {
        arg <N>
    }
    flag --repeat-last-n help="考虑惩罚的最近 token 数（默认：64，禁用）" {
        arg <N>
    }
    flag --repeat-penalty help="重复 token 惩罚（默认：1.00，禁用）" {
        arg <N>
    }
    flag --presence-penalty help=重复存在惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --frequency-penalty help=重复频率惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --dry-multiplier help="DRY 采样乘数（默认：0.00，禁用）" {
        arg <N>
    }
    flag --dry-base help="DRY 采样基础值（默认：1.75）" {
        arg <N>
    }
    flag --dry-allowed-length help="DRY 采样允许长度（默认：2）" {
        arg <N>
    }
    flag --dry-penalty-last-n help="最近 n 个 token 的 DRY 惩罚（默认：64，禁用）" {
        arg <N>
    }
    flag --dry-sequence-breaker help="添加 DRY 采样的序列中断器" {
        arg <STRING>
    }
    flag --adaptive-target help="adaptive-p：选择接近此概率的令牌（有效范围 0.0 到 1.0；负数 = 禁用）（默认：-1.00）" {
        arg <N>
    }
    flag --adaptive-decay help="adaptive-p：目标适应的衰减率。值越低反应越快，值越高越稳定。（有效范围 0.0 到 0.99）（默认：0.90）" {
        arg <N>
    }
    flag --dynatemp-range help=动态温度范围（默认：0.00，禁用） {
        arg <N>
    }
    flag --dynatemp-exp help=动态温度指数（默认：1.00） {
        arg <N>
    }
    flag --mirostat help="使用 Mirostat 采样（默认：0，禁用）" {
        arg <N>
    }
    flag --mirostat-lr help="Mirostat 学习率（默认：0.10）" {
        arg <N>
    }
    flag --mirostat-ent help="Mirostat 目标熵（默认：5.00）" {
        arg <N>
    }
    flag "-l --logit-bias" help="修改令牌在完成中出现的可能性，例如 `--logit-bias 15043+1` 增加令牌 ' Hello' 的可能性，或 `--logit-bias 15043-1` 降低令牌 ' Hello' 的可能性" {
        arg "<TOKEN_ID(+_-)BIAS>"
    }
    flag --grammar help="用于约束生成的 BNF 风格语法" {
        arg <GRAMMAR>
    }
    flag --grammar-file help=语法文件路径 {
        arg <FNAME>
    }
    flag "-j --json-schema" help="用于约束生成的 JSON Schema" {
        arg <SCHEMA>
    }
    flag --json-schema-file help="包含 JSON Schema 的文件" {
        arg <FILE>
    }
    flag --backend-sampling help=启用后端采样（实验性，默认：禁用）
    flag --fit-print help="打印预估所需内存（'on' 或 'off'，默认：'off'）" {
        arg <ON_OFF>
    }
}
cmd quantize help=量化模型 {
    flag --allow-requantize help=""
    flag --leave-output-tensor help=""
    flag --pure help=""
    flag --imatrix help="" {
        arg <FILE_NAME>
    }
    flag --include-weights help="" {
        arg <TENSOR_NAME>
    }
    flag --exclude-weights help="" {
        arg <TENSOR_NAME>
    }
    flag --output-tensor-type help="" {
        arg <GGML_TYPE>
    }
    flag --token-embedding-type help="" {
        arg <GGML_TYPE>
    }
    flag --tensor-type help="" {
        arg <TENSOR_NAME_GGML_TYPE>
    }
    flag --tensor-type-file help="" {
        arg <TENSOR_TYPES_TXT>
    }
    flag --prune-layers help="" {
        arg <L0_L1_L2>
    }
    flag --keep-split help=""
    flag --override-kv help="" {
        arg <KEY_TYPE_VALUE>
    }
    flag --dry-run help=""
    flag --max-buffer-size help="" {
        arg <MIB>
    }
}
cmd perplexity help="计算模型困惑度与 KL 散度" {
    flag "-h --help --usage" help=打印用法并退出
    flag --version help=显示版本和构建信息
    flag --cache-list help=列出缓存中的模型
    flag --completion-bash help="输出可 source 的 bash 补全脚本"
    flag "-t --threads" help="生成时使用的 CPU 线程数（默认：-1）" {
        arg <N>
    }
    flag "-C --cpu-mask" help="CPU 亲和性掩码：任意长度十六进制。与 cpu-range 互补（默认：\"\")" {
        arg <M>
    }
    flag --cpu-range help="CPU 亲和性范围。与 --cpu-mask 互补" {
        arg <LO_HI>
    }
    flag --cpu-strict help="使用严格的 CPU 放置（默认：0）" {
        arg <N>
    }
    flag --prio help="设置进程/线程优先级：low(-1), normal(0), medium(1), high(2), realtime(3)（默认：0）" {
        arg <N>
    }
    flag --poll help="使用轮询级别等待工作（0 - 不轮询，默认：50）" {
        arg <0_100>
    }
    flag --cpu-mask-batch help="批处理 CPU 亲和性掩码（默认：与 --cpu-mask 相同）" {
        arg <M>
    }
    flag --cpu-strict-batch help="使用严格的 CPU 放置（默认：与 --cpu-strict 相同）" {
        arg <N>
    }
    flag --prio-batch help="设置进程/线程优先级：0-normal, 1-medium, 2-high, 3-realtime（默认：0）" {
        arg <N>
    }
    flag --poll-batch help="使用轮询等待工作（默认：与 --poll 相同）" {
        arg <N>
    }
    flag "-c --ctx-size" help="提示词上下文大小（默认：512，0 = 从模型加载）" {
        arg <N>
    }
    flag "-n --predict --n-predict" help="预测的 token 数量（默认：-1，-1 = 无限）" {
        arg <N>
    }
    flag "-b --batch-size" help=逻辑最大批处理大小（默认：2048） {
        arg <N>
    }
    flag --ubatch-size help=物理最大批处理大小（默认：512） {
        arg <N>
    }
    flag --keep help="从初始提示词中保留的 token 数（默认：0，-1 = 全部）" {
        arg <N>
    }
    flag "--perf --no-perf" help="是否启用内部 libllama 性能计时（默认：false）"
    flag "-e --escape --no-escape" help="是否处理转义序列 (\\n, \\r, \\t, \\', \\\", \\\\) （默认：false）"
    flag --parallel help=并行解码的序列数（默认：1） {
        arg <N>
    }
    flag --override-tensor help=覆盖张量缓冲区类型 {
        arg <TENSOR>
    }
    flag --cpu-moe help="将所有 MoE 权重保留在 CPU 中"
    flag --n-cpu-moe help="将前 N 层的 MoE 权重保留在 CPU 中" {
        arg <N>
    }
    flag --n-cpu-ffn help="将前 N 层的密集 FFN 权重保留在 CPU 中" {
        arg <N>
    }
    flag --check-tensors help=检查模型张量数据中的无效值（默认：false）
    flag --override-kv help=按键覆盖模型元数据的高级选项 {
        arg <KEY_TYPE_VALUE>
    }
    flag "--op-offload --no-op-offload" help=是否将主机张量操作卸载到设备（默认：true）
    flag "-m --model" help=要加载的模型路径 {
        arg <FNAME>
    }
    flag --model-url help="模型下载 URL（默认：未使用）" {
        arg <MODEL_URL>
    }
    flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
        arg <FILE>
    }
    flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
        arg <TOKEN>
    }
    flag --rope-scaling help="RoPE 频率缩放方法（默认：linear）" {
        arg <NONE_LINEAR_YARN>
    }
    flag --rope-scale help="RoPE 上下文缩放因子" {
        arg <N>
    }
    flag --rope-freq-base help="RoPE 基础频率（默认：从模型加载）" {
        arg <N>
    }
    flag --rope-freq-scale help="RoPE 频率缩放因子" {
        arg <N>
    }
    flag --yarn-orig-ctx help="YaRN：模型原始上下文大小（默认：0 = 模型训练上下文大小）" {
        arg <N>
    }
    flag --yarn-ext-factor help=YaRN：外推混合因子（默认：-1.00） {
        arg <N>
    }
    flag --yarn-attn-factor help="YaRN：缩放 sqrt(t) 或注意力幅度（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-slow help="YaRN：高修正维度或 alpha（默认：-1.00）" {
        arg <N>
    }
    flag --yarn-beta-fast help="YaRN：低修正维度或 beta（默认：-1.00）" {
        arg <N>
    }
    flag "--repack --no-repack" help=是否启用权重重打包（默认：启用）
    flag --no-host help=绕过主机缓冲区以允许使用额外缓冲区
    flag --cache-type-k help="K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --cache-type-v help="V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag --defrag-thold help="KV 缓存碎片整理阈值（已弃用）" {
        arg <N>
    }
    flag --swa-full help="使用完整大小的 SWA 缓存（默认：false）"
    flag --rpc help="RPC 服务器列表，逗号分隔（host:port）" {
        arg <SERVERS>
    }
    flag --mlock help="已弃用，请使用 `--load-mode`：强制系统将模型保留在内存中，而不是交换或压缩"
    flag "--mmap --no-mmap" help="已弃用，请使用 `--load-mode`：是否将模型映射到内存（如果禁用 mmap，加载较慢，但如果未使用 mlock，可能会减少页面换出）"
    flag --load-mode help=模型加载模式（默认：auto） {
        arg <MODE>
    }
    flag --lazy-mode help=按需读取特定张量（默认：auto） {
        arg <MODE>
    }
    flag --device help="用于卸载的设备列表，逗号分隔（none = 不卸载）" {
        arg <DEV1_DEV2>
    }
    flag "--gpu-layers --n-gpu-layers" help="存储在显存中的最大层数，精确数字、'auto' 或 'all'（默认：auto）" {
        arg <N>
    }
    flag --tensor-split help="各 GPU 卸载比例，逗号分隔，如 3,1" {
        arg <N0_N1_N2>
    }
    flag --main-gpu help="用于模型的 GPU（默认：0）" {
        arg <INDEX>
    }
    flag --fit help=是否自动调整参数以适应显存（默认：on） {
        arg <ON_OFF>
    }
    flag --fit-ctx help="可由 --fit 设置的最小上下文大小（默认：4096）" {
        arg <N>
    }
    flag --lora help="LoRA 适配器路径（逗号分隔可加载多个）" {
        arg <FNAME>
    }
    flag --lora-scaled help="带缩放系数的 LoRA 适配器路径（格式：FNAME:SCALE,...）" {
        arg <FNAME_SCALE>
    }
    flag --control-vector help=添加控制向量（逗号分隔可添加多个） {
        arg <FNAME>
    }
    flag --control-vector-layer-range help=应用控制向量的层范围（含首尾） {
        arg <START>
    }
    flag --samplers help=按顺序使用的采样器，用分号分隔 {
        arg <SAMPLERS>
    }
    flag "-s --seed" help="随机数种子（默认：-1，-1 为随机）" {
        arg <SEED>
    }
    flag --ignore-eos help="忽略流结束 token 并继续生成"
    flag "--temp --temperature" help=温度（默认：0.80） {
        arg <N>
    }
    flag --top-k help="top-k 采样（默认：40，0 = 禁用）" {
        arg <N>
    }
    flag --top-p help="top-p 采样（默认：0.95，1.0 = 禁用）" {
        arg <N>
    }
    flag --min-p help="min-p 采样（默认：0.05，0.0 = 禁用）" {
        arg <N>
    }
    flag "--top-nsigma --top-n-sigma" help="top-n-sigma 采样（默认：-1.00，禁用）" {
        arg <N>
    }
    flag --xtc-probability help="XTC 概率（默认：0.00，禁用）" {
        arg <N>
    }
    flag --xtc-threshold help="XTC 阈值（默认：0.10，禁用）" {
        arg <N>
    }
    flag "--typical --typical-p" help=局部典型采样（默认：1.00，禁用） {
        arg <N>
    }
    flag --repeat-last-n help="考虑惩罚的最近 token 数（默认：64，禁用）" {
        arg <N>
    }
    flag --repeat-penalty help="重复 token 惩罚（默认：1.00，禁用）" {
        arg <N>
    }
    flag --presence-penalty help=重复存在惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --frequency-penalty help=重复频率惩罚（默认：0.00，禁用） {
        arg <N>
    }
    flag --dry-multiplier help="DRY 采样乘数（默认：0.00，禁用）" {
        arg <N>
    }
    flag --dry-base help="DRY 采样基础值（默认：1.75）" {
        arg <N>
    }
    flag --dry-allowed-length help="DRY 采样允许长度（默认：2）" {
        arg <N>
    }
    flag --dry-penalty-last-n help="最近 n 个 token 的 DRY 惩罚（默认：64，禁用）" {
        arg <N>
    }
    flag --dry-sequence-breaker help="添加 DRY 采样的序列中断器" {
        arg <STRING>
    }
    flag --adaptive-target help="adaptive-p：选择接近此概率的令牌（有效范围 0.0 到 1.0；负数 = 禁用）（默认：-1.00）" {
        arg <N>
    }
    flag --adaptive-decay help="adaptive-p：目标适应的衰减率。值越低反应越快，值越高越稳定。（有效范围 0.0 到 0.99）（默认：0.90）" {
        arg <N>
    }
    flag --dynatemp-range help=动态温度范围（默认：0.00，禁用） {
        arg <N>
    }
    flag --dynatemp-exp help=动态温度指数（默认：1.00） {
        arg <N>
    }
    flag --mirostat help="使用 Mirostat 采样（默认：0，禁用）" {
        arg <N>
    }
    flag --mirostat-lr help="Mirostat 学习率（默认：0.10）" {
        arg <N>
    }
    flag --mirostat-ent help="Mirostat 目标熵（默认：5.00）" {
        arg <N>
    }
    flag "-l --logit-bias" help="修改令牌在完成中出现的可能性，例如 `--logit-bias 15043+1` 增加令牌 ' Hello' 的可能性，或 `--logit-bias 15043-1` 降低令牌 ' Hello' 的可能性" {
        arg <TOKEN_ID_BIAS>
    }
    flag --grammar help="用于约束生成的 BNF 风格语法" {
        arg <GRAMMAR>
    }
    flag --grammar-file help=语法文件路径 {
        arg <FNAME>
    }
    flag "-j --json-schema" help="用于约束生成的 JSON Schema" {
        arg <SCHEMA>
    }
    flag --json-schema-file help="包含 JSON Schema 的文件" {
        arg <FILE>
    }
    flag --backend-sampling help=启用后端采样（实验性，默认：禁用）
    flag "-p --prompt" help="起始生成的提示词；系统消息请用 -sys" {
        arg <PROMPT>
    }
    flag "-f --file" help=包含提示词的文件（默认：无） {
        arg <FNAME>
    }
    flag --binary-file help=包含提示词的二进制文件（默认：无） {
        arg <FNAME>
    }
    flag "--warmup --no-warmup" help=是否用空运行进行预热（默认：启用）
    flag --chunks help=处理的最大块数（默认：-1，全部） {
        arg <N>
    }
    flag --hellaswag help="计算 HellaSwag 分数"
    flag --hellaswag-tasks help="计算 HellaSwag 分数的任务数（默认：400）" {
        arg <N>
    }
    flag --winogrande help="计算 Winogrande 分数"
    flag --winogrande-tasks help="计算 Winogrande 分数的任务数" {
        arg <N>
    }
    flag --multiple-choice help=计算多选题分数
    flag --multiple-choice-tasks help=计算多选题分数的任务数（默认：0） {
        arg <N>
    }
    flag --kl-divergence help="计算 KL 散度"
    flag --ppl-stride help=困惑度计算步幅（默认：0） {
        arg <N>
    }
    flag --ppl-output-type help=困惑度计算输出类型（默认：0） {
        arg <N>
    }
    flag --log-disable help=禁用日志
    flag --log-file help=日志输出到文件 {
        arg <FNAME>
    }
    flag "--log-jsonl --no-log-jsonl" help="以 JSONL 格式记录日志（默认：禁用）"
    flag --log-colors help=设置彩色日志（默认：auto） {
        arg <ON_OFF_AUTO>
    }
    flag "-v --verbose --log-verbose" help=设置最大详细级别（用于调试）
    flag --offline help=离线模式：强制使用缓存，禁止网络访问
    flag "--verbosity --log-verbosity" help="详细级别阈值（默认：3，0=通用输出, 1=错误, 2=警告, 3=信息, 4=跟踪, 5=调试）" {
        arg <N>
    }
    flag "--log-prefix --no-log-prefix" help=在日志消息中启用前缀
    flag "--log-timestamps --no-log-timestamps" help=在日志消息中启用时间戳
    flag --threads-batch help="批处理和提示词处理的线程数（默认：与 --threads 相同）" {
        arg <N>
    }
    flag --cpu-range-batch help="批处理 CPU 亲和性范围。与 --cpu-mask-batch 互补" {
        arg <LO_HI>
    }
    flag --flash-attn help="设置闪存注意力使用（'on'、'off' 或 'auto'，默认：'auto'）" {
        arg <ON_OFF_AUTO>
    }
    flag "--kv-offload --no-kv-offload" help="是否启用 KV 缓存卸载（默认：启用）"
    flag "--direct-io --no-direct-io" help="已弃用，请使用 `--load-mode`：如果可用，使用 DirectIO"
    flag --numa help="尝试优化某些 NUMA 系统上的性能 - distribute：在所有节点上均匀分布执行 - isolate：仅在执行开始的节点上的 CPU 上生成线程 - numactl：使用 numactl 提供的 CPU 映射。如果之前未运行此命令，建议在使用此命令前丢弃系统页面缓存。参见 https://github.com/ggml-org/llama.cpp/issues/1437" {
        arg <TYPE>
    }
    flag --list-devices help=列出可用设备并退出
    flag --split-mode help="多 GPU 模型分割方式（默认：layer）" {
        arg <NONE_LAYER_ROW_TENSOR>
    }
    flag --fit-target help=每个设备的目标余量（MiB），默认：1024 {
        arg <MIB0_MIB1_MIB2>
    }
    flag --control-vector-scaled help=添加带缩放的控制向量（格式：FNAME:SCALE,...） {
        arg <FNAME_SCALE>
    }
    flag --docker-repo help="Docker Hub 模型仓库。repo 可选，默认为 ai/。quant 可选，默认为 :latest。示例：gemma3（默认：未使用）" {
        arg <REPO_MODEL_QUANT>
    }
    flag --hf-repo help="Hugging Face 模型仓库；quant 可选，不区分大小写，默认 Q4_K_M，如果 Q4_K_M 不存在则回退到仓库中的第一个文件。mmproj 如果可用也会自动下载。要禁用，请添加 --no-mmproj。示例：ggml-org/GLM-4.7-Flash-GGUF:Q4_K_M（默认：未使用）" {
        arg <USER_MODEL_QUANT>
    }
    flag "--spec-draft-type-k --cache-type-k-draft" help="草稿模型 K 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--spec-draft-type-v --cache-type-v-draft" help="草稿模型 V 的 KV 缓存数据类型（默认：f16）" {
        arg <TYPE>
    }
    flag "--sampler-seq --sampling-seq" help=采样器的简化序列（默认：edskypmxt） {
        arg <SEQUENCE>
    }
    flag "--kv-unified --no-kv-unified" help="使用跨所有序列共享的统一 KV 缓冲区"
    flag "--context-shift --no-context-shift" help=是否在无限文本生成时使用上下文移位（默认：禁用）
    flag "--save-all-logits --kl-divergence-base" help="设置 logits 文件" {
        arg <FNAME>
    }
}
cmd version help=显示版本
cmd licenses help=显示第三方许可
cmd help help=显示可用命令
__USAGE_EOF__
    # shellcheck disable=SC2207
	COMPREPLY=($(compgen -W "$(command usage complete-word --shell bash -f "$spec_file" --cword="$cword" -- "${words[@]}")" -- "$cur"))
	__ltrim_colon_completions "$cur"
    # shellcheck disable=SC2181
    if [[ $? -ne 0 ]]; then
        unset COMPREPLY
    fi
    return 0
}

if [[ "${BASH_VERSINFO[0]}" -eq 4 && "${BASH_VERSINFO[1]}" -ge 4 || "${BASH_VERSINFO[0]}" -gt 4 ]]; then
    shopt -u hostcomplete && complete -o nospace -o bashdefault -o nosort -F _llama llama
else
    shopt -u hostcomplete && complete -o nospace -o bashdefault -F _llama llama
fi
# vim: noet ci pi sts=0 sw=4 ts=4 ft=sh
