# @generated by usage-cli from llama-bench/zh-CN.usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in llama-bench." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_llama_bench 'name "LLaMA Bench (中文)"
bin llama-bench
version "0.4.0-dev"
about 模型性能基准测试工具
flag --numa help="NUMA 模式（默认：禁用）" {
    arg <TYPE>
}
flag "-r --repetitions" help=每个测试的重复次数（默认：5） {
    arg <N>
}
flag --prio help="进程/线程优先级（默认：0）" {
    arg <N>
}
flag --delay help=每次测试间的延迟（默认：0） {
    arg <0_N>
}
flag --list-devices help=列出可用设备并退出
flag --hf-repo help="Hugging Face 模型仓库；quant 可选，不区分大小写，默认 Q4_K_M，如果 Q4_K_M 不存在则回退到仓库中的第一个文件。示例：ggml-org/GLM-4.7-Flash-GGUF:Q4_K_M（默认：未使用）" {
    arg <USER_MODEL_QUANT>
}
flag --hf-file help="Hugging Face 模型文件。如果指定，将覆盖 --hf-repo 中的 quant（默认：未使用）" {
    arg <FILE>
}
flag --hf-token help="Hugging Face 访问令牌（默认：HF_TOKEN 环境变量的值）" {
    arg <TOKEN>
}
flag --rpc help="注册 RPC 设备（逗号分隔）" {
    arg <RPC_SERVERS>
}
flag "-o --output" help="stdout 输出格式（默认：md）" {
    arg <CSV_JSON_JSONL_MD_SQL>
}
flag --output-err help="stderr 输出格式（默认：无）" {
    arg <CSV_JSON_JSONL_MD_SQL>
}
flag "-v --verbose" help=详细输出
flag --progress help=打印测试进度指示器
flag --no-warmup help=跳过基准测试前的预热运行
flag --fit-target help=适配设备内存的余量（MiB，默认：关闭） {
    arg <MIB>
}
flag --fit-ctx help="--fit-target 的最小上下文大小（默认：4096）" {
    arg <N>
}
flag --offline help=离线模式：强制使用缓存，禁止网络访问（默认：禁用）
flag "-h --help" help=""
flag "-m --model" help="模型文件路径（默认：models/7B/ggml-model-q4_0.gguf）" {
    arg <FILENAME>
}
flag "-p --n-prompt" help=提示词大小（默认：512） {
    arg <N>
}
flag "-n --n-gen" help="生成的 token 数量（默认：128）" {
    arg <N>
}
flag "-d --n-depth" help=递归深度（默认：0） {
    arg <N>
}
flag "-b --batch-size" help=逻辑最大批处理大小（默认：2048） {
    arg <N>
}
flag --ubatch-size help=物理最大批处理大小（默认：512） {
    arg <N>
}
flag --cache-type-k help="KV 缓存 K 的数据类型（默认：f16）" {
    arg <T>
}
flag --cache-type-v help="KV 缓存 V 的数据类型（默认：f16）" {
    arg <T>
}
flag "-t --threads" help=线程数（默认：10） {
    arg <N>
}
flag "-C --cpu-mask" help="CPU 亲和性掩码（默认：0x0）" {
    arg <HEX_HEX>
}
flag --cpu-strict help="使用严格的 CPU 放置（默认：0）" {
    arg <N>
}
flag --poll help=使用轮询级别等待工作（默认：50） {
    arg <0_100>
}
flag --n-gpu-layers help=存储在显存中的最大层数（默认：-1） {
    arg <N>
}
flag --n-cpu-moe help="将前 N 层的 MoE 权重保留在 CPU 中（默认：0）" {
    arg <N>
}
flag --split-mode help=分割模式（默认：layer） {
    arg <NONE_LAYER_ROW_TENSOR>
}
flag --main-gpu help="主 GPU 索引（默认：0）" {
    arg <I>
}
flag --no-kv-offload help="不使用 KV 缓存卸载（默认：0）" {
    arg <N>
}
flag --flash-attn help=闪存注意力（默认：auto） {
    arg <ON_OFF_AUTO>
}
flag --device help=设备（默认：auto） {
    arg <DEV0_DEV1>
}
flag --load-mode help="" {
    arg <AUTO_NONE_MMAP_MLOCK_MMAP_MLOCK_DIO>
}
flag --lazy-mode help=延迟加载模式（默认：auto） {
    arg <ON_AUTO_OFF>
}
flag --mmap help="已弃用，请使用 --load-mode" {
    arg <N>
}
flag --direct-io help="已弃用，请使用 --load-mode" {
    arg <N>
}
flag --embeddings help=是否返回嵌入（默认：0） {
    arg <N>
}
flag --tensor-split help=张量分割比例（默认：0） {
    arg <TS0_TS1>
}
flag --override-tensor help=覆盖张量缓冲区类型（默认：disabled） {
    arg <TENSOR>
}
flag --no-op-offload help=不使用主机张量操作卸载（默认：0） {
    arg <N>
}
flag --no-host help=不使用主机（默认：0） {
    arg <N>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_llama_bench.spec"
echo $_usage_spec_llama_bench > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc llama-bench -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc llama-bench -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
