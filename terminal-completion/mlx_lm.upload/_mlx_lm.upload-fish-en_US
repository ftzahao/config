# @generated by usage-cli from mlx_lm.upload/usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.upload." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_mlx_lm_upload 'name "MLX Upload"
bin mlx_lm.upload
version "0.31.3"
about "Upload a model to the Hugging Face Hub"
flag "-h --help" help="show this help message and exit"
flag --path help="Path to the MLX model." {
    arg <PATH>
}
flag --upload-repo help="The Hugging Face repo to upload the model to." {
    arg <UPLOAD_REPO>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_mlx_lm_upload.spec"
echo $_usage_spec_mlx_lm_upload > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc mlx_lm.upload -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc mlx_lm.upload -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
