# @generated by usage-cli from mlx_lm.gptq/usage.kdl
module _mlx_lm_gptq_completions {
    const _usage_spec_mlx_lm_gptq = r##'name "MLX GPTQ"
bin mlx_lm.gptq
version "0.31.3"
flag "-h --help" help="show this help message and exit"
flag "-m --model" {
    arg <MODEL>
}
flag --mlx-path {
    arg <MLX_PATH>
}
flag --bits help="Quantization bits for GPTQ layers" {
    arg <BITS>
}
flag --group-size help="Quantization group size for GPTQ layers" {
    arg <GROUP_SIZE>
}
flag --fallback-bits help="Quantization bits for non-GPTQ layers" {
    arg <FALLBACK_BITS>
}
flag --fallback-group-size help="Quantization group size for non-GPTQ layers" {
    arg <FALLBACK_GROUP_SIZE>
}
flag --num-samples help="Number of samples from the calibration dataset, use -1 for all." {
    arg <NUM_SAMPLES>
}
flag --sequence-length help="Sequence length for the calibration data." {
    arg <SEQUENCE_LENGTH>
}
flag --seed {
    arg <SEED>
}
'##
    def mlx_lm_gptq_completer [spans: list<string>] {
        let spec_dir = ($env.XDG_CACHE_HOME? | default ($nu.home-dir? | default $nu.home-path? | path join ".cache") | path join "usage")
        if not ($spec_dir | path exists) {
            mkdir $spec_dir
            if $nu.os-info.family != "windows" { ^chmod 0700 $spec_dir }
        }
        let spec_file = ($spec_dir | path join $"usage__usage_spec_mlx_lm_gptq.spec")
        $_usage_spec_mlx_lm_gptq | save -f $spec_file

        (^usage complete-word -f $spec_file --shell nu -- ...$spans)
        | lines
        | each {|it| $it | split column "\t" | rename value description | into record }
    }

    @complete mlx_lm_gptq_completer
    export extern mlx_lm.gptq []
}


# if "usage" is not installed show an error
if (which usage | is-empty) {
    return (error make {
        msg: "Error: usage CLI not found. This is required for completions to work in mlx_lm.gptq.",
        help: "See https://usage.jdx.dev for more information."
    })
}
use _mlx_lm_gptq_completions *
