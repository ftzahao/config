# @generated by usage-cli from mlx_lm.gptq/zh-CN.usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.gptq." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_mlx_lm_gptq 'name "MLX GPTQ 量化"
bin mlx_lm.gptq
version "0.31.3"
flag "-h --help" help=显示此帮助信息并退出
flag "-m --model" {
    arg <MODEL>
}
flag --mlx-path help=保存模型的路径 {
    arg <MLX_PATH>
}
flag --bits help="GPTQ 层的量化位数" {
    arg <BITS>
}
flag --group-size help="GPTQ 层的量化组大小" {
    arg <GROUP_SIZE>
}
flag --fallback-bits help="非 GPTQ 层的量化位数" {
    arg <FALLBACK_BITS>
}
flag --fallback-group-size help="非 GPTQ 层的量化组大小" {
    arg <FALLBACK_GROUP_SIZE>
}
flag --num-samples help="校准数据集的样本数量，-1 表示全部。" {
    arg <NUM_SAMPLES>
}
flag --sequence-length help=校准数据的序列长度。 {
    arg <SEQUENCE_LENGTH>
}
flag --seed help=随机种子 {
    arg <SEED>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_mlx_lm_gptq.spec"
echo $_usage_spec_mlx_lm_gptq > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc mlx_lm.gptq -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc mlx_lm.gptq -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
