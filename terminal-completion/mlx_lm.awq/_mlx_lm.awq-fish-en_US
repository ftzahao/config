# @generated by usage-cli from mlx_lm.awq/usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.awq." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_mlx_lm_awq 'name "MLX AWQ"
bin mlx_lm.awq
version "0.31.3"
flag "-h --help" help="show this help message and exit"
flag "-m --model" {
    arg <MODEL>
}
flag --mlx-path {
    arg <MLX_PATH>
}
flag --bits {
    arg <BITS>
}
flag --group-size {
    arg <GROUP_SIZE>
}
flag --embed-bits {
    arg <EMBED_BITS>
}
flag --embed-group-size {
    arg <EMBED_GROUP_SIZE>
}
flag --num-samples {
    arg <NUM_SAMPLES>
}
flag --sequence-length {
    arg <SEQUENCE_LENGTH>
}
flag --n-grid {
    arg <N_GRID>
}
flag --seed {
    arg <SEED>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_mlx_lm_awq.spec"
echo $_usage_spec_mlx_lm_awq > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc mlx_lm.awq -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc mlx_lm.awq -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
