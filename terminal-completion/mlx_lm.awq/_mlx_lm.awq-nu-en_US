# @generated by usage-cli from mlx_lm.awq/usage.kdl
module _mlx_lm_awq_completions {
    const _usage_spec_mlx_lm_awq = r##'name "MLX AWQ"
bin mlx_lm.awq
version "0.31.3"
flag "-h --help" help="show this help message and exit"
flag "-m --model" {
    arg <MODEL>
}
flag --mlx-path {
    arg <MLX_PATH>
}
flag --bits {
    arg <BITS>
}
flag --group-size {
    arg <GROUP_SIZE>
}
flag --embed-bits {
    arg <EMBED_BITS>
}
flag --embed-group-size {
    arg <EMBED_GROUP_SIZE>
}
flag --num-samples {
    arg <NUM_SAMPLES>
}
flag --sequence-length {
    arg <SEQUENCE_LENGTH>
}
flag --n-grid {
    arg <N_GRID>
}
flag --seed {
    arg <SEED>
}
'##
    def mlx_lm_awq_completer [spans: list<string>] {
        let spec_dir = ($env.XDG_CACHE_HOME? | default ($nu.home-dir? | default $nu.home-path? | path join ".cache") | path join "usage")
        if not ($spec_dir | path exists) {
            mkdir $spec_dir
            if $nu.os-info.family != "windows" { ^chmod 0700 $spec_dir }
        }
        let spec_file = ($spec_dir | path join $"usage__usage_spec_mlx_lm_awq.spec")
        $_usage_spec_mlx_lm_awq | save -f $spec_file

        (^usage complete-word -f $spec_file --shell nu -- ...$spans)
        | lines
        | each {|it| $it | split column "\t" | rename value description | into record }
    }

    @complete mlx_lm_awq_completer
    export extern mlx_lm.awq []
}


# if "usage" is not installed show an error
if (which usage | is-empty) {
    return (error make {
        msg: "Error: usage CLI not found. This is required for completions to work in mlx_lm.awq.",
        help: "See https://usage.jdx.dev for more information."
    })
}
use _mlx_lm_awq_completions *
