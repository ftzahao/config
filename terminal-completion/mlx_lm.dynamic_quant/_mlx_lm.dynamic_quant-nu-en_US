# @generated by usage-cli from mlx_lm.dynamic_quant/usage.kdl
module _mlx_lm_dynamic_quant_completions {
    const _usage_spec_mlx_lm_dynamic_quant = r##'name "MLX Dynamic Quant"
bin mlx_lm.dynamic_quant
version "0.31.3"
about ""
flag "-h --help" help="show this help message and exit"
flag "-m --model" {
    arg <MODEL>
}
flag --mlx-path help="Path to save the model" {
    arg <MLX_PATH>
}
flag --seed {
    arg <SEED>
}
flag --sensitivities help="Path to a pre-computed sensitivity JSON file." {
    arg <SENSITIVITIES>
}
flag --target-bpw help="Target bits per weight." {
    arg <TARGET_BPW>
}
flag --low-bits {
    arg <LOW_BITS>
}
flag --low-group-size {
    arg <LOW_GROUP_SIZE>
}
flag --high-bits {
    arg <HIGH_BITS>
}
flag --high-group-size {
    arg <HIGH_GROUP_SIZE>
}
flag --report-ppl help="Compute the perplexity of the base and quantized models."
flag --grad-checkpoint help="Use gradient checkpointing to reduce memory use."
flag --accumulation-dtype help="What type to use to accumulate the gradients for the sensitivities" {
    arg <ACCUMULATION_DTYPE> {
        choices float32 bfloat16
    }
}
'##
    def mlx_lm_dynamic_quant_completer [spans: list<string>] {
        let spec_dir = ($env.XDG_CACHE_HOME? | default ($nu.home-dir? | default $nu.home-path? | path join ".cache") | path join "usage")
        if not ($spec_dir | path exists) {
            mkdir $spec_dir
            if $nu.os-info.family != "windows" { ^chmod 0700 $spec_dir }
        }
        let spec_file = ($spec_dir | path join $"usage__usage_spec_mlx_lm_dynamic_quant.spec")
        $_usage_spec_mlx_lm_dynamic_quant | save -f $spec_file

        (^usage complete-word -f $spec_file --shell nu -- ...$spans)
        | lines
        | each {|it| $it | split column "\t" | rename value description | into record }
    }

    @complete mlx_lm_dynamic_quant_completer
    export extern mlx_lm.dynamic_quant []
}


# if "usage" is not installed show an error
if (which usage | is-empty) {
    return (error make {
        msg: "Error: usage CLI not found. This is required for completions to work in mlx_lm.dynamic_quant.",
        help: "See https://usage.jdx.dev for more information."
    })
}
use _mlx_lm_dynamic_quant_completions *
