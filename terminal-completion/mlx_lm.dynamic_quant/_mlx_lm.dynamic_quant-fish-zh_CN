# @generated by usage-cli from mlx_lm.dynamic_quant/zh-CN.usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.dynamic_quant." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_mlx_lm_dynamic_quant 'name "MLX 动态量化"
bin mlx_lm.dynamic_quant
version "0.31.3"
about ""
flag "-h --help" help=显示此帮助信息并退出
flag "-m --model" {
    arg <MODEL>
}
flag --mlx-path help=保存模型的路径 {
    arg <MLX_PATH>
}
flag --seed {
    arg <SEED>
}
flag --sensitivities help="预先计算的敏感性 JSON 文件的路径。" {
    arg <SENSITIVITIES>
}
flag --target-bpw help=每个权重的目标比特数。 {
    arg <TARGET_BPW>
}
flag --low-bits {
    arg <LOW_BITS>
}
flag --low-group-size {
    arg <LOW_GROUP_SIZE>
}
flag --high-bits {
    arg <HIGH_BITS>
}
flag --high-group-size {
    arg <HIGH_GROUP_SIZE>
}
flag --report-ppl help=计算基础模型和量化模型的困惑度。
flag --grad-checkpoint help=使用梯度检查点以减少内存使用。
flag --accumulation-dtype help=用于累积敏感性梯度的数据类型 {
    arg <ACCUMULATION_DTYPE> {
        choices float32 bfloat16
    }
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_mlx_lm_dynamic_quant.spec"
echo $_usage_spec_mlx_lm_dynamic_quant > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc mlx_lm.dynamic_quant -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc mlx_lm.dynamic_quant -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
