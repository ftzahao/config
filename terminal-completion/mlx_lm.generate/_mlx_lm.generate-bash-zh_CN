# @generated by usage-cli from mlx_lm.generate/zh-CN.usage.kdl
# Requires bash-completion (https://github.com/scop/bash-completion), which must be
# sourced before this script.
_mlx_lm_generate() {
    if ! type -P usage &> /dev/null; then
        echo >&2
        echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.generate." >&2
        echo "See https://usage.jdx.dev for more information." >&2
        return 1
    fi
    if ! declare -F _init_completion > /dev/null 2>&1; then
        echo >&2
        echo "Error: bash-completion is required for mlx_lm.generate completions but was not loaded." >&2
        echo "Install it from your package manager and source it before this script." >&2
        return 1
    fi

	local cur prev words cword comp_args
    _init_completion -n : -- "$@" || return
    local spec_dir="${XDG_CACHE_HOME:-$HOME/.cache}/usage"
    [[ -d "$spec_dir" ]] || mkdir -p -m 700 "$spec_dir"
    local spec_file="$spec_dir/usage__usage_spec_mlx_lm_generate.spec"
    cat >| "$spec_file" <<'__USAGE_EOF__'
name "MLX 生成"
bin mlx_lm.generate
version "0.31.3"
about "LLM 推理脚本"
flag "-h --help" help=显示此帮助信息并退出
flag --model help="本地模型目录或 Hugging Face 仓库的路径。若未指定模型，则使用 mlx-community/Llama-3.2-3B-Instruct-4bit。" {
    arg <MODEL>
}
flag --trust-remote-code help=允许信任分词器的远程代码
flag --adapter-path help=已训练适配器权重和配置的可选路径。 {
    arg <ADAPTER_PATH>
}
flag --extra-eos-token help="在停止生成的 eos token 列表中添加 token。" {
    arg <EXTRA_EOS_TOKEN>… var=#true
}
flag --system-prompt help=用于聊天模板的系统提示词 {
    arg <SYSTEM_PROMPT>
}
flag "-p --prompt" help="要交给模型处理的消息（'-' 表示从 stdin 读取）" {
    arg <PROMPT>
}
flag --prefill-response help=用于聊天模板的预填充响应 {
    arg <PREFILL_RESPONSE>
}
flag "-m --max-tokens" help="生成的最大 token 数" {
    arg <MAX_TOKENS>
}
flag --temp help=采样温度 {
    arg <TEMP>
}
flag --top-p help="采样 top-p" {
    arg <TOP_P>
}
flag --min-p help="采样 min-p" {
    arg <MIN_P>
}
flag --top-k help="采样 top-k" {
    arg <TOP_K>
}
flag --xtc-probability help="每个下一 token 发生 XTC 采样的概率" {
    arg <XTC_PROBABILITY>
}
flag --xtc-threshold help="每个下一 token 候选被 XTC 采样的概率阈值" {
    arg <XTC_THRESHOLD>
}
flag --min-tokens-to-keep help="min-p 采样时保留的最少 token 数。" {
    arg <MIN_TOKENS_TO_KEEP>
}
flag --seed help="PRNG 随机种子" {
    arg <SEED>
}
flag --ignore-chat-template help=使用原始提示词，不应用分词器的聊天模板。
flag --use-default-chat-template help=使用默认聊天模板
flag --chat-template-config help="`apply_chat_template` 的附加配置。应为字符串键到值的字典，表示为可 JSON 解析的字符串。" {
    arg <CHAT_TEMPLATE_CONFIG>
}
flag --verbose help="'True' 或 'T' 时输出详细日志；'False' 或 'F' 时仅打印回复" {
    arg <VERBOSE>
}
flag --max-kv-size help=设置最大键值缓存大小 {
    arg <MAX_KV_SIZE>
}
flag --prompt-cache-file help="包含已保存 KV 缓存的文件，可避免重新计算" {
    arg <PROMPT_CACHE_FILE>
}
flag --quantize-activations help=使用对应层的量化配置对激活值进行量化。
flag --kv-bits help="KV 缓存量化位数。默认不量化。" {
    arg <KV_BITS>
}
flag --kv-group-size help="KV 缓存分组的组大小。" {
    arg <KV_GROUP_SIZE>
}
flag --quantized-kv-start help="设置 --kv-bits 后，从此步开始量化 KV 缓存。" {
    arg <QUANTIZED_KV_START>
}
flag --draft-model help=用于投机解码的模型。 {
    arg <DRAFT_MODEL>
}
flag --num-draft-tokens help="使用投机解码时要预生成的 token 数量。" {
    arg <NUM_DRAFT_TOKENS>
}
__USAGE_EOF__
    # shellcheck disable=SC2207
	COMPREPLY=($(compgen -W "$(command usage complete-word --shell bash -f "$spec_file" --cword="$cword" -- "${words[@]}")" -- "$cur"))
	__ltrim_colon_completions "$cur"
    # shellcheck disable=SC2181
    if [[ $? -ne 0 ]]; then
        unset COMPREPLY
    fi
    return 0
}

if [[ "${BASH_VERSINFO[0]}" -eq 4 && "${BASH_VERSINFO[1]}" -ge 4 || "${BASH_VERSINFO[0]}" -gt 4 ]]; then
    shopt -u hostcomplete && complete -o nospace -o bashdefault -o nosort -F _mlx_lm_generate mlx_lm.generate
else
    shopt -u hostcomplete && complete -o nospace -o bashdefault -F _mlx_lm_generate mlx_lm.generate
fi
# vim: noet ci pi sts=0 sw=4 ts=4 ft=sh
