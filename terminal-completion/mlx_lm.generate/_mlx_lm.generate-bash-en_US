# @generated by usage-cli from mlx_lm.generate/usage.kdl
# Requires bash-completion (https://github.com/scop/bash-completion), which must be
# sourced before this script.
_mlx_lm_generate() {
    if ! type -P usage &> /dev/null; then
        echo >&2
        echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.generate." >&2
        echo "See https://usage.jdx.dev for more information." >&2
        return 1
    fi
    if ! declare -F _init_completion > /dev/null 2>&1; then
        echo >&2
        echo "Error: bash-completion is required for mlx_lm.generate completions but was not loaded." >&2
        echo "Install it from your package manager and source it before this script." >&2
        return 1
    fi

	local cur prev words cword comp_args
    _init_completion -n : -- "$@" || return
    local spec_dir="${XDG_CACHE_HOME:-$HOME/.cache}/usage"
    [[ -d "$spec_dir" ]] || mkdir -p -m 700 "$spec_dir"
    local spec_file="$spec_dir/usage__usage_spec_mlx_lm_generate.spec"
    cat >| "$spec_file" <<'__USAGE_EOF__'
name "MLX Generate"
bin mlx_lm.generate
version "0.31.3"
about "LLM inference script"
flag "-h --help" help="show this help message and exit"
flag --model help="The path to the local model directory or Hugging Face repo. If no model is specified, then mlx-community/Llama-3.2-3B-Instruct-4bit is used." {
    arg <MODEL>
}
flag --trust-remote-code help="Enable trusting remote code for tokenizer"
flag --adapter-path help="Optional path for the trained adapter weights and config." {
    arg <ADAPTER_PATH>
}
flag --extra-eos-token help="Add tokens in the list of eos tokens that stop generation." {
    arg <EXTRA_EOS_TOKEN>… var=#true
}
flag --system-prompt help="System prompt to be used for the chat template" {
    arg <SYSTEM_PROMPT>
}
flag "-p --prompt" help="Message to be processed by the model ('-' reads from stdin)" {
    arg <PROMPT>
}
flag --prefill-response help="Prefill response to be used for the chat template" {
    arg <PREFILL_RESPONSE>
}
flag "-m --max-tokens" help="Maximum number of tokens to generate" {
    arg <MAX_TOKENS>
}
flag --temp help="Sampling temperature" {
    arg <TEMP>
}
flag --top-p help="Sampling top-p" {
    arg <TOP_P>
}
flag --min-p help="Sampling min-p" {
    arg <MIN_P>
}
flag --top-k help="Sampling top-k" {
    arg <TOP_K>
}
flag --xtc-probability help="Probability of XTC sampling to happen each next token" {
    arg <XTC_PROBABILITY>
}
flag --xtc-threshold help="Thresold the probs of each next token candidate to be sampled by XTC" {
    arg <XTC_THRESHOLD>
}
flag --min-tokens-to-keep help="Minimum tokens to keep for min-p sampling." {
    arg <MIN_TOKENS_TO_KEEP>
}
flag --seed help="PRNG seed" {
    arg <SEED>
}
flag --ignore-chat-template help="Use the raw prompt without the tokenizer's chat template."
flag --use-default-chat-template help="Use the default chat template"
flag --chat-template-config help="Additional config for `apply_chat_template`. Should be a dictionary of string keys to values represented as a JSON decodable string." {
    arg <CHAT_TEMPLATE_CONFIG>
}
flag --verbose help="Log verbose output when 'True' or 'T' or only print the response when 'False' or 'F'" {
    arg <VERBOSE>
}
flag --max-kv-size help="Set the maximum key-value cache size" {
    arg <MAX_KV_SIZE>
}
flag --prompt-cache-file help="A file containing saved KV caches to avoid recomputing them" {
    arg <PROMPT_CACHE_FILE>
}
flag --quantize-activations help="Quantize activations using the same quantization config as the corresponding layer."
flag --kv-bits help="Number of bits for KV cache quantization. Defaults to no quantization." {
    arg <KV_BITS>
}
flag --kv-group-size help="Group size for KV cache quantization." {
    arg <KV_GROUP_SIZE>
}
flag --quantized-kv-start help="When --kv-bits is set, start quantizing the KV cache from this step onwards." {
    arg <QUANTIZED_KV_START>
}
flag --draft-model help="A model to be used for speculative decoding." {
    arg <DRAFT_MODEL>
}
flag --num-draft-tokens help="Number of tokens to draft when using speculative decoding." {
    arg <NUM_DRAFT_TOKENS>
}
__USAGE_EOF__
    # shellcheck disable=SC2207
	COMPREPLY=($(compgen -W "$(command usage complete-word --shell bash -f "$spec_file" --cword="$cword" -- "${words[@]}")" -- "$cur"))
	__ltrim_colon_completions "$cur"
    # shellcheck disable=SC2181
    if [[ $? -ne 0 ]]; then
        unset COMPREPLY
    fi
    return 0
}

if [[ "${BASH_VERSINFO[0]}" -eq 4 && "${BASH_VERSINFO[1]}" -ge 4 || "${BASH_VERSINFO[0]}" -gt 4 ]]; then
    shopt -u hostcomplete && complete -o nospace -o bashdefault -o nosort -F _mlx_lm_generate mlx_lm.generate
else
    shopt -u hostcomplete && complete -o nospace -o bashdefault -F _mlx_lm_generate mlx_lm.generate
fi
# vim: noet ci pi sts=0 sw=4 ts=4 ft=sh
