# @generated by usage-cli from mlx_lm.dwq/usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.dwq." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_mlx_lm_dwq 'name "MLX DWQ"
bin mlx_lm.dwq
version "0.31.3"
flag "-h --help" help="show this help message and exit"
flag "-m --model" help="A model to distill from for DWQ. If `quantized-model` is not given the student model will be this model quantized according to `bits` and `group-size`." {
    arg <MODEL>
}
flag --quantized-model help="An already quantized model (the student model) to improve with DWQ." {
    arg <QUANTIZED_MODEL>
}
flag --mlx-path help="Path to save the quantized model." {
    arg <MLX_PATH>
}
flag --bits help="Bits per weight for quantization." {
    arg <BITS>
}
flag --group-size help="Group size for quantization." {
    arg <GROUP_SIZE>
}
flag --num-samples help="Number of samples to use for training." {
    arg <NUM_SAMPLES>
}
flag --max-seq-length {
    arg <MAX_SEQ_LENGTH>
}
flag --seed {
    arg <SEED>
}
flag --learning-rate {
    arg <LEARNING_RATE>
}
flag --batch-size {
    arg <BATCH_SIZE>
}
flag --data-path help="A Hugging Face dataset which is compatible with an mlx-lm dataset format." {
    arg <DATA_PATH>
}
flag --grad-checkpoint help="Use gradient checkpointing to reduce memory use."
flag --target-dir help="Directory to save/load targets." {
    arg <TARGET_DIR>
}
flag --targets-only help="Compute the targets and exit."
flag --pipeline help="Use pipeline parallel instead of data parallel."
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_mlx_lm_dwq.spec"
echo $_usage_spec_mlx_lm_dwq > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc mlx_lm.dwq -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc mlx_lm.dwq -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
