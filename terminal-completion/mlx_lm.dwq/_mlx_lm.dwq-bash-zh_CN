# @generated by usage-cli from mlx_lm.dwq/zh-CN.usage.kdl
# Requires bash-completion (https://github.com/scop/bash-completion), which must be
# sourced before this script.
_mlx_lm_dwq() {
    if ! type -P usage &> /dev/null; then
        echo >&2
        echo "Error: usage CLI not found. This is required for completions to work in mlx_lm.dwq." >&2
        echo "See https://usage.jdx.dev for more information." >&2
        return 1
    fi
    if ! declare -F _init_completion > /dev/null 2>&1; then
        echo >&2
        echo "Error: bash-completion is required for mlx_lm.dwq completions but was not loaded." >&2
        echo "Install it from your package manager and source it before this script." >&2
        return 1
    fi

	local cur prev words cword comp_args
    _init_completion -n : -- "$@" || return
    local spec_dir="${XDG_CACHE_HOME:-$HOME/.cache}/usage"
    [[ -d "$spec_dir" ]] || mkdir -p -m 700 "$spec_dir"
    local spec_file="$spec_dir/usage__usage_spec_mlx_lm_dwq.spec"
    cat >| "$spec_file" <<'__USAGE_EOF__'
name "MLX DWQ 量化"
bin mlx_lm.dwq
version "0.31.3"
flag "-h --help" help=显示此帮助信息并退出
flag "-m --model" help="DWQ 蒸馏所用的模型。若未指定 `quantized-model`，学生模型将按 `bits` 和 `group-size` 对此模型量化。" {
    arg <MODEL>
}
flag --quantized-model help="已量化的模型（学生模型），用 DWQ 进行改进。" {
    arg <QUANTIZED_MODEL>
}
flag --mlx-path help=保存量化模型的路径。 {
    arg <MLX_PATH>
}
flag --bits help=每个权重的量化位数。 {
    arg <BITS>
}
flag --group-size help=量化的组大小。 {
    arg <GROUP_SIZE>
}
flag --num-samples help=训练使用的样本数量。 {
    arg <NUM_SAMPLES>
}
flag --max-seq-length help=最大序列长度 {
    arg <MAX_SEQ_LENGTH>
}
flag --seed help=随机种子 {
    arg <SEED>
}
flag --learning-rate help=学习率 {
    arg <LEARNING_RATE>
}
flag --batch-size help=批大小 {
    arg <BATCH_SIZE>
}
flag --data-path help="兼容 mlx-lm 数据集格式的 Hugging Face 数据集。" {
    arg <DATA_PATH>
}
flag --grad-checkpoint help=使用梯度检查点以减少内存占用。
flag --target-dir help="保存/加载目标的目录。" {
    arg <TARGET_DIR>
}
flag --targets-only help=仅计算目标后退出。
flag --pipeline help=使用流水线并行而非数据并行。
__USAGE_EOF__
    # shellcheck disable=SC2207
	COMPREPLY=($(compgen -W "$(command usage complete-word --shell bash -f "$spec_file" --cword="$cword" -- "${words[@]}")" -- "$cur"))
	__ltrim_colon_completions "$cur"
    # shellcheck disable=SC2181
    if [[ $? -ne 0 ]]; then
        unset COMPREPLY
    fi
    return 0
}

if [[ "${BASH_VERSINFO[0]}" -eq 4 && "${BASH_VERSINFO[1]}" -ge 4 || "${BASH_VERSINFO[0]}" -gt 4 ]]; then
    shopt -u hostcomplete && complete -o nospace -o bashdefault -o nosort -F _mlx_lm_dwq mlx_lm.dwq
else
    shopt -u hostcomplete && complete -o nospace -o bashdefault -F _mlx_lm_dwq mlx_lm.dwq
fi
# vim: noet ci pi sts=0 sw=4 ts=4 ft=sh
