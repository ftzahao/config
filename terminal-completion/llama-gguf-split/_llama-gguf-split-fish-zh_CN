# @generated by usage-cli from llama-gguf-split/zh-CN.usage.kdl

# if "usage" is not installed show an error
if ! type -P usage &> /dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in llama-gguf-split." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_llama_gguf_split 'name "LLaMA GGUF Split (中文)"
bin llama-gguf-split
version "0.4.0-dev"
about "GGUF 文件拆分/合并工具"
flag "-h --help" help=显示帮助信息并退出
flag --version help=显示版本和构建信息
flag --split help="将 GGUF 拆分为多个 GGUF（默认启用）"
flag --merge help="将多个 GGUF 合并为单个 GGUF"
flag --split-max-tensors help=每个分片的最大张量数（默认：128）
flag --dry-run help=仅打印拆分计划并退出，不写入任何新文件
flag --delete-splits help="合并期间删除分片文件以释放磁盘空间（⚠️ 不安全选项）"
flag --split-max-size help="" {
    arg <N_M_G>
}
flag --no-tensor-first-split help="" {
    arg <DO>
}
'
set -l spec_dir (if set -q XDG_CACHE_HOME; echo $XDG_CACHE_HOME; else; echo $HOME/.cache; end)/usage
test -d "$spec_dir"; or mkdir -p -m 700 "$spec_dir"
set -l spec_file "$spec_dir/usage__usage_spec_llama_gguf_split.spec"
echo $_usage_spec_llama_gguf_split > "$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc llama-gguf-split -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc llama-gguf-split -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
